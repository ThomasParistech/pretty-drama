# PrettyDrama Voices

Outil libre pour troupes de théâtre : répétition « à l'italienne » avec les vraies
voix des acteurs. Site statique (GitHub Pages) + GitHub Action Python/ffmpeg.
Tout est en français côté utilisateur (UI, README, messages d'erreur de
l'Action, qui finissent affichés dans le journal des dépôts).
Convention d'écriture : jamais de tiret cadratin, ni dans cette doc, ni dans
les textes affichés à l'utilisateur. Utiliser deux-points, point-virgule,
virgule, parenthèses ou une phrase de plus.

## Architecture

- **Frontend** : React + Vite, multi-pages (pas de SPA). Entrées déclarées dans
  `vite.config.js` : `index.html` (accueil acteurs), `respo.html` (accueil
  responsable), `rehearsal.html`, `recorder.html`,
  `dashboard.html`, `editor.html`. Sources dans `src/<page>/`, partagé dans
  `src/shared/` (`data.js` = fetch + helpers, `useManifest.js`,
  `PageHeader.jsx` (bandeau des écrans sans pièce à nommer, de la MÊME géométrie
  que `PlayHeader` : sceau et titre en rangée du haut, `HomeLink` en pied ; ces
  écrans sont l'attente des quatre pages à bandeau de pièce, et tant que la
  marque vivait ici en haut à gauche et là en pied, le nom du site sautait d'un
  bout à l'autre du bandeau à l'arrivée du manifest),
  `PageState.jsx` = écran chargement/erreur commun (ses DEUX états, l'attente
  comme le message, vivent dans la carte partagée `.page-notice` de `theme.css`,
  comme tout ce qui porte du contenu sur ce site ; en texte nu sur le crème ces
  écrans étaient les seuls à ne rien poser sur le papier et se lisaient comme une
  page à moitié chargée, et une carte qui n'arriverait qu'au second état se
  lirait comme un changement de page). **Aucun bandeau du site n'écrit son
  libellé de page** (« Répétition », « Édition », « Avancement ») : le sceau dit
  où on est, et l'onglet du navigateur le répète. `title` ne dit donc QUE le
  titre de la pièce, et il est **facultatif** : tant que la pièce est inconnue,
  la rangée du haut n'a rien à dire et le titre n'est pas rendu du tout
  (`PageHeader` ne pose même pas le `<span>`). C'est la seule façon que le titre
  **apparaisse** au lieu d'en **remplacer** un autre : un libellé écrit pendant
  le chargement se faisait recouvrir par le titre de la pièce une fraction de
  seconde plus tard, et ce clignotement se voyait à chaque ouverture de page.
  Le vide ne coûte rien, la hauteur de la rangée étant fixée par le sceau (36 px)
  et non par le titre (24 px de ligne) : rien ne bouge quand il arrive, et le
  message de l'écran, lui, est dans la carte au-dessous. Les écrans **définitifs**
  nomment donc la pièce comme les quatre bandeaux, et il y en a **trois**, tous
  rendus après le chargement, exprès : l'Édition ouverte au doigt,
  l'Enregistrement sur un navigateur qui ne sait pas enregistrer, et la
  Répétition d'une pièce encore vide. Un écran où l'on reste est le contenu final
  de la page, pas son attente. Corollaire : le repli quand la pièce n'a pas de
  titre est « Pièce sans titre » partout, sur les quatre bandeaux comme sur ces
  trois écrans (il valait « Répétition » ici et « Pièce sans titre » là). Le
  titre lui-même vit dans **une seule règle CSS** pour les deux bandeaux
  (`.play-header-title, .page-header .page-title`) : il était rendu en 15 px
  gris dans la font UI d'un côté et en serif 20 px de l'autre, donc il changeait
  de corps, de graisse, de couleur ET de fonte selon le bandeau qui le portait
  (et prenait au passage l'IBM Plex du re-skin sur l'Édition, alors que
  `--header-serif` existe pour l'empêcher). `.page-header` porte pour la même
  raison le `--header-shadow` de `.play-header`, sinon le bandeau *gagnait* son
  ombre en cours de route,
  `HomeLink.jsx` = le retour à l'accueil (logo aux deux masques + le mot
  « PrettyDrama », encadré de deux filets courts), **un seul composant pour les
  deux bandeaux** : c'est ce qui garantit que la marque est au même endroit
  pendant le chargement et après,
  `PageMark.jsx` = la pastille de sceau, qui pose la classe `page-<clé>` sur
  elle-même (elle porte donc ses couleurs partout, y compris hors d'un bandeau).
  Deux autres endroits la posent : les deux bandeaux partagés sur leur racine
  (les tokens de sceau sont ainsi disponibles à tout leur contenu ; plus rien ne
  les y consomme depuis le retrait du filet de 3 px), et le lien de `HomeLink`,
  en `page-home`, lui pour une vraie raison, la nappe crème de son survol, qui
  doit rester celle de la marque sur les quatre pages (le garde CI de
  `test_contracts.py` interdit les tokens de sceau aux règles de bandeau et
  exempte nommément `.play-header-home*`, cette classe posée en JSX lui étant
  invisible),
  `useScrollToActiveCard.js` = défilement vers la `.dialogue-card.active`,
  partagé par Répétition et Enregistrement, `theme.css`). `dist/` est
  gitignoré, build en CI.
- **Backend** : aucun serveur, **deux workflows** aux rôles étanches.
  `uploads.yml` (sur push touchant `uploads/**`) traite les dépôts, écrit le
  journal, commite, **puis appelle `build.yml`** (`jobs.site.uses`) ;
  `build.yml` (sur tout autre push, `workflow_dispatch`, `workflow_call`)
  construit et déploie Pages et n'écrit **jamais** dans le dépôt. Trois choses
  à ne pas défaire : (1) l'appel explicite, parce qu'un commit poussé avec le
  `GITHUB_TOKEN` ne déclenche aucun workflow (garde anti-récursion), donc sans
  lui le site ne serait jamais republié après un dépôt ; (2) les **groupes de
  `concurrency` distincts** (`uploads` et `pages`), un workflow appelé qui
  partage le groupe de son appelant étant détecté comme interblocage et annulé ;
  (3) le `paths-ignore: uploads/**` de `build.yml`, sans quoi un dépôt
  déclencherait les deux en parallèle et publierait un site sans les nouvelles
  voix. `build.yml` fait son checkout sur `ref: github.ref_name` et pas sur le
  SHA du run : appelé, il tourne dans le run d'un push antérieur au commit
  qu'`uploads.yml` vient de pousser.
  **Aucun des deux n'écrit à destination du respo sur GitHub** : ni issue, ni
  statut dans le README (il part du bouton de la page Avancement et ne visite
  jamais le dépôt, donc il ne les lirait pas). Son seul retour est le journal
  des dépôts de la page Avancement. Corollaires assumés : un run en échec ne se
  raconte pas (mais la date du dernier dépôt cesse d'avancer), et le commit
  précède le déploiement (l'inverse imposerait de transporter `clips/` et
  `data/` en artefact entre deux jobs ; si le déploiement échoue, les données
  sont dans le dépôt et le prochain build les publie).
- Dev : `npm run dev` (un middleware Vite sert `data/` et `clips/` depuis le
  repo avec de vrais 404), ou `npm start` = `scripts/dev.sh` (install si besoin,
  ouverture des DEUX accueils dans le navigateur dès que le serveur répond ;
  `exec ./node_modules/.bin/vite`, pas `npm run dev`, pour que Vite prenne la
  place du script : le Ctrl+C tue vraiment le serveur et libère le port, sans
  wrapper npm ni pid à surveiller), `npm run build`. **Deux suites de tests**,
  toutes deux rejouées par `build.yml`, donc à chaque push de code comme après
  chaque dépôt (`uploads.yml` finit par l'appeler) :
  - Python, `python3 -m unittest discover -s scripts/tests` : statuts,
    normalisation (cas partagés dans `normalize-cases.json`), contrat ZIP,
    journal, plus les **contrats inter-fichiers** (`test_contracts.py`, voir
    plus bas). Un seul test :
    `python3 -m unittest scripts.tests.test_normalize.TestNormalizeSharedCases.test_idempotent`.
  - JS, `npm test` = `node --test` **sans argument** : la logique **pure** du
    front, celle qui ne se relit pas à l'œil. `reducer.test.js` (réparation d'un
    `script.json` douteux, ids jamais recyclés, qui parle après une
    suppression), `history.test.js` (fusion des frappes, « Modifications non
    téléchargées »), `data.test.js` (URL de dépôt, slugs), `useRecorder.test.js`
    (l'extension du membre audio reste dans l'alphabet qu'accepte l'Action).
    **Aucune dépendance de test** : `node --test` est intégré à Node, ces
    modules sont du JS pur sans React ni DOM, et un fork de troupe ne doit pas
    payer un `npm ci` plus lourd pour ça. Pas de test de composant React, donc
    pas de rendu : ce qui touche au DOM se vérifie toujours à la main.
    **Pas d'argument de chemin**, et c'est voulu : `node --test src/` marchait
    sur Node 20 mais Node 22+ traite un positionnel comme un fichier de test, pas
    comme un dossier à parcourir (« Cannot find module .../src »). Sans argument,
    Node applique ses motifs par défaut (`**/*.test.js`, `node_modules` exclu) et
    trouve exactement les mêmes fichiers, de Node 20 à Node 24. Contrepartie
    assumée : le périmètre n'est plus `src/` mais tout le dépôt, donc un futur
    `test-*.js`, `*-test.js` ou un dossier nommé `test/` (au singulier, motif
    `**/test/**`) serait exécuté sans qu'on l'ait demandé ; `scripts/tests/` y
    échappe parce qu'il est au pluriel. La piste écartée est le glob explicite
    `node --test 'src/**/*.test.js'` : correct sur Node 22+ seulement, et ses
    guillemets simples ne sont pas des guillemets pour le `cmd.exe` que npm
    utilise sous Windows.

  Build de prod à tester à la main (l'Action copie `data/` et `clips/` dans
  `dist/`) : `npm run build && cp -r data clips dist/ && npm run preview`.

## Flux de données

1. `data/script.json` : **source de vérité**, produit par l'éditeur. Il n'est
   plus déposé par-dessus : il arrive dans `uploads/`, est validé
   (`validate_script`) puis promu. Reste potentiellement malformé (édition à la
   main dans le dépôt) : tout consommateur doit être tolérant, cf.
   `sanitize_script`.
2. Page Enregistrement → ZIP `voix-<slug>.zip` contenant :
   - `manifest.json` : **mapping nu `{lineId: texte brut au moment de
     l'enregistrement}`** (rien d'autre : ni nom de fichier ni personnage) ;
   - un audio `{lineId}.{ext}` par réplique (`ext` = webm/mp4/ogg selon le
     navigateur, cf. `extensionForMimeType`). L'Action retrouve le fichier
     depuis l'id seul.
3. L'acteur envoie le ZIP au responsable, qui le glisse dans `uploads/`. **Un
   seul dossier de dépôt** pour les deux sortes de fichiers (le respo n'a qu'une
   adresse à connaître, d'où le bouton unique de l'Avancement) ; le type vient
   de la seule extension (`kind_of` : `.zip` = voix, `.json` = script), pas du
   nom, que le navigateur renomme volontiers en « script (1).json ».
4. `scripts/process_uploads.py`, pour chaque fichier de `uploads/` :
   - `.zip` : valide TOUT le manifest, transcode chaque clip (ffmpeg : trim
     silences + loudnorm, mp3 mono 64 kbps) → `clips/{lineId}.mp3` et
     `data/clips.json` (`{lineId: texte brut}`). Merge **tout-ou-rien par ZIP** ;
   - `.json` : `validate_script` puis promotion **verbatim** dans
     `data/script.json` (passer par `sanitize_script` perdrait les teintes des
     personnages) ;
   - autre : consigné comme inconnu.
   Le fichier est supprimé même en cas d'erreur (sinon il échouerait à chaque
   run) ; les fichiers cachés (`.gitkeep`) sont laissés en place. Sort de chaque
   fichier (succès **et** erreur) écrit dans `uploads_result.json` (éphémère,
   gitignoré).
5. `scripts/update_history.py` : `uploads_result.json` → une entrée
   `{at, files}` dans `data/history.json` (journal des dépôts affiché par
   l'Avancement, plafonné à `MAX_RUNS`). Écrit par `uploads.yml` seul, donc le
   journal ne contient QUE des dépôts, jamais une reconstruction du site.
6. `scripts/build_manifest.py` : join stateless `script.json` × `clips.json` →
   `data/manifest.json`, **seul fichier lu par les pages** (l'éditeur lit aussi
   `script.json`) ; y recopie aussi `history.json`. Statut par réplique : `ok` / `perime` (« À refaire », texte modifié depuis
   l'enregistrement) / `manquant` (« À enregistrer »).
7. `scripts/build_script_pdf.py` : `script.json` → `data/script.pdf`, le script
   de la pièce mis en page pour l'impression (LaTeX). Dérivé comme le manifest,
   mais **gitignoré** et construit par `build.yml` seul, donc jamais commité.

## Invariants (à ne pas casser)

- **Normalisation de texte : une seule implémentation**, `scripts/normalize.py`,
  appelée uniquement dans `build_manifest.compute_status`. Le navigateur stocke
  et transporte du texte **brut**, jamais normalisé.
- **Les ids de répliques ne sont jamais recyclés** (ils nomment les mp3).
  `SAFE_ID` (`src/editor/reducer.js`) et `LINE_ID_PATTERN`
  (`scripts/process_uploads.py`) doivent rester synchrones :
  `^[0-9a-zA-Z-]{1,64}$` (alphanumérique, pas seulement hex : ids lisibles
  édités à la main acceptés). Ce n'est plus une consigne mais un test :
  `test_contracts.py` lit les deux fichiers et compare les deux expressions,
  donc les faire diverger casse la CI.
- Le format du ZIP est un contrat navigateur ↔ Action : toute modification doit
  toucher `downloadZip` (recorder `App.jsx`) ET `parse_manifest`
  (`process_uploads.py`) en même temps.
- `sanitize_script` (Python) est le miroir tolérant du `sanitizeScript` de
  l'éditeur : une entrée malformée est ignorée, jamais un crash de workflow.
  Asymétrie assumée : le JS valide chaque id contre `SAFE_ID` et **reminte** les
  ids invalides/dupliqués (il est le producteur, doit rester réparable) ; le
  Python ne vérifie qu'« id = chaîne non vide » (il ne peut pas reminter sans
  orpheliner les mp3 déjà nommés). C'est sans conséquence : un id hors `SAFE_ID`
  hand-édité dans `script.json` n'a jamais de clip (l'Action rejette ces ids à
  l'upload) → statut `manquant`, `clip: null`, aucune URL forgée n'est émise.
- **`sanitizeScript` ne déplace jamais une réplique d'un personnage à un
  autre.** Deux cas à ne pas confondre quand un id de personnage est reminté :
  hors `SAFE_ID`, l'id n'est porté que par lui, donc ses répliques le suivent
  (`characterRemap`) ; **dupliqué**, le premier porteur garde l'id ET ses
  répliques, le second repart avec un id neuf et aucune réplique. Remapper un
  doublon changerait qui parle dans la pièce alors que les mp3, nommés par id
  de *réplique*, ne bougeraient pas : la voix enregistrée par l'un sortirait
  sous le nom de l'autre. Corollaire de tuyauterie : le remap se consulte sur
  la valeur brute du `characterId`, **avant** tout contrôle contre `SAFE_ID`,
  puisque les ids qu'il contient sont justement ceux qui n'y passent pas.
- **Un no-op ne doit pas fabriquer un nouvel état.** `updateScene`
  (`reducer.js`) rend l'état reçu tel quel quand la scène ne change pas, et
  `scriptReducer` en fait autant pour toute action refusée. `history.js`
  reconnaît une action sans effet à l'identité (`present === state.present`) :
  sans cette sortie, reposer une réplique glissée exactement où elle était
  allumait « Modifications non téléchargées » et laissait une étape vide à
  annuler.
- Uploads hostiles : caps de taille réels (les en-têtes ZIP mentent), noms de
  membres validés par fullmatch, un fichier cassé ne bloque jamais les autres.
- **`validate_script` est volontairement plus strict que `sanitize_script`** :
  le second est un lecteur tolérant, le premier décide d'écraser la source de
  vérité. Un JSON valide mais étranger (`[1,2,3]`, un export d'autre chose) se
  sanitiserait en pièce vide et effacerait la pièce de la troupe, d'où le
  garde-fou : un candidat sans aucune réplique ne remplace jamais une pièce qui
  en a.
- Prises d'enregistrement : en mémoire uniquement (garde `LeaveGuard` tant que
  non exportées), une seule prise par réplique, `URL.revokeObjectURL` à chaque
  remplacement.

## Repères rapides

| Quoi | Où |
| --- | --- |
| Lien GitHub affiché (footer accueil) | `src/home/App.jsx`, repo `ThomasParistech/prettydrama-voices` |
| Les deux accueils | même composant `src/home/App.jsx`, deux entrées et deux listes de cartes (`ACTOR_CARDS`/`RESPO_CARDS` dans `src/shared/pages.js`) : `index.html` + `src/home/main.jsx` ne montre que Répétition et Enregistrement (c'est le lien donné à la troupe : un acteur ne doit jamais tomber sur l'éditeur, d'où se télécharge le script), `respo.html` + `src/home/respo.jsx` montre les quatre pages en carré 2x2. **Aucun lien ne mène de l'accueil des acteurs vers celui du responsable** (sinon la séparation ne sert à rien) : il se bookmarke, et l'adresse à retenir est le raccourci `…/respo` (sans `.html`), servi par `public/respo/index.html`, une redirection `meta refresh` vers `../respo.html`. C'est une redirection et pas une entrée Vite `respo/index.html` parce que tout le site vit en chemins relatifs (`base: "./"`, `fetch("data/manifest.json")`, `href="./rehearsal.html"`) : une vraie page à un niveau de profondeur obligerait le code partagé (`data.js`, `pages.js`, `homeHref`) à savoir dans quel dossier elle tourne. Contrepartie assumée : la barre d'adresse finit sur `respo.html`. Corollaire porté par `homeHref(page)` (`pages.js`) et consommé par les deux bandeaux partagés : le retour à l'accueil des pages Édition et Avancement mène à `respo.html`, celui des deux autres à `index.html` (sur les pages à bandeau repliable ce n'est plus la marque qui porte ce lien, cf. la ligne « Bandeau de pièce »). Les cartes n'ont plus de sous-titre « Pour les acteurs / Pour le responsable » : l'URL le dit. `npm run dev` et `npm run preview` annoncent les deux URLs (plugin `printHomeUrls` dans `vite.config.js`, qui enveloppe `server.printUrls`) : l'accueil caché n'est pas à retenir de tête |
| Couleurs des sceaux | `.page-<clé>` dans `src/shared/theme.css` (`--page-mark` / `--page-mark-soft`), **source de vérité** ; la couleur de page ne teinte plus QUE la pastille : le filet de 3 px qu'elle posait en haut de chaque écran (bandeaux et accueil) est retiré, il barrait la page d'un trait dont le sceau disait déjà tout ; les favicons et `theme-color` des `.html` les redupliquent (un `<link>` ne lit pas une variable CSS). Les trois pages de la troupe (marque, Répétition, Enregistrement) partagent EXACTEMENT le même couple, bordeaux `#8b2635` sur sable `#f5eeda` : elles avaient chacune sa nuance (deux bordeaux voisins et une terre), assez proches pour qu'on n'y lise pas une distinction, assez différentes pour qu'on les croie ratées. Ce sont les icônes (masques, bulles, micro) qui disent la page. Seuls les modes du responsable gardent une couleur propre, vert pour l'Avancement et violet pour l'Édition : elle ne sépare plus deux pages voisines, elle marque un autre territoire. Corollaire : la terre `#a84f00` n'est plus qu'un statut (`--warn`, « À enregistrer »), plus une identité de page |
| Logo (les deux masques) | `MasksIcon` dans `src/shared/icons.jsx`. **Géométrie fournie par un fichier de design (`design/drama-wine.svg`, la livraison est versionnée, cf. `design/README.md`), à ne pas redessiner** : les 8 tracés et leurs `transform` sont repris tels quels. Deux adaptations assumées, sans toucher aux formes : les remplissages passent en `currentColor` (le vin) et `var(--page-mark-soft)` (les deux aplats d'intérieur) pour que la marque suive le système des sceaux au lieu de figer des hex ; le `viewBox` est recadré au carré (`37.5 36 262 262`) parce que l'encre était décentrée dans la boîte d'origine (259x262 à l'offset 39,36 dans 329x345), ce qui donnait un anneau inégal dans la pastille. Le dessin ne se lit qu'à partir de ~34 px (à 20 px les deux masques se touchent) : il sert en grand dans le hero de l'accueil, sur une pastille agrandie (`.home-brand-mark`, 52 px pour 34 px de dessin), et **en petit sur le lien de retour à l'accueil du bandeau déplié** (`.play-header-home-mark`), où la contrainte qui gagne est l'accord des deux pastilles : le bandeau ne porte que le sceau de la page et celle-ci, et une taille chacune se lisait comme un décalage, donc elle prend exactement la taille du sceau (36 px, 30 px sous 800 px). Ce qu'on lui rend à la place, c'est du remplissage : le dessin y occupe environ trois quarts de la pastille (26 px dans 36, et 22 px dans 30) là où un sceau de page en occupe un peu plus de la moitié. Sous le plancher de lisibilité, donc, mais le mot « PrettyDrama » est écrit juste à côté. Les bandeaux portent par ailleurs le sceau de leur page. La marque n'étant pas une page, `.page-home` prend le bordeaux `#8b2635` de la marque (et non plus l'or) : logo et mot « PrettyDrama » s'accordent. Favicon d'`index.html` et de `respo.html` : même géométrie et même recadrage, **en bordeaux sur tuile crème `#f5eeda`, comme la pastille du hero** (les deux aplats d'intérieur en crème aussi, exactement le rapport de `MasksIcon` sur `--page-mark-soft`), comme les six icônes du site, cf. la ligne suivante. Corollaire mobile : la règle qui rapetisse le sceau à 30/17 px sous 800 px (fin de `theme.css`) est visée sur `.page-header .page-mark` et `.play-header .page-mark`, **jamais sur `.page-mark` tout court**, sinon la marque tombe à 17 px, taille à laquelle les deux visages se touchent. Ça tenait avant par accident (même spécificité que `.home-brand-mark`, qui ne gagnait que parce que `main.jsx` importe `theme.css` avant `home.css`) |
| Icônes | `src/shared/icons.jsx`, toutes en SVG dimensionné sur la font-size (1em) et en `currentColor` : **aucun emoji dans l'UI**, ni de contrôle ni de ponctuation. Sur mobile, `▶ ⏸ ⚠️ 🤖` rendent en couleur pleine, hors palette, avec une hauteur qui change d'une plateforme à l'autre. Deux familles : aplats pour les contrôles (lecture, micro), trait de 2 px pour tout le reste. Exceptions assumées : quelques caractères restent des caractères, parce qu'ils sont monochromes (aucune variante emoji, donc aucun rendu en couleur pleine) et qu'ils suivent la fonte du texte : `✓ ✕ ↓` des étiquettes et boutons, le chevron `▼` du repli de `PlayHeader`, la poignée de glisser `⠿` de `LineRow`, et le `?` de la pastille « type inconnu » du journal. Autre exception, celle-là graphique : `SparkleIcon` (« voix de synthèse ») est en aplat alors que sa famille est au trait, parce que son étiquette est en 11.5 px et qu'un trait s'y referme (un robot au trait n'y était plus qu'une tache). `WarnIcon` porte lui-même sa classe d'alignement `.warn-icon` (`theme.css`) : il ne sert qu'en tête de phrase, dans les quatre avertissements du site |
| Icônes d'onglet et d'écran d'accueil | favicon SVG en `data:` URI dans chaque `.html`, plus un PNG par page dans `public/`. **Chaque icône est la pastille de sceau de sa page** : tuile en `--page-mark-soft` (crème, vert pâle, lilas), glyphe en `--page-mark` (le dessin de la page, aux mêmes tracés que son `PageMark`), donc exactement ce que `.page-mark` peint à l'écran, hex recopiés puisqu'un `<link>` ne lit pas une variable CSS. C'était l'inverse (tuile pleine, glyphe blanc) : ces icônes **sortent du site**, ce sont elles que WhatsApp et iOS montrent en vignette quand on partage le lien de la troupe, et le négatif d'un sceau ne se reconnaissait pas comme le sceau. Rien n'est perdu en petit : à 32 px les deux sens sont nets, à 16 px les deux sont molles de la même façon (les masques se touchent sous ~34 px, cf. la ligne « Logo »), donc le choix se joue en grand, là où vit la vignette. `theme-color`, lui, garde le `--page-mark` plein : il colore la barre du navigateur, pas une icône, et la couleur de page y reste la couleur de page : Safari ne lit ni les favicons SVG ni les `data:` URI, et iOS exige un PNG pour « Ajouter à l'écran d'accueil ». `apple-touch-icon.png` sert les deux accueils (les masques), les quatre autres pages ont le leur. **Dérivés des mêmes tracés que les favicons, pas redessinés** : on reprend le SVG du `<link rel="icon">`, on lui pose `width`/`height` à 180, on retire le `rx='6'` (iOS applique son propre masque arrondi) puis on rastérise et on aplatit en RGB sans alpha (iOS remplit le transparent en noir). Toucher un favicon oblige donc à régénérer son PNG. Les `href` s'écrivent absolus (`/apple-touch-icon.png`) : Vite les rend relatifs au build, `public/` étant recopié tel quel dans `dist/` |
| Cartes de dialogue (look commun Répétition/Enregistrement) | `.dialogue-card` dans `src/shared/theme.css`, qui porte aussi la palette rose/doré « mes répliques » (`.dialogue-card.mine`) et la bordure `.active` communes. Les pages posent `.mine` à côté de leur classe sémantique (`.muted` côté répétition, `.own` côté enregistrement) et ne gardent que leurs vrais écarts (`.hide-text`, `.fresh`, `.recording`…) ; côté enregistrement l'état des prises est porté par l'étiquette `.rec-status` orange/verte (palette en variables `--rec-*` locales à `recorder.css`) |
| Bandeau de pièce + barre de contrôle basse (communs Répétition/Enregistrement) | composants `src/shared/PlayHeader.jsx` (titre de la pièce, repliable, marque en pied) et `src/shared/ProgressBar.jsx` (slider indexé) ; CSS dans `theme.css` (`.play-header*`, `.controls`, `.ctrl-btn`, y compris `.ctrl-btn.my-jump` pour les sauts « ma réplique », communs aux deux pages…). Pas de tiret cadratin dans les headers. `PlayHeader` sert aussi à l'Édition et à l'Avancement : les quatre pages qui connaissent la pièce affichent son titre en haut, au même endroit et dans la même serif. **Où vivent la marque et le lien vers l'accueil** : plus dans la rangée du haut. Celle-ci ne dit plus QUE le titre de la pièce (avec le sceau, qui dit la page) : le bouton de repli l'avale en entier, et le mot « PrettyDrama » est descendu en pied du bandeau, où il rejoint le lien. Il y prend exactement l'aspect qu'il avait en haut (`.play-header-home-word` : `--header-serif` 700, 20 px, `--header-accent`, `text-decoration: none` sur le lien pour qu'il ne ressemble jamais à un hyperlien souligné), il change juste de place. Motif : la rangée du haut est la seule ligne toujours visible, et sur mobile la marque y mangeait la largeur du titre de la pièce, qui se coupait en « … » ; le nom du site, lui, se lit très bien une ligne plus bas, là où il est aussi la destination du lien. Ce lien est donc le logo aux deux masques sur sa pastille **plus le mot à sa droite** (`src/shared/HomeLink.jsx`, un seul composant pour les deux bandeaux ; `.play-header-home`, `homeHref` donc `respo.html` depuis l'Édition et l'Avancement), **en pied du bandeau déplié, centré**. Toujours pas de « Retour à l'accueil » écrit (le logo et le nom du site disent la destination) : c'est l'`aria-label`/`title` « Accueil PrettyDrama » qui porte le verbe, et il contient bien le texte visible ; le sceau reste décoratif. Deux filets courts qui s'effacent vers l'extérieur l'encadrent (`.play-header-foot`) : seule et centrée, la pastille avait l'air tombée là ; encadrée, elle ferme le bandeau comme un cul-de-lampe ferme une page (dégradés et courts plutôt qu'une règle pleine d'un bord à l'autre, qui aurait pesé sous des réglages déjà chargés). Son survol reprend le **geste** de toutes les surfaces cliquables du site (cartes de l'accueil, bouton de dépôt) sans le copier à la lettre : il n'en garde que le pas d'un pixel vers le haut, et ce qui remplace son filet et son ombre est **le crème du sceau de la marque qui s'étend et englobe le bloc entier** (logo + mot, c'est un seul bouton) : le sable de la pastille déborde d'elle et enveloppe le nom du site, qui ne font plus qu'un objet posé sur un fond chaud. Il vit dans un `::before` : dégradé radial `closest-side` du crème vers le transparent, qui atteint la transparence AVANT le bord de son propre calque, lui-même plus grand que l'ovale (`inset: -14px -30px`), donc la nappe finit de s'éteindre dehors et plus rien ne se termine sur une ligne (les arrêts intermédiaires, quatre paliers, servent la même cause : deux arrêts seuls laissent une frange là où la pente est raide). Plein crème au centre, là où le vin devait rester sous les 20 % : le sable est assez proche du blanc du bandeau pour le supporter, et c'est la largeur et la pente douce, pas la pâleur, qui l'empêchent de se montrer. Un peu plus lent que le pas de l'ovale pour que le fond monte au lieu de s'allumer d'un coup. La couleur vient de `--page-mark-soft` via la classe **`page-home` posée sur le lien lui-même** (`PlayHeader.jsx`) : ni hex recopié dans `theme.css`, ni token de plus, et sur les pages du responsable la nappe reste crème au lieu de prendre le vert ou le violet du bandeau. `z-index: -1` la passe sous le logo et le mot sans la passer sous le bandeau (c'est le `.play-header` collant qui crée le contexte d'empilement, l'ovale n'en crée pas), `pointer-events: none` garde la cible du clic sur l'ovale seul, et le `border-radius: 999px` de l'ovale ne peint donc plus rien : il ne reste que pour la forme de la bague de focus. Trois essais gardés en commentaire dans `theme.css` parce qu'ils sont tentants : **le filet** à la couleur de marque des cartes (là-bas il existe déjà au repos, c'est la bordure de la carte que le survol teinte ; ici il apparaissait de nulle part et cernait les masques comme une cible, en plus de n'allumer que le rond et de laisser le mot éteint à côté), **le fond teinté + `box-shadow` flous** (un fond s'arrête net au bord de sa boîte, une ombre portée redessine la silhouette : dans les deux cas l'arête revenait), et **la même nappe en vin** (une lueur rouge autour du logo, une alerte plutôt qu'un accueil : ce qui doit s'étendre est le fond du sceau, pas son encre). Le `border: 1px solid transparent` qui le portait au repos est donc retiré aussi : sans lui la pastille fait ses 36 px pleins, exactement comme le sceau de la page (en `border-box`, le filet transparent en mangeait un). Le voile est un `color-mix` sur `--header-accent` et pas `--accent-soft` (couleur figée, qui ne suivrait pas la teinte de la marque) ni `--accent` (re-skinné en violet par l'éditeur, alors que le bandeau partagé doit rendre pareil sur les quatre pages) ; `--shadow-hover` et pas `--shadow` pour cette dernière raison, l'éditeur met le second à `none`. Deux pistes écartées : inverser la pastille (fond vin, dessin sable) noie les masques, qui peignent leurs deux visages en `--page-mark-soft`, et il ne reste qu'une tache pâle ; l'agrandir la redessine à une taille qu'elle ne supporte pas. Bague de focus de la même géométrie que `--focus-ring` mais bâtie sur `--header-accent`, la bague partagée dérivant de `--accent`, que l'éditeur re-skinne. Sur mobile le pouce vise le haut de la barre pour la replier et tombait sur la marque : le geste le plus courant de la page renvoyait au menu principal, en perdant le personnage choisi et, sur l'Enregistrement, les prises non exportées (le `LeaveGuard` prévenait, mais l'accident restait quotidien). Maintenant tout ce que le doigt atteint dans la rangée replie, et sortir de la page demande de déplier d'abord. En pied et pas en tête du bandeau : une sortie de page ne se met pas au-dessus des réglages de la page. Le sceau, lui, reste hors du bouton : son `role="img"`/`aria-label` brouillerait le nom accessible du bouton, et c'est la seule zone inerte de la rangée (elle l'a toujours été). **Corollaire : le bandeau se replie sur les quatre pages, y compris l'Avancement**, qui n'a pourtant aucun réglage ; une page qui ne se replierait pas serait la seule à garder son bandeau ouvert sous le pouce. Son bandeau à elle ne contient que sa doc (cf. la ligne « Phrases de doc » : à quoi sert la page, jamais comment lire la grille, ça c'est le rôle de `.dash-legend` sous le tableau) et le logo de retour : déplier pour ne trouver qu'un logo faisait vide. D'où aussi l'infobulle unique du bouton de repli, « Déplier ou replier le bandeau » et non « les réglages » : une formule par contenu se désaccorderait au premier ajout. Seul `PageHeader` (titre = libellé de page) reste pour les écrans qui n'ont pas encore le manifest, via `PageState` : même géométrie, et il porte le MÊME `HomeLink` en pied, sans quoi la marque sauterait du haut vers le bas du bandeau à l'arrivée du manifest |
| Réglages de la Répétition (les 4 cases) | `.checks-row` (`src/rehearsal/rehearsal.css`), rangée `flex-wrap` à côté du select de personnage : « Couper ma voix » (mes répliques ne sont pas jouées, je les dis), « Cacher mon texte » (les miennes sont floutées), « Bip avant ma réplique », « Démarrer une réplique avant la mienne » (les flèches « ma réplique » s'arrêtent sur la réplique qui me lance, pas sur la mienne). Les libellés disent ce que la case fait : « Muet », « Bip » et « Avant » ne se comprenaient qu'une fois essayés. **La quatrième ne se raccourcit pas en « Démarrer avant ma réplique »** : « avant » y désignerait le rang de la réplique où l'on démarre, alors qu'il désigne un instant dans le libellé du bip juste au-dessus ; deux sens du même mot dans deux cases voisines, dont l'une est celle qui décale les sauts, on la relit deux fois. La longueur est le prix assumé, et ces quatre libellés sont ce qui remplace la phrase de doc du bandeau, que la Répétition n'a plus. **Le repli reste en `flex-wrap`, jamais un nombre de colonnes fixé** (la grille 2x2 a été essayée et retirée) : une largeur de colonne imposée aux quatre les aligne sur le plus long, donc sur téléphone les quatre cases passent à deux lignes chacune alors que les deux courtes tiennent côte à côte. **Sous 800 px, c'est la hauteur des étiquettes (32 px) qui espace les lignes, et le `row-gap` est à zéro** : les deux s'additionnaient, et la zone tactile de 40 px du premier essai laissait 19 px de vide entre deux lignes, plus que les 18 px qui séparent deux cases côte à côte, exactement au moment où la fenêtre devient trop étroite pour tenir les quatre sur une ligne. Un seul chiffre à régler, donc, et la recommandation des 44 px se rattrape en largeur (l'étiquette entière est cliquable). Le `margin-block: -5px` du même bloc va avec : cette hauteur de cible ne doit espacer que les cases entre elles, alors qu'au bord du bloc elle s'ajoutait au `row-gap` de `.character-row` et éloignait les cases du select « Qui jouez-vous ? » (et du pied du bandeau) au passage sous 800 px. La boîte extérieure épouse donc le texte, l'intérieur reste haut. L'infobulle de chaque case reste, elle dit le pourquoi ; le libellé dit le quoi |
| Phrases de doc (cartes d'accueil + bandeaux) | **Deux éléments par mode, pas plus, et un seul endroit pour le premier.** (1) `PAGES[page].desc` (`src/shared/pages.js`) : la phrase compacte, servie à la fois par la carte de l'accueil et par le bandeau, que **`PlayHeader` rend lui-même** (`.header-hint`) sur les quatre pages ; une carte qui promet une chose et un bandeau qui en dit une autre décrivaient deux pages différentes, et la même phrase recopiée dans deux fichiers finit par dériver. (2) le `hint` que chaque page passe à `PlayHeader` (même `.header-hint`, une seule classe : les deux paragraphes ont le même style, c'est leur place qui les distingue) : les précisions qui n'auraient aucun sens sur une carte, quand on choisit encore où aller. Les pages n'écrivent donc plus de `.header-hint` elles-mêmes. **Les deux paragraphes encadrent les réglages** : la phrase compacte ouvre le bandeau déplié, juste sous le titre de la pièce et avant le choix d'acte/scène (à quoi sert la page se lit avant qu'on y touche, et c'est le même endroit sur les quatre pages ; conséquence assumée dans l'éditeur, elle passe aussi au-dessus du champ de titre éditable, qui est un réglage comme un autre), le `hint` ferme le bandeau sous les réglages (on le relit une fois, il ne doit pas éloigner les réglages du titre). Forme commune aux deux, à tenir : verbe d'action à l'impératif en tête, une dizaine de mots, deux phrases au maximum pour un `hint`, pas de question posée au lecteur (« Qui a enregistré quoi ? » a sauté), les deux-points seulement pour énumérer. **Le `hint` est facultatif, et seules deux pages en ont** : l'Enregistrement (où appuyer pour enregistrer, quand télécharger) et l'Édition (où va le script téléchargé, plus la touche Entrée). La Répétition n'en a pas, ses quatre cases à cocher disent elles-mêmes ce qu'elles font (cf. « Réglages de la Répétition ») ; l'Avancement non plus, la carte de dépôt est juste sous le bandeau et se lit seule. Une phrase qui redit ce que l'écran montre ne fait que repousser les réglages. Corollaires : le libellé exact des boutons ne se recopie plus dans la phrase (« par le même bouton que les voix des acteurs » plutôt que le nom du bouton en gras plus `script.json` en `code`, d'où la disparition de `.editor-header-hint code`, et celle d'un `.header-hint-more` que rien ne stylait), et la troupe dit « le responsable » et jamais « votre responsable » (possessif de hiérarchie). L'éditeur est la seule page dont la doc porte un lien vers une autre page, et ce lien **n'est pas un mot souligné** : c'est le sceau de la page visée plus son nom (`.hint-page-link` / `.hint-page-mark` dans `editor.css`, classe `page-dashboard` posée sur le lien pour ses couleurs), même forme que le retour à l'accueil en pied de bandeau, et le site nomme ses pages par leur pastille partout ailleurs. Un hyperlien classique y était la seule chose soulignée du bandeau. La pastille est la plus petite du site (19 px dans du 13.5 px) et se préfixe `.play-header` pour échapper à la règle mobile qui rapetisse les sceaux du bandeau, sans quoi elle GRANDIRAIT à 30 px sous 800 px. Deux réglages d'alignement mesurés au pixel, à ne pas « simplifier » : le lien est `inline` et pas `inline-flex`, pour que le mot s'asseye sur la ligne de base du paragraphe comme n'importe quel autre mot (une boîte flex n'expose pas la ligne de base de son texte, il fallait la recentrer à la main et elle retombait un pixel trop bas) ; et le sceau se cale sur la bande des capitales, pas sur la hauteur d'x, via `vertical-align: middle` PLUS `margin-bottom: 3px` (`middle` centre la boîte de marge, donc 3 px en bas remontent la pastille de 1,5 px). Une longueur en `vertical-align` aurait été plus directe mais elle part de la ligne de base du sceau, et un `inline-flex` dont l'unique enfant est un SVG n'en a pas de fiable : Chrome la synthétise, la spec parle du bord bas de la marge, soit 4 px d'écart possible d'un navigateur à l'autre. Le remplissage vertical du lien (3 px) ne sert qu'à ce que la nappe de survol couvre le sceau, qui dépasse de la boîte de contenu du texte ; le remplissage droit sert la nappe et pas l'espacement, d'où la marge négative qui l'annule (sans elle le mot suivant se lisait à deux espaces du lien) |
| Repli animé du bandeau | les réglages restent **montés** (sinon rien à animer) dans un conteneur grille dont l'unique ligne passe de la hauteur du contenu à zéro : c'est interpolable, contrairement à `height: auto`. Deux pièges déjà payés, à ne pas réintroduire : la piste s'écrit `minmax(0, 1fr)` / `minmax(0, 0fr)` et **jamais** `1fr` / `0fr` (le minimum d'une piste `fr` vaut `auto`, donc la contribution min-content du contenu : la piste « à zéro » restait haute du padding bas des réglages, soit une ligne vide sous le titre), et l'enfant `.play-header-settings-inner` garde `min-height: 0` (sans quoi le minimum automatique d'un élément de grille le plafonnerait à sa hauteur de contenu, et rien ne se replierait). `visibility: hidden` (propriété discrète, donc appliquée en fin de fermeture et au début de l'ouverture) sort les selects du parcours clavier sans les démonter. Le bouton de repli prend `align-self: stretch` : sans lui il ne mesurait que les 28 px de son titre dans une rangée haute de 36 px, et `align-items: center` laissait quatre pixels morts de part et d'autre, en haut de la barre, exactement là où le pouce vise pour replier (rien ne bouge à l'écran, le fond est transparent et le contenu reste centré). Le repli n'enlève QUE les réglages : la rangée du haut ne bouge pas d'un pixel (ni padding, ni taille du sceau, ni corps du titre), et le titre reste donc centré dans une barre au padding symétrique. Le rognage est posé sur la grille et **pas** sur son enfant : en `border-box`, un enfant de hauteur 0 garde ses 14 px de padding bas, qui débordaient sous le bandeau fermé et l'épaississaient. Il n'est actif **que** fermé ou en mouvement (classe `animating`, retirée par un minuteur de 340 ms) : ouvert et immobile, le popover de couleur de l'éditeur doit pouvoir dépasser du bandeau. Minuteur et pas `transitionend` : à mouvement réduit l'événement ne vient jamais et le bandeau resterait rogné. Durées et `--ease-header` dans `theme.css`, neutralisées par le bloc `prefers-reduced-motion` en fin de fichier |
| Page Enregistrement | structurée comme la Répétition : sélecteurs acte/scène/personnage, navigation contrainte à SES répliques, micro central (SVG) dans la barre basse, bouton Télécharger à droite. Les prises survivent au changement de personnage (ZIP multi-voix : `voix-<noms>.zip`). Tant qu'aucun personnage n'est choisi : `IntroCard` à la place de la liste de répliques (« Qui jouez-vous ? » + mode d'emploi + un bouton par personnage avec son reste à enregistrer), barre basse masquée et `hint` du bandeau à `null` (la phrase compacte, elle, reste : c'est le `desc` partagé avec la carte d'accueil), select du bandeau en accent (`.character-select.unset`) |
| Statut des répliques (Enregistrement) | 3 états par réplique, étiquette au coin de la carte + légende en tête de liste : `todo` « À enregistrer » (point ambre), `fresh` « À télécharger » (prise de la séance, halo vert + lecteur vert vif ; reste fresh MÊME après téléchargement du ZIP : « Déjà enregistrée » n'est vrai qu'une fois le ZIP intégré par le respo et le site republié), `done` « Déjà enregistrée » (clip publié à jour uniquement, lecteur vert grisé). Le téléchargement ne touche pas aux statuts, il ne pilote que la note « pas sauvegardé »/« téléchargé » ; les flèches de la barre basse parcourent TOUTES mes répliques et portent donc le design des sauts « ma réplique » de la Répétition (`.ctrl-btn.my-jump`, icônes `SkipPrev`/`SkipNext`) ; l'avertissement « pas sauvegardé » vit dans le bandeau du HAUT (jamais dans la barre basse) ; lecteur intégré à la carte (`TakePlayer` : play rond, durée avec workaround Infinity des blobs MediaRecorder, onde décorative déterministe) |
| Jeter une prise (Enregistrement) | bouton corbeille en bout de rangée du lecteur (`.player-delete`, `TrashIcon`), au trait rouge du thème sur une pastille `--accent-soft`, plein au survol : il côtoie le bouton d'écoute (vert, en aplat), où un dessin gris se perdait alors que c'est le seul geste irréversible de la page, **seulement sur une prise de la séance** (`onDelete` n'est passé que si `takes[line.id]` existe) : un clip déjà publié se réécoute mais ne se supprime pas depuis le navigateur, il vit dans le dépôt (le supprimer demanderait un canal de dépôt « retirer ce clip », que rien ne porte aujourd'hui). Désactivé pendant un enregistrement, comme les autres commandes. `deleteTake` révoque l'URL du blob, retire la clé, et **repose `downloaded` à faux** exactement comme refaire une prise : le ZIP déjà téléchargé contient encore la prise jetée, donc il est à refaire. La réplique reprend l'état qu'elle avait avant, `done` ou `todo`, sans code dédié (`lineState` ne fait que retomber sur `line.status`) |
| Couleurs des personnages (éditeur) | teinte **stockée** sur le personnage (`{id, name, hue}` dans script.json) ; palette `CHARACTER_HUES` (12 teintes, rendu `oklch(0.58 0.14 H)`, attribution auto entrelacée via `HUE_ASSIGN_ORDER`) dans `src/editor/reducer.js`, helpers dans `CharacterPanel.jsx` ; auto = 1re libre, modifiable via popover ; teinte invalide réparée au chargement |
| Design « Rail » de l'éditeur | tokens re-skinnés dans `src/editor/editor.css` (`:root` local à la page) : même fond crème `--paper` que les autres pages, filets/neutres réchauffés assortis, accent `#7a5cc0`, fonts IBM Plex Sans/Mono + Spectral (chargées par `editor.html`) ; les autres pages gardent le thème chaud partagé. Le re-skin ne fuit jamais dans le bandeau partagé : PageHeader/PlayHeader consomment les tokens réservés `--header-accent`/`--header-serif` (theme.css), qu'aucune page ne redéfinit (Cormorant 700 chargée aussi par `editor.html`). Les répliques de la scène vivent dans UN panneau blanc (`.line-list` : card blanche arrondie sur le crème, lignes fines à filet 1px dedans) pour le même contraste blanc-sur-crème que les cartes des autres modes |
| Édition réservée à l'ordinateur | `src/editor/useTouchPointer.js` : sur un pointeur tactile (`(pointer: coarse)`), l'éditeur ne se rend pas du tout, `App` renvoie un `PageState` d'une seule phrase (« Pour des raisons de praticité, le mode Édition n'est disponible que depuis un ordinateur. »), dans la carte `.page-notice` partagée par tous les états pleine page, sans la classe `.load-error` que le script illisible ajoute par-dessus pour élargir la carte et aligner à gauche ses paragraphes de marche à suivre. L'éditeur y redonne une ombre (`--ed-panel-shadow`), puisque son `:root` local met `--shadow` à `none` : cette carte est le contenu de l'écran, comme son panneau de répliques. Rien d'autre dans le message : ni la liste des gestes qui manquent (glisser une réplique, saisir un long texte), ni les liens vers les trois pages qui marchent au doigt, essayés puis retirés (le retour à l'accueil du bandeau suffit à sortir). C'est la SEULE page ainsi murée, parce que c'est la seule qui demande de glisser des répliques et de saisir de longs textes. **Le critère est le pointeur, pas la largeur** : un téléphone en paysage fait 844 px (il passerait un seuil de largeur) et reste inutilisable, alors qu'une fenêtre d'ordinateur rétrécie garde souris et clavier. Corollaire : ça ne se teste pas en réduisant une fenêtre, il faut l'émulation d'appareil des outils de développement. La requête est **écoutée** (`change`), pas seulement lue au montage, sinon un hybride qui rattache son clavier resterait bloqué jusqu'au rechargement ; sans `matchMedia` on ne bloque pas. Le `fetch` du script a lieu **même quand la page est murée**, et son seul emploi est le bandeau : cet écran nomme la pièce comme les quatre autres bandeaux du site (`title={script.title || "Pièce sans titre"}`), il ne dit pas « Édition » en toutes lettres. Sauter la requête était une économie de rien (un JSON sur une page qui n'affiche qu'une phrase) payée par la seule rangée du haut du site à porter un libellé de page. D'où l'ordre des sorties de `App` : le chargement d'abord (le titre n'arrive qu'avec le script, et cet écran d'attente est le seul de l'Édition à porter encore « Édition »), le mur ensuite, l'erreur de lecture après lui (un script illisible n'apprend rien à qui ne peut pas éditer). La carte Édition de l'accueil respo n'est **pas** masquée sur mobile : une carte qui disparaît se lit comme une page perdue, un écran qui s'explique ne se lit qu'une fois. Conséquence dans `editor.css` : ses règles tactiles ont été retirées (micro-boutons portés à 40 px, zone de clic de la pastille de couleur étendue par un `::after`), plus aucun doigt ne les atteignait et elles épaississaient les rangées là où la place manque ; sa section « Mobile » est devenue « Fenêtre étroite », et ce qui y reste (repli des `.line-row` à 620 px, popover de couleur en barre basse à 800 px) ne se justifie plus que par le débordement et la largeur de saisie, jamais par le doigt |
| Navigation d'édition | une scène à la fois via le `PlayHeader` partagé (acte/scène + « + Scène »/« + Acte ») ; à la place du select personnage des autres pages : la **gestion** des personnages en puces (`CharacterChips` dans `src/editor/CharacterPanel.jsx`) ; titre de pièce éditable dans le bandeau ; téléchargement dans les `actions` du bandeau, **en icône seule** (`.btn.script-download-btn`, nom qualifié : l'Enregistrement a son propre bouton de téléchargement, `.zip-download-btn`, aux règles contraires), comme la paire annuler/rétablir qu'il côtoie : son libellé écrit était le seul texte de la rangée toujours visible à concurrencer le titre de la pièce sur une fenêtre étroite. Le verbe vit dans l'`aria-label`/`title` « Télécharger le script », et la phrase de doc du bandeau dit quoi faire du fichier. Il reste l'action principale, donc son dessin est plus grand (19 px contre 17), mais sa HAUTEUR est calée sur celle des boutons d'historique (31 px de part et d'autre) : trois boutons voisins dont un dépasse de deux pixels se lisent comme un défaut d'alignement, pas comme une hiérarchie ; c'est la largeur et l'accent qui la disent. Il est **désactivé quand `dirty` est faux**, comme les deux boutons d'historique le sont quand la pile est vide : redéposer le script tel qu'il est déjà publié n'apprend rien à l'Action, et `dirty` étant un comparatif d'états et pas un drapeau, annuler jusqu'au dernier téléchargement l'éteint aussi. Seule l'infobulle change (« Aucune modification à télécharger pour l'instant »), jamais l'`aria-label` : le nom d'un bouton ne dépend pas de son état. **Cette infobulle est portée par une enveloppe `.btn-tip` et jamais par le bouton** : un contrôle `disabled` ne reçoit aucun événement souris, donc son propre `title` ne s'affiche pas (Chrome, Safari) et l'explication n'arrivait jamais au moment où elle sert ; les trois boutons de la rangée suivent la même règle. Corollaire connu : un `script.json` réparé au chargement par `sanitizeScript` n'est pas téléchargeable tant qu'on n'y touche pas (la pièce affichée diffère alors du fichier du dépôt) ; la moindre édition rouvre la porte |
| Annuler / rétablir (éditeur) | `src/editor/history.js` : pile d'états qui **enveloppe** `scriptReducer` (qui reste pur et inchangé), `{present, past, future, lastKey, saved}`, `past` plafonné à 100 entrées (les états sont structurellement partagés, une entrée coûte quelques objets). Toute nouvelle édition vide `future` (la timeline fourche). Les frappes clavier (`EDIT_TEXT` d'une même réplique, `SET_TITLE`) sont fusionnées en une seule étape, close par toute autre action, par un undo/redo ou par `HISTORY_BREAK` (dispatché au blur du textarea et du titre) ; `LOAD_SCRIPT` réinitialise la pile ; une action refusée par le reducer n'empile rien. Paire de boutons icône `.history-group` dans les `actions` du bandeau + raccourcis Ctrl+Z / Ctrl+Y (et Cmd+Maj+Z) globaux, interceptés même dans un textarea (l'undo natif du navigateur désynchroniserait la pile) sauf quand il n'y a rien à annuler/rétablir |
| Confirmations de suppression (éditeur) | jamais de `window.confirm` (dialogue natif hors thème) : `src/shared/ConfirmModal.jsx` (partagé depuis que l'enregistrement s'en sert aussi, via `LeaveGuard`), même habillage `.modal-backdrop`/`.modal` que le modal de suppression de personnage, rendu en **portail** dans `document.body` (une `.line-row` peut porter un transform dnd-kit, qui deviendrait le bloc conteneur du backdrop `fixed`). Escape annule, le bouton destructif prend le focus (sauf quand un `primaryLabel`/`onPrimary` optionnel offre une sortie sûre : c'est elle qui le prend). Utilisé pour réplique (`LineRow`), scène (`SceneEditor`), acte et sortie de page (`App`), et hors éditeur pour la prise d'enregistrement (`TakePlayer`) ; supprimer un élément VIDE ne demande rien. La citation de la réplique visée (`.confirm-quote`, coupée par `excerpt` de `data.js`) est partagée par les deux pages qui citent : elle vit dans `theme.css`, plus dans `editor.css`, et son filet suit `--border` (donc la teinte de la page) |
| « Modifications non téléchargées » (éditeur) | dérivé, jamais un drapeau : `dirty = present !== saved` où `saved` est l'état de la pile d'annulation correspondant au dernier `script.json` téléchargé (le script chargé au départ). Annuler jusqu'à cet état fait donc disparaître l'étiquette, y compris quand la pile n'est pas vide (édition, téléchargement, édition, annulation). Le téléchargement dispatche `MARK_SAVED` ; l'identité des objets suffit, la pile restitue les états qu'elle a stockés |
| Sortie de page avec du travail non téléchargé | **un seul composant pour les deux pages concernées** : `src/shared/LeaveGuard.jsx` (`active`, `title`, `saveLabel`, `onSave`, message en children), monté par l'éditeur (`active={dirty}`, `onSave={download}`) et par l'enregistrement (`active={hasUnexported}`, `onSave={downloadZip}`, awaité car async). Il porte les deux couches : (1) clics interceptés en phase capture sur `document`, tout `a[href]` qui sort de la page ouvre un `ConfirmModal` du thème (« Télécharger … puis quitter » / « Quitter quand même » / Annuler) ; écoute globale plutôt que lien par lien, donc les futurs liens sont couverts d'office ; clic modifié, `target="_blank"`, `download` et ancres internes passent ; (2) `setBeforeUnloadGuard` (seul appelant restant) comme filet pour le rechargement, la barre d'adresse et la fermeture d'onglet : là le navigateur n'autorise QUE son dialogue (message et style imposés depuis Chrome 51 / Firefox 44), rien à habiller, ne pas réessayer. Détails à ne pas défaire : `setBeforeUnloadGuard(false)` à la main avant de naviguer (sinon le dialogue natif se superpose au modal), 200 ms de délai après le téléchargement (décharger la page dans la même tâche l'annule), et le modal reste affiché quand `active` retombe. **Aucune persistance locale du travail** (localStorage) : décision produit, un brouillon oublié dans un navigateur redeviendrait une source de vérité périmée face au dépôt |
| Enregistrement micro | `src/recorder/useRecorder.js` (MediaRecorder, stream réutilisé, `release()` en fin de session) |
| Construction du ZIP | `downloadZip` dans `src/recorder/App.jsx` |
| Grille d'avancement (Avancement) | `ProgressTable` dans `src/dashboard/App.jsx`, CSS `.dash-table*` dans `dashboard.css` : personnages × scènes, « enregistrées / total » par case, la même échelle ambre/vert portée par les cases, les noms, les numéros de scène et les actes (`statusClass`), donc une rangée, une colonne ou un acte entièrement vert se lit sans compter. Deux choses à ne pas défaire. (1) **La colonne des noms est figée** (`position: sticky; left: 0` sur `.dash-name` et sur les deux `.dash-corner`, qui sont de la même colonne) et c'est le conteneur qui défile, jamais la page : une pièce à dix scènes ne tient sur aucun téléphone, et une case « 2/5 » sans son nom en face ne dit rien. Le coin prend le fond de la PAGE (`--paper`) et non plus `background: none`, sinon les numéros de scène défilent visiblement par-dessous, et c'est LUI qui porte le filet gauche des deux rangées d'en-tête (`border-right`) : sur la première cellule d'acte et de scène, ce filet partait au défilement et le haut du tableau se retrouvait ouvert à gauche. Le conteneur est une région nommée et `tabIndex={0}`, sans quoi une zone qui défile et ne contient rien de focalisable serait hors d'atteinte au clavier. **Aucune ombre au bord de la colonne figée** : essayée pour dire qu'une case passe dessous, elle salissait chaque nom d'un dégradé gris ; ce qui sépare est le filet continu du haut du tableau à son pied. (2) **Le tableau est en `border-collapse: separate`** (`border-spacing: 0`), et ce n'est pas un détail de style : sous `collapse` les filets sont peints par le tableau et pas par les cellules, donc ils ne suivent pas une cellule `sticky` et la colonne des noms perdait toutes ses bordures dès le premier pixel de défilement. Chaque cellule porte donc son filet droit et bas, et deux règles rendent les bords qui n'ont aucun voisin pour les porter (filet gauche des noms, filet haut des actes et du premier nom) ; le filet haut ne se pose surtout pas sur tous les noms, il s'ajouterait au filet bas du précédent et donnerait une ligne de 2 px entre personnages. Le nom est enveloppé dans un `.dash-name-text` qui se coupe en « … » (`title` sur la cellule pour le rendre en entier) : la colonne figée prend sur la largeur des scènes, un nom à rallonge ne doit pas laisser un filet de grille pour tout le reste. Responsive : c'est cette colonne qu'on resserre d'abord sous 560 px (nom plus court, moins de gouttière), les cases ensuite, et le pas des scènes descend à 44 px, de quoi tenir « 12/12 » |
| Seul retour du respo (journal des dépôts) | `scripts/update_history.py` → `data/history.json` (`{runs: [{at, files}]}`, la plus récente d'abord, plafonné à 30), recopié dans `manifest.json` par `build_manifest` ; affiché par `Journal` dans `src/dashboard/App.jsx`. **C'est le canal d'erreur du projet** : il n'y a plus ni issue GitHub ni statut README, donc un fichier refusé ne se dit QUE là. **Une ligne par FICHIER et pas par dépôt** (chaque fichier a son propre sort : un ZIP abîmé au milieu de trois bons n'empêche pas les autres), dans un `<table>` dont le conteneur défile (`max-height` + `overflow: auto`, en-tête `sticky` : une trentaine de dépôts ne doit pas allonger la page sans fin). Quatre colonnes : date (année comprise, un journal se relit des mois plus tard), statut, type, détail. **Les deux colonnes d'icônes empruntent la pastille des sceaux** (`page-mark` + `.dash-journal-mark`, centrées) : le type porte le sceau de la page productrice (micro de l'Enregistrement, plume de l'Édition, les deux mêmes que le bouton de dépôt juste au-dessus, donc aucune légende à donner ; pastille neutre avec un `?` pour un fichier qu'aucune page ne revendique), et le statut prend les mêmes vert et ambre que la grille au-dessus avec `CheckIcon`/`CrossIcon`. Aucun mot dans ces deux colonnes, donc chaque pastille porte un `aria-label` et un `title`. Détail optionnel (`detailOf`) : rien pour un script réussi, nom du ZIP et compte de répliques pour des voix, nom et motif pour un échec. Le tableau est plafonné à `JOURNAL_ROWS` lignes, et **le dit** (`.dash-journal-more`, « N dépôts plus anciens non affichés ») : dans le seul canal de retour du projet, un tableau qui s'arrête sans un mot se lit comme « il n'y a rien de plus ». **Aucun rappel en haut de page** : la ligne du journal suffit, un bandeau d'alerte a été essayé puis retiré. La date de la première ligne fait aussi office de détecteur de panne (un run en échec ne commite rien, donc elle arrête d'avancer) ; **pas d'horodatage du run dans le manifest**, un champ réécrit à chaque exécution ferait différer `manifest.json` à tous les pushes, donc un commit robot chaque fois |
| Bouton de dépôt (Avancement) | `githubUploadUrl()` (`src/shared/data.js`) → `https://github.com/<owner>/<repo>/upload/master/uploads`, reconstruit depuis l'URL Pages ; `null` hors github.io (dev, domaine perso), où la carte se masque plutôt que de forger un 404. Deux formes d'URL Pages à couvrir : site de projet (`owner.github.io/<repo>/…`, le dépôt est le premier segment) et **site racine** (`owner.github.io/…`, où le premier segment est un nom de fichier et le dépôt porte le nom du domaine) ; sans ce second cas le bouton pointait vers `github.com/<owner>/dashboard.html`. **Une seule carte** (`UploadLinks`) : il n'y a qu'un dossier de dépôt, et deux cartes vers la même URL se liraient comme deux destinations. Les deux sceaux (micro, plume) l'encadrent et disent les deux sortes de fichiers, `.dash-upload-word` colorant chaque mot avec le `--page-mark` de sa page |
| Filtre audio ffmpeg | `audio_filter(peak)` dans `scripts/process_uploads.py`, construit par prise à partir de son pic (mesuré par `measure_peak_dbfs`, une passe `volumedetect` avant la conversion). Deux choses à ne pas défaire : le seuil de silence est **relatif au pic** de la prise, jamais une valeur absolue (les niveaux varient énormément selon la distance au micro et l'AGC du navigateur : un seuil fixe laissait une seconde de vide sur une prise forte et mangeait le premier mot d'une prise faible), et `start_duration` est **obligatoire** (les navigateurs posent un clic de quelques dizaines de ms sur les tout premiers échantillons ; sans durée d'attaque minimale, `silenceremove` considère que le son a commencé à l'échantillon 0 et ne coupe rien du tout). Sous `SILENT_PEAK_DBFS` la prise est convertie sans trim : la rogner entièrement écrirait un mp3 vide et illisible |
| Contrats inter-fichiers tenus par la CI | `scripts/tests/test_contracts.py`. Il vérifie ce qui ne vivait que dans un commentaire « keep in sync », en **lisant les sources** plutôt qu'en recopiant les valeurs attendues (recopier ne ferait que déplacer le problème) : `SAFE_ID` (JS) identique à `LINE_ID_PATTERN` (Python) ; aucun CSS de page ne redéfinit un token `--header-*` (la liste est lue dans `theme.css`, donc un nouveau token réservé est couvert d'office) ; aucune règle de bandeau ne consomme `--accent`, `--font-serif` ou `--shadow`, tous re-skinnés par l'éditeur ; chaque clé de `PAGES` a ses deux variables de sceau dans `theme.css` et pas l'inverse ; les entrées de `vite.config.js` et les `.html` de la racine coïncident exactement. Un test qui ne peut pas échouer ne sert à rien : chacun de ces gardes a été vérifié par mutation |
| Script de la pièce en PDF | `scripts/build_script_pdf.py` : `script.json` → `.tex` → `data/script.pdf`. Moteur : **`pdflatex`, paquets TeX de la distribution** (`texlive-latex-recommended`, `texlive-lang-french`, `texlive-fonts-recommended`, `lmodern`), c'est aussi celui du script d'origine de la troupe ; `tectonic` reste accepté en second dans `ENGINES` pour qui l'a déjà, le rendu est équivalent à l'espace avant les deux-points près (en XeTeX il suit la règle française de plus près). **tectonic a été essayé en CI puis écarté, ne pas y revenir sans nouvel élément** : il est plus rapide en régime établi (binaire statique de 26 Mo, ~0,5 s de compilation contre 0,25 s), mais il imposait une URL de release figée, une empreinte sha256 à faire suivre à chaque version et un `actions/cache` pour ne pas repayer 40 s de téléchargement de fichiers TeX (cache que GitHub évince au bout de 7 jours sans usage, donc souvent froid pour une troupe hors saison). Trois choses qui pourrissent dans un dépôt fait pour être forké puis laissé tranquille des années, pour gagner moins d'une minute sur un job hebdomadaire ; les paquets apt sont signés par la distribution et n'ajoutent aucune relation de confiance. La compilation elle-même n'a jamais été le coût : 0,25 s pour les 26 pages de la pièce, tout le reste est l'installation. La mise en page reprend celle du script LaTeX que la troupe utilisait déjà (`article` 10pt deux colonnes, babel français, `\speak` = nom en capitales grasses puis deux-points, filet entre scènes) : le PDF doit ressembler au script que les acteurs ont eu en main. Trois choses à ne pas défaire. (1) **Ce script ne peut pas faire échouer le déploiement** : il rend 0 même quand LaTeX plante, et ses deux étapes de `build.yml` sont en `continue-on-error`. C'est structurel, pas de la prudence : `build.yml` n'écrit ni issue ni journal, donc un run rouge ne se raconte nulle part et le respo ne verrait que le site cesser de se mettre à jour. (2) **Le texte est échappé** (`latex_escape`) avant d'entrer dans le `.tex` : il vient d'un champ de saisie, et `% & $ # _ { } ~ ^ \` sont du code pour LaTeX. Une seule passe de `re.sub`, jamais un `str.replace` par caractère, qui ré-échapperait les antislashs qu'il vient d'écrire. La même fonction **aplatit les blancs**, et ce n'est pas de la cosmétique : une ligne vide est un `\par` pour TeX, or `\lhead` (titre courant) et `\MakeUppercase` (nom du personnage) s'arrêtent net sur une fin de paragraphe dans leur argument, donc un titre ou un nom hand-édité sur deux paragraphes faisait disparaître le PDF de TOUTE la pièce, sans un mot au respo. LaTeX ramenant de toute façon toute suite de blancs à une espace, il n'y a rien à perdre ; une réplique y gagne même de rester un seul paragraphe, donc de garder son nom de personnage en tête. (3) `lmodern` n'est pas cosmétique : sans lui les fontes T1 n'existent qu'en tailles discrètes et le titre demandé à 52 pt sortait à 35,83 pt avec un simple avertissement dans le journal (c'est ce qui arrivait au script d'origine, d'où les 36 pt d'aujourd'hui, taille à laquelle le titre tient sur une ligne). **La page de titre demande trois précautions cumulées**, chacune mesurée sur le PDF rendu et aucune suffisante seule : `\newgeometry{margin=...,nohead,nofoot}` (la géométrie du corps réserve 1,5 pouce en haut pour l'en-tête contre 0,75 en bas, et sans `nohead,nofoot` geometry garde encore la hauteur d'en-tête), `\topskip` à zéro (TeX cale la première boîte d'une page à au moins cette distance du haut sans rien rendre au ressort du dessous), et un `\vbox to \textheight` avec un ressort de chaque côté plutôt que deux `\vspace*{\fill}`, qui laissaient une douzaine de pixels d'écart constants. Ces deux ressorts sont dans un rapport **3:5 et non 1:1** : le titre se pose au centre OPTIQUE, aux deux cinquièmes de la hauteur, parce qu'un titre mathématiquement centré sur une page par ailleurs vide se lit comme tombé trop bas. Les rapports voisins ont été comparés sur le rendu (1:1 tombe, 1:2 remonte trop et le vide du dessous déséquilibre la page). Le `\hsize\textwidth` de ce vbox n'est pas décoratif : en document deux colonnes sa largeur vaudrait une demi-page et le titre se couperait. Vient ensuite un **verso blanc** (`\thispagestyle{empty}\null`), pour qu'en recto-verso le texte commence sur une belle page ; `\restoregeometry` se place **après** lui et jamais avant, sinon son saut de page expédie une page vide de plus, celle-là avec le titre courant en haut. La découpe est **pure d'un côté, binaire de l'autre** : `render_tex` rend une chaîne, donc toute la mise en page se teste en lisant du texte (`test_build_script_pdf.py`), et seul le test de bout en bout compile vraiment, en se sautant là où aucun moteur n'est installé. Aucune dépendance Python : le module écrit du texte et lance un binaire. Le bouton de téléchargement vit sur l'Avancement (`ScriptPdfLink`, `.dash-script-btn`), pas sur un accueil : c'est au respo de décider quelle version fait foi. Le fichier reste servi à une URL publique, c'est la discrétion de `respo.html`, pas un verrou. C'est **`.btn.primary`, le bouton de téléchargement du site**, le même que le ZIP des prises (Enregistrement) et que le script (Édition) : un téléchargement se présente pareil partout. Les deux gestes de la page **ne se ressemblent donc pas au repos**, et c'est le but : le dépôt est une carte blanche (une destination, on part sur GitHub), le téléchargement un aplat d'accent. Deux essais écartés avant : un lien sobre en gris (trop effacé), puis une deuxième carte identique à celle du dépôt (les deux se confondaient). Ils vivent dans `.dash-actions`, colonne centrée en `align-items: center` et **pas** `stretch`, qui étirerait le bouton à la largeur de la carte, ce qu'aucun autre bouton du site ne fait. Hors github.io la carte de dépôt disparaît et le bouton reste seul. **Le bouton se masque lui aussi quand le PDF n'est pas là**, et pas seulement en dev : le fichier est gitignoré et produit par deux étapes `continue-on-error`, donc il manque dès qu'une install apt ou une compilation échoue, et sur une pièce jamais saisie. `ScriptPdfLink` le sonde en `HEAD` au montage et ne rend rien tant qu'il n'a pas répondu (`ready` à `null`, donc le bouton apparaît après la page mais ne disparaît jamais sous la souris) : dans le seul canal de retour du respo, un téléchargement qui rend une page d'erreur renommée « transport-de-femmes.pdf » est pire que pas de bouton, parce qu'il se croit réussi. Même raison qui fait `githubUploadUrl()` masquer la carte plutôt que forger un 404 ; le middleware de dev de `vite.config.js` répond donc au `HEAD` (sans corps) et connaît le type MIME `.pdf`. **Le survol, lui, est commun** : c'est la classe partagée `.lift-hover` (`theme.css`), ombre haute plus pas d'un pixel vers le haut, le geste de toutes les surfaces cliquables du site (les six cartes des deux accueils la portent aussi, avec `--lift: -3px`, seul écart légitime : une carte haute se lève de plus). Le bouton n'en reprend pas le filet teinté (sa bordure est déjà en accent, il n'y aurait rien à voir) et garde à la place l'assombrissement de `.btn.primary:hover`. La `transition` reste chez chaque surface et n'est **pas** remontée dans `.lift-hover` : `transition` est une seule propriété, donc une déclaration locale l'écraserait au lieu de s'y ajouter ; toute surface qui prend la classe doit lister `transform` et `box-shadow` dans SA transition (ici il fallait de toute façon les ajouter, `.btn` ne faisant glisser que `background` et `border-color`). Le retour à l'accueil du bandeau ne prend pas la classe : il garde le pas d'un pixel mais remplace l'ombre par la nappe crème du sceau, c'est un autre geste. **Pas de sceau sur le bouton** : sur la carte de dépôt la plume désigne le `script.json` qu'on DÉPOSE, la reprendre pour un PDF qu'on TÉLÉCHARGE ferait lire deux gestes opposés sous la même pastille. Libellé « Télécharger la pièce à imprimer (PDF) » et **surtout pas « le script de la pièce »** : la carte de dépôt dit déjà « script de la pièce (JSON) », et deux libellés partageant leur groupe de mots ne se distinguaient plus que par l'acronyme, alors que PDF est le seul des trois (ZIP, JSON, PDF) qu'un respo lit sans y penser. « à imprimer » dit l'usage ; le mot « script » reste au dépôt, où il désigne le fichier de travail. Le `download` renomme le fichier avec le slug de la pièce, « script.pdf » ne disant rien dans un dossier de téléchargements. Les didascalies n'y sont pas encore : elles n'existent pas dans `script.json`, et le script LaTeX d'origine en distinguait trois sortes (`\stage`, `\stageinline`, `\stagemain`), donc le futur champ ne sera pas un booléen |
| Chemins repo côté Python | `scripts/common.py` (`REPO_ROOT`, `write_json`) |
| Middleware dev data/clips | `serveRepoData` dans `vite.config.js` |

Pour tester les pages sans build : éditer `data/manifest.json` à la main puis
`npm run dev`.

`data/history.json` est **livré vide** (`{"runs": []}`) : le journal d'un dépôt
est l'histoire d'une troupe, pas une donnée d'exemple. `Journal` reste affiché
même vide (tableau et colonnes en place, une ligne d'explication à la place des
dépôts) : c'est le seul canal de retour du projet, donc il doit se faire
connaître avant le premier dépôt, pas apparaître le jour où il a une mauvaise
nouvelle à donner. Pour voir la page peuplée, copier le jeu d'exemple partagé,
qui couvre exprès les quatre cas d'affichage :

```bash
cp scripts/tests/history-example.json data/history.json && python3 scripts/build_manifest.py
```

C'est le **même fichier que les tests** (`test_history.py` vérifie sa forme et
`test_build_manifest.py` sa recopie dans le manifest) : un exemple qui dérive du
contrat casse la CI au lieu de mentir tranquillement à l'écran.
