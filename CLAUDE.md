# PrettyDrama Voices

Outil libre pour troupes de théâtre : répétition « à l'italienne » avec les vraies
voix des acteurs. Site statique (GitHub Pages) + GitHub Action Python/ffmpeg.
L'**interface est bilingue**, français par défaut et anglais, et aucun texte
visible ne vit dans un composant : tout passe par les catalogues de
`src/shared/locales/` (cf. la ligne « Interface bilingue » des repères rapides).
Le DÉPÔT, lui, reste en français : README, commentaires, et les messages d'erreur
de l'Action, qui finissent affichés dans le journal des dépôts (le respo lit son
dépôt, pas une locale).
Convention d'écriture : jamais de tiret cadratin, ni dans cette doc, ni dans
les textes affichés à l'utilisateur, ni dans le catalogue anglais. Utiliser
deux-points, point-virgule, virgule, parenthèses ou une phrase de plus.

## Architecture

- **Frontend** : React + Vite, multi-pages (pas de SPA). Entrées déclarées dans
  `vite.config.js` : `index.html` (accueil acteurs), `respo.html` (accueil
  responsable), `rehearsal.html`, `recorder.html`, `stats.html`,
  `dashboard.html`, `editor.html`. Sources dans `src/<page>/`, partagé dans
  `src/shared/` (`data.js` = fetch + helpers, `useManifest.js`,
  `PageHeader.jsx` (bandeau des écrans sans pièce à nommer, de la MÊME géométrie
  que `PlayHeader` : sceau et titre en rangée du haut, `HomeLink` en pied ; ces
  écrans sont l'attente des cinq pages à bandeau de pièce, et tant que la
  marque vivait ici en haut à gauche et là en pied, le nom du site sautait d'un
  bout à l'autre du bandeau à l'arrivée du manifest),
  `PageState.jsx` = écran chargement/erreur commun (ses DEUX états, l'attente
  comme le message, vivent dans la carte partagée `.page-notice` de `theme.css`,
  comme tout ce qui porte du contenu sur ce site ; en texte nu sur le crème ces
  écrans étaient les seuls à ne rien poser sur le papier et se lisaient comme une
  page à moitié chargée, et une carte qui n'arriverait qu'au second état se
  lirait comme un changement de page). **Aucun bandeau du site n'écrit son
  libellé de page** (« Répétition », « Édition », « Avancement ») : le sceau dit
  où on est, et l'onglet du navigateur le répète. `title` ne dit donc QUE le
  titre de la pièce, et il est **facultatif** : tant que la pièce est inconnue,
  la rangée du haut n'a rien à dire et le titre n'est pas rendu du tout
  (`PageHeader` ne pose même pas le `<span>`). C'est la seule façon que le titre
  **apparaisse** au lieu d'en **remplacer** un autre : un libellé écrit pendant
  le chargement se faisait recouvrir par le titre de la pièce une fraction de
  seconde plus tard, et ce clignotement se voyait à chaque ouverture de page.
  Le vide ne coûte rien, la hauteur de la rangée étant fixée par le sceau (36 px)
  et non par le titre (24 px de ligne) : rien ne bouge quand il arrive, et le
  message de l'écran, lui, est dans la carte au-dessous. Les écrans **définitifs**
  nomment donc la pièce comme les cinq bandeaux, et il y en a **trois**, tous
  rendus après le chargement, exprès : l'Édition ouverte au doigt,
  l'Enregistrement sur un navigateur qui ne sait pas enregistrer, et la
  Répétition d'une pièce encore vide. Un écran où l'on reste est le contenu final
  de la page, pas son attente. Corollaire : le repli quand la pièce n'a pas de
  titre est « Pièce sans titre » partout, sur les cinq bandeaux comme sur ces
  trois écrans (il valait « Répétition » ici et « Pièce sans titre » là). Le
  titre lui-même vit dans **une seule règle CSS** pour les deux bandeaux
  (`.play-header-title, .page-header .page-title`) : il était rendu en 15 px
  gris dans la font UI d'un côté et en serif 20 px de l'autre, donc il changeait
  de corps, de graisse, de couleur ET de fonte selon le bandeau qui le portait
  (et prenait au passage l'IBM Plex du re-skin sur l'Édition, alors que
  `--header-serif` existe pour l'empêcher). `.page-header` porte pour la même
  raison le `--header-shadow` de `.play-header`, sinon le bandeau *gagnait* son
  ombre en cours de route,
  `HomeLink.jsx` = le retour à l'accueil (logo aux deux masques + le mot
  « PrettyDrama », encadré de deux filets courts), **un seul composant pour les
  deux bandeaux** : c'est ce qui garantit que la marque est au même endroit
  pendant le chargement et après,
  `PageMark.jsx` = la pastille de sceau, qui pose la classe `page-<clé>` sur
  elle-même (elle porte donc ses couleurs partout, y compris hors d'un bandeau).
  Deux autres endroits la posent : les deux bandeaux partagés sur leur racine
  (les tokens de sceau sont ainsi disponibles à tout leur contenu ; plus rien ne
  les y consomme depuis le retrait du filet de 3 px), et le lien de `HomeLink`,
  en `page-home`, lui pour une vraie raison, la nappe crème de son survol, qui
  doit rester celle de la marque sur les cinq pages (le garde CI de
  `test_contracts.py` interdit les tokens de sceau aux règles de bandeau et
  exempte nommément `.play-header-home*`, cette classe posée en JSX lui étant
  invisible),
  `useScrollToActiveCard.js` = défilement vers la `.dialogue-card.active`,
  partagé par Répétition et Enregistrement, `theme.css`, plus tout l'appareil de
  la traduction : `i18n.js` (moteur pur), `locale.js` (`t`, `fmt`, la locale du
  document), `T.jsx` (une phrase qui porte du balisage), `LocaleSwitch.jsx`,
  `locales/fr.js` et `locales/en.js`, `structureLabels.js` (« Acte II »,
  « Scène 3 », dérivés du rang), cf. la ligne « Interface bilingue »). `dist/` est
  gitignoré, build en CI.
- **Backend** : aucun serveur, **deux workflows** aux rôles étanches.
  `uploads.yml` (sur push touchant `uploads/**`) traite les dépôts, écrit le
  journal, commite, **puis appelle `build.yml`** (`jobs.site.uses`) ;
  `build.yml` (sur tout autre push, `workflow_dispatch`, `workflow_call`)
  construit et déploie Pages et n'écrit **jamais** dans le dépôt. Trois choses
  à ne pas défaire : (1) l'appel explicite, parce qu'un commit poussé avec le
  `GITHUB_TOKEN` ne déclenche aucun workflow (garde anti-récursion), donc sans
  lui le site ne serait jamais republié après un dépôt ; (2) les **groupes de
  `concurrency` distincts** (`uploads` et `pages`), un workflow appelé qui
  partage le groupe de son appelant étant détecté comme interblocage et annulé ;
  (3) le `paths-ignore: uploads/**` de `build.yml`, sans quoi un dépôt
  déclencherait les deux en parallèle et publierait un site sans les nouvelles
  voix. `build.yml` fait son checkout sur `ref: github.ref_name` et pas sur le
  SHA du run : appelé, il tourne dans le run d'un push antérieur au commit
  qu'`uploads.yml` vient de pousser.
  **Aucun des deux n'écrit à destination du respo sur GitHub** : ni issue, ni
  statut dans le README (il part du bouton de la page Avancement et ne visite
  jamais le dépôt, donc il ne les lirait pas). Son seul retour est le journal
  des dépôts de la page Avancement. Corollaires assumés : un run en échec ne se
  raconte pas (mais la date du dernier dépôt cesse d'avancer), et le commit
  précède le déploiement (l'inverse imposerait de transporter `clips/` et
  `data/` en artefact entre deux jobs ; si le déploiement échoue, les données
  sont dans le dépôt et le prochain build les publie).
- Dev : `npm run dev` (un middleware Vite sert `data/` et `clips/` depuis le
  repo avec de vrais 404), ou `npm start` = `scripts/dev.sh` (install si besoin,
  ouverture des DEUX accueils dans le navigateur dès que le serveur répond ;
  `exec ./node_modules/.bin/vite`, pas `npm run dev`, pour que Vite prenne la
  place du script : le Ctrl+C tue vraiment le serveur et libère le port, sans
  wrapper npm ni pid à surveiller), `npm run build`. **Deux suites de tests**,
  toutes deux rejouées par `build.yml`, donc à chaque push de code comme après
  chaque dépôt (`uploads.yml` finit par l'appeler) :
  - Python, `python3 -m unittest discover -s scripts/tests` : statuts,
    normalisation (cas partagés dans `normalize-cases.json`), contrat ZIP,
    journal, plus les **contrats inter-fichiers** (`test_contracts.py`, voir
    plus bas). Un seul test :
    `python3 -m unittest scripts.tests.test_normalize.TestNormalizeSharedCases.test_idempotent`.
  - JS, `npm test` = `node --test` **sans argument** : la logique **pure** du
    front, celle qui ne se relit pas à l'œil. `reducer.test.js` (réparation d'un
    `script.json` douteux, ids jamais recyclés, qui parle après une
    suppression), `history.test.js` (fusion des frappes, « Modifications non
    téléchargées », le remplacement global qui ne fait qu'une étape),
    `search.test.js` (le repliement à longueur conservée, donc des offsets qui
    restent ceux du texte brut ; « Mot entier » ; le remplacement qui contient la
    requête), `data.test.js` (URL de dépôt, slugs), `useRecorder.test.js`
    (l'extension du membre audio reste dans l'alphabet qu'accepte l'Action),
    `characterColors.test.js` (le comblement déterministe des couleurs, donc
    l'accord entre l'Édition et la Répartition sur un script qui n'en porte
    aucune), `stats.test.js` (la somme des largeurs du bloc vaut le nombre de
    mots, le bornage des portées, les répliques sans personnage),
    `i18n.test.js` (quelle locale gagne des trois couches, le pluriel choisi par
    `Intl`, le placeholder manquant laissé VISIBLE plutôt que rendu
    « undefined »), `structureLabels.test.js` (les chiffres romains, et l'accord
    avec le PDF), et `locales/parity.test.js` (les deux catalogues se répondent :
    mêmes clés, mêmes placeholders, mêmes formes de pluriel, insécables
    françaises présentes et absentes de l'anglais).
    **Aucune dépendance de test** : `node --test` est intégré à Node, ces
    modules sont du JS pur sans React ni DOM, et un fork de troupe ne doit pas
    payer un `npm ci` plus lourd pour ça. Pas de test de composant React, donc
    pas de rendu : ce qui touche au DOM se vérifie toujours à la main.
    **Pas d'argument de chemin**, et c'est voulu : `node --test src/` marchait
    sur Node 20 mais Node 22+ traite un positionnel comme un fichier de test, pas
    comme un dossier à parcourir (« Cannot find module .../src »). Sans argument,
    Node applique ses motifs par défaut (`**/*.test.js`, `node_modules` exclu) et
    trouve exactement les mêmes fichiers, de Node 20 à Node 24. Contrepartie
    assumée : le périmètre n'est plus `src/` mais tout le dépôt, donc un futur
    `test-*.js`, `*-test.js` ou un dossier nommé `test/` (au singulier, motif
    `**/test/**`) serait exécuté sans qu'on l'ait demandé ; `scripts/tests/` y
    échappe parce qu'il est au pluriel. La piste écartée est le glob explicite
    `node --test 'src/**/*.test.js'` : correct sur Node 22+ seulement, et ses
    guillemets simples ne sont pas des guillemets pour le `cmd.exe` que npm
    utilise sous Windows.

  Build de prod à tester à la main (l'Action copie `data/` et `clips/` dans
  `dist/`) : `npm run build && cp -r data clips dist/ && npm run preview`.

## Flux de données

1. `data/script.json` : **source de vérité**, produit par l'éditeur. Il n'est
   plus déposé par-dessus : il arrive dans `uploads/`, est validé
   (`validate_script`) puis promu. Reste potentiellement malformé (édition à la
   main dans le dépôt) : tout consommateur doit être tolérant, cf.
   `sanitize_script`.
2. Page Enregistrement → ZIP `voix-<slug>.zip` (le NOM suit la locale du lecteur,
   `voices-<slug>.zip` en anglais : il vient du catalogue comme tout nom de fichier
   téléchargé, et l'Action ne le lit jamais, cf. `kind_of`) contenant :
   - `manifest.json` : **mapping nu `{lineId: texte brut au moment de
     l'enregistrement}`** (rien d'autre : ni nom de fichier ni personnage) ;
   - un audio `{lineId}.{ext}` par réplique (`ext` = webm/mp4/ogg selon le
     navigateur, cf. `extensionForMimeType`). L'Action retrouve le fichier
     depuis l'id seul.
3. L'acteur envoie le ZIP au responsable, qui le glisse dans `uploads/`. **Un
   seul dossier de dépôt** pour les deux sortes de fichiers (le respo n'a qu'une
   adresse à connaître, d'où le bouton unique de l'Avancement) ; le type vient
   de la seule extension (`kind_of` : `.zip` = voix, `.json` = script), pas du
   nom, que le navigateur renomme volontiers en « script (1).json ».
4. `scripts/process_uploads.py`, pour chaque fichier de `uploads/` :
   - `.zip` : valide TOUT le manifest, transcode chaque clip (ffmpeg : trim
     silences + loudnorm, mp3 mono 64 kbps) → `clips/{lineId}.mp3` et
     `data/clips.json` (`{lineId: texte brut}`). Merge **tout-ou-rien par ZIP** ;
   - `.json` : `validate_script` puis promotion **verbatim** dans
     `data/script.json` (passer par `sanitize_script` perdrait les teintes des
     personnages) ;
   - autre : consigné comme inconnu.
   Le fichier est supprimé même en cas d'erreur (sinon il échouerait à chaque
   run) ; les fichiers cachés (`.gitkeep`) sont laissés en place. Sort de chaque
   fichier (succès **et** erreur) écrit dans `uploads_result.json` (éphémère,
   gitignoré).
5. `scripts/update_history.py` : `uploads_result.json` → une entrée
   `{at, files}` dans `data/history.json` (journal des dépôts affiché par
   l'Avancement, plafonné à `MAX_RUNS`). Écrit par `uploads.yml` seul, donc le
   journal ne contient QUE des dépôts, jamais une reconstruction du site.
6. `scripts/build_manifest.py` : join stateless `script.json` × `clips.json` →
   `data/manifest.json`, **seul fichier lu par les pages** (l'éditeur lit aussi
   `script.json`) ; y recopie aussi `history.json`. Statut par réplique : `ok` / `perime` (« À refaire », texte modifié depuis
   l'enregistrement) / `manquant` (« À enregistrer »).
7. `scripts/build_script_pdf.py` : `script.json` → `data/script.pdf`, le script
   de la pièce mis en page pour l'impression (LaTeX). Dérivé comme le manifest,
   mais **gitignoré** et construit par `build.yml` seul, donc jamais commité.

## Invariants (à ne pas casser)

- **Normalisation de texte : une seule implémentation**, `scripts/normalize.py`,
  appelée uniquement dans `build_manifest.compute_status`. Le navigateur stocke
  et transporte du texte **brut**, jamais normalisé.
- **Les ids de répliques ne sont jamais recyclés** (ils nomment les mp3).
  `SAFE_ID` (`src/editor/reducer.js`) et `LINE_ID_PATTERN`
  (`scripts/process_uploads.py`) doivent rester synchrones :
  `^[0-9a-zA-Z-]{1,64}$` (alphanumérique, pas seulement hex : ids lisibles
  édités à la main acceptés). Ce n'est plus une consigne mais un test :
  `test_contracts.py` lit les deux fichiers et compare les deux expressions,
  donc les faire diverger casse la CI.
- Le format du ZIP est un contrat navigateur ↔ Action : toute modification doit
  toucher `downloadZip` (recorder `App.jsx`) ET `parse_manifest`
  (`process_uploads.py`) en même temps.
- `sanitize_script` (Python) est le miroir tolérant du `sanitizeScript` de
  l'éditeur : une entrée malformée est ignorée, jamais un crash de workflow.
  Asymétrie assumée : le JS valide chaque id contre `SAFE_ID` et **reminte** les
  ids invalides/dupliqués (il est le producteur, doit rester réparable) ; le
  Python ne vérifie qu'« id = chaîne non vide » (il ne peut pas reminter sans
  orpheliner les mp3 déjà nommés). C'est sans conséquence : un id hors `SAFE_ID`
  hand-édité dans `script.json` n'a jamais de clip (l'Action rejette ces ids à
  l'upload) → statut `manquant`, `clip: null`, aucune URL forgée n'est émise.
  Seconde asymétrie assumée, celle-là dans l'autre sens : le JS **plancher** la
  structure (une pièce sans acte, ou un acte sans scène, en reçoit un vide) parce
  qu'il faut bien une scène où écrire, le Python **jamais** (`acts: []` rend
  `acts: []`), parce qu'un lecteur n'invente pas un « Acte I / Scène 1 » que le
  fichier ne porte pas et qui s'afficherait vide dans la grille de l'Avancement et
  dans la portée de la Répartition. Tout le reste doit dire exactement la même
  chose des deux côtés, y compris ce qui ne se lit pas dans un `isinstance` :
  **un personnage sans nom réel est écarté par les deux** (`c.name.trim()` en JS,
  `c["name"].strip()` en Python). Le garder d'un seul côté mettait une ligne
  anonyme dans la grille de l'Avancement et un bouton sans libellé dans la légende
  de la Répartition, là où l'Édition montrait déjà ses répliques comme non
  attribuées ; écarté, elles retombent sur le « ? » commun.
- **`sanitizeScript` ne déplace jamais une réplique d'un personnage à un
  autre.** Deux cas à ne pas confondre quand un id de personnage est reminté :
  hors `SAFE_ID`, l'id n'est porté que par lui, donc ses répliques le suivent
  (`characterRemap`) ; **dupliqué**, le premier porteur garde l'id ET ses
  répliques, le second repart avec un id neuf et aucune réplique. Remapper un
  doublon changerait qui parle dans la pièce alors que les mp3, nommés par id
  de *réplique*, ne bougeraient pas : la voix enregistrée par l'un sortirait
  sous le nom de l'autre. Corollaire de tuyauterie : le remap se consulte sur
  la valeur brute du `characterId`, **avant** tout contrôle contre `SAFE_ID`,
  puisque les ids qu'il contient sont justement ceux qui n'y passent pas.
- **Un no-op ne doit pas fabriquer un nouvel état.** `updateScene`
  (`reducer.js`) rend l'état reçu tel quel quand la scène ne change pas, et
  `scriptReducer` en fait autant pour toute action refusée. `history.js`
  reconnaît une action sans effet à l'identité (`present === state.present`) :
  sans cette sortie, reposer une réplique glissée exactement où elle était
  allumait « Modifications non téléchargées » et laissait une étape vide à
  annuler.
- Uploads hostiles : caps de taille réels (les en-têtes ZIP mentent), noms de
  membres validés par fullmatch, un fichier cassé ne bloque jamais les autres.
- **`validate_script` est volontairement plus strict que `sanitize_script`** :
  le second est un lecteur tolérant, le premier décide d'écraser la source de
  vérité. Un JSON valide mais étranger (`[1,2,3]`, un export d'autre chose) se
  sanitiserait en pièce vide et effacerait la pièce de la troupe, d'où le
  garde-fou : un candidat sans aucune réplique ne remplace jamais une pièce qui
  en a.
- Prises d'enregistrement : en mémoire uniquement (garde `LeaveGuard` tant que
  non exportées), une seule prise par réplique, `URL.revokeObjectURL` à chaque
  remplacement.
- **Aucun texte visible ne vit dans un composant.** Tout passe par
  `src/shared/locales/fr.js` et `en.js` : texte entre balises, `title`,
  `aria-label`, `placeholder`, `alt`, et les props qui portent du texte (`hint`,
  `error`, `label`, `unit`, `confirmLabel`, `primaryLabel`, `saveLabel`), jusqu'au
  nom des fichiers téléchargés. Un littéral oublié ne se voit PAS côté français,
  donc ce n'est pas une consigne mais trois tests (`TestCatalogues` dans
  `test_contracts.py`) : un littéral accentué, un attribut porteur de texte, un
  nœud de texte JSX. Corollaire de tuyauterie : un module couvert par
  `node --test` n'importe jamais `locale.js` (qui lit l'URL, le stockage et le
  navigateur dès son import) ; il reçoit `t` en argument (`stats.js`) ou rend un
  CODE que la page traduit (`useRecorder.js` et son `"mic"`).

## Repères rapides

| Quoi | Où |
| --- | --- |
| Lien GitHub affiché (footer accueil) | `src/home/App.jsx`, repo `ThomasParistech/prettydrama-voices` |
| Interface bilingue | Moteur PUR dans `src/shared/i18n.js` (quelle locale gagne, le pluriel par `Intl.PluralRules`, le remplissage des `{placeholder}`, les guillemets et les formats de nombre et de date par locale), face environnement dans `src/shared/locale.js` (lit `?lang=`, puis le choix mémorisé, puis le navigateur ; expose `t`, `fmt`, `LOCALE`, `applyDocumentLanguage`). Deux catalogues plats et dotés, `src/shared/locales/fr.js` (langue de référence) et `en.js`. **Un singleton de module et pas un contexte React**, et c'est une conséquence de l'architecture : le site est multi-pages, sept documents, et changer de langue NAVIGUE, donc la locale est une constante par document. Le choix est mémorisé dans le seul `localStorage` du projet (`prettydrama.lang`), exception assumée à la règle « aucune persistance locale », qui porte sur le TRAVAIL : sans lui, `navigator.language` ramène un francophone au français sur chaque page qui perd le paramètre, et ce site le perd à CHAQUE navigation (un lien interne est un href relatif nu, donc `?lang=en` disparaît dès qu'on quitte la page ouverte, et un bookmark ou une adresse tapée à la main n'en a jamais eu). Trois choses à ne pas défaire. (1) **Une phrase qui porte du balisage au milieu passe par `<T k="…" p={{ … }} />`** (`src/shared/T.jsx`), le morceau de JSX devenant un PARAMÈTRE : découpée en fragments dans le composant, elle y figerait l'ordre des mots français, et c'est irréparable en traduction (le pire cas du dépôt était la carte de dépôt de l'Avancement, six fragments dans quatre spans imbriqués, où l'ordre comptait parce que chaque couleur suit son nom). (2) **Aucun pluriel bricolé** : une entrée `{ one, other }` et `t(clé, { count })`, le choix venant d'`Intl.PluralRules`, qui sait que zéro est singulier en français et pluriel en anglais, ce qu'aucun `n > 1 ? "s" : ""` ne savait ; même règle pour les nombres, les pourcentages, les dates et les guillemets (`fmt.percent`, `fmt.dateTime`, `fmt.quote`), qui remplacent un `.replace(".", ",")`, un `"fr-FR"` en dur et des `«&nbsp;…&nbsp;»` écrits à la main. **Un paramètre NUMÉRIQUE est formaté par le moteur lui-même** et pas au point d'appel : tout `{count}` du site est une quantité, il y en a une douzaine, et un seul oublié se relit parfaitement en français, d'où « 10 307 mots » et « 10,307 words » sans qu'aucun composant s'en occupe. `fmt.number` ne sert donc qu'aux nombres écrits SEULS, hors de toute phrase (le total au centre de l'anneau de la Répartition et les décomptes de sa légende), et c'est le même formateur des deux côtés. Un paramètre CHAÎNE traverse intact, ce qui garde dehors un pourcentage déjà composé et le chiffre romain d'un acte. (3) **La typographie française vit DANS les chaînes** (insécable avant `?`, `!`, `:`, guillemets) et pas dans le JSX : c'est un fait de langue, donc l'affaire du traducteur, et l'anglais ne le porte pas. Un libellé que deux endroits nomment ne s'écrit qu'une fois, le second INTERPOLANT la clé du premier (le vide de la Répartition cite `stats.scopeAllOption`, l'aide d'une scène vide cite `rail.characters`). Le cas le plus lourd est le **nom d'une page citée dans une phrase** : six entrées de chaque catalogue nomment l'Édition, donc elles reçoivent un `{page}` alimenté par `t(pageLabelKey("editor"))` plutôt que le mot recopié, et un garde de `test_contracts.py` interdit désormais la tournure « page X » / « mode X » écrite en clair. Il est BORNÉ à cette tournure exprès : en français les noms de page sont des noms communs, donc « Enregistrement… » (la prise en cours) et « Avancement par personnage et par scène » sont des emplois légitimes qu'un garde plus large relevait, et une liste d'exemptions qui grandit à chaque phrase n'aide personne. À ne pas confondre avec la **langue de la PIÈCE** (`script.language`, choisie dans le plan du rail) : celle-là pilote le PDF et la voix de synthèse, alors que les libellés d'acte et de scène (`src/shared/structureLabels.js`) suivent la locale du LECTEUR, étant de la navigation. Le sélecteur de langue est `src/shared/LocaleSwitch.jsx`, deux vrais liens (donc clic droit et nouvel onglet, et aucun gestionnaire de clic : le `href` porte `?lang=`, et c'est le chargement de page suivant qui le mémorise). **Il vit au pied des DEUX accueils et nulle part ailleurs** : une langue est un réglage de SITE et pas de page, donc elle se choisit en entrant, et le pied du bandeau partagé est une composition finie (le sceau seul et centré, encadré de deux filets courts qui le ferment comme un cul-de-lampe), qu'un second objet décentrerait ; sur sa propre ligne il donnerait au bandeau un troisième paragraphe là où la doctrine en autorise deux. Sous la phrase du pied et pas à côté d'elle : la phrase dit ce qu'est le site, le sélecteur est une commande, et sur une seule ligne on lisait « PrettyDrama FR | EN » comme une suite de liens. **Ce sont deux DRAPEAUX** (`FlagIcon`, cf. la ligne « Icônes ») et non plus les sigles « FR » / « EN » : un drapeau nomme un pays et pas une langue, ce qui reste vrai, donc le nom de la langue voyage avec lui, en `title` et en nom accessible du lien (le drapeau est `aria-hidden`, et la langue courante est un `<span>` en `role="img"`, le motif de `PageMark`) ; ce qu'on gagne pour ce défaut, c'est un sélecteur qu'on trouve sans le lire, quand on ne lit justement pas la langue dans laquelle la page est affichée, c'est-à-dire exactement le lecteur à qui ce contrôle sert. Les endonymes auraient rendu le même service (« Français » reste « Français » dans une interface anglaise), mais deux mots dans un pied de page se lisent comme une phrase là où deux drapeaux se lisent comme un interrupteur. Et le nom d'une langue s'y écrit **dans cette langue, jamais traduit** (`NAMES` dans `LocaleSwitch.jsx`, le seul littéral accentué que le garde CI exempte nommément) : on cherche sa langue avec son propre mot pour elle. La langue courante n'est pas un lien (rien à cliquer, donc rien ne s'allume au survol) et c'est un filet d'accent sous son drapeau qui dit « vous êtes ici », jamais une opacité réduite sur l'autre, qui ferait lire l'autre langue comme indisponible alors que c'est celle qu'on propose. **Le même couple de drapeaux sert la langue de la PIÈCE** dans le plan du rail, et les deux ne se confondent pas pour autant : ici on choisit la langue qu'on LIT et le choix est un lien qui recharge le site, là on énonce un fait sur un document et le choix est un champ (des boutons radio) qui édite la pièce, d'où un nom de langue TRADUIT de ce côté-là (« Anglais » dans une interface française) et un cadre plein plutôt qu'un filet, le vocabulaire du panneau |
| Les deux accueils | même composant `src/home/App.jsx`, deux entrées et deux listes de cartes (`ACTOR_CARDS`/`RESPO_CARDS` dans `src/shared/pages.js`) : `index.html` + `src/home/main.jsx` ne montre que Répétition, Enregistrement et Répartition (c'est le lien donné à la troupe : un acteur ne doit jamais tomber sur l'éditeur, d'où se télécharge le script), `respo.html` + `src/home/respo.jsx` montre les cinq pages, trois par rangée (les pages de la troupe en haut, les deux modes du responsable centrés au-dessous). **Aucun lien ne mène de l'accueil des acteurs vers celui du responsable** (sinon la séparation ne sert à rien) : il se bookmarke, et l'adresse est `respo.html`, tout simplement. Un **raccourci `…/respo`** (sans `.html`) a existé, servi par un `public/respo/index.html` qui rebondissait en `meta refresh` sur `../respo.html`, et il est **retiré** : la barre d'adresse finissait de toute façon sur `respo.html`, donc le raccourci n'économisait que la frappe d'une adresse qu'on met en favori, et il coûtait un fichier de plus, un aller-retour réseau à chaque ouverture, une page sans JS (donc le seul texte affiché du site que la locale du lecteur ne pouvait pas atteindre) et un `<p>` de repli pour les navigateurs qui ignorent le `meta refresh`. La piste qui restait écartée est une vraie entrée Vite `respo/index.html` : tout le site vit en chemins relatifs (`base: "./"`, `fetch("data/manifest.json")`, `href="./rehearsal.html"`), donc une page à un niveau de profondeur obligerait le code partagé (`data.js`, `pages.js`, `homeHref`) à savoir dans quel dossier elle tourne. Corollaire porté par `homeHref(page)` (`pages.js`) et consommé par les deux bandeaux partagés : le retour à l'accueil des pages Édition et Avancement mène à `respo.html`, celui des deux autres à `index.html` (sur les pages à bandeau repliable ce n'est plus la marque qui porte ce lien, cf. la ligne « Bandeau de pièce »). Les cartes n'ont plus de sous-titre « Pour les acteurs / Pour le responsable » : l'URL le dit. `npm run dev` et `npm run preview` annoncent les deux URLs (plugin `printHomeUrls` dans `vite.config.js`, qui enveloppe `server.printUrls`) : l'accueil caché n'est pas à retenir de tête |
| Couleurs des sceaux | `.page-<clé>` dans `src/shared/theme.css` (`--page-mark` / `--page-mark-soft`), **source de vérité** ; la couleur de page ne teinte plus QUE la pastille : le filet de 3 px qu'elle posait en haut de chaque écran (bandeaux et accueil) est retiré, il barrait la page d'un trait dont le sceau disait déjà tout ; les favicons et `theme-color` des `.html` les redupliquent (un `<link>` ne lit pas une variable CSS). Les pages de la troupe (marque, Répétition, Enregistrement, Répartition) partagent EXACTEMENT le même couple, bordeaux `#8b2635` sur sable `#f5eeda` : elles avaient chacune sa nuance (deux bordeaux voisins et une terre), assez proches pour qu'on n'y lise pas une distinction, assez différentes pour qu'on les croie ratées. Ce sont les icônes (masques, bulles, micro, camembert) qui disent la page. Seuls les modes du responsable gardent une couleur propre, vert pour l'Avancement et violet pour l'Édition : elle ne sépare plus deux pages voisines, elle marque un autre territoire. Corollaire : la terre `#a84f00` n'est plus qu'un statut (`--warn`, « À enregistrer »), plus une identité de page |
| Logo (les deux masques) | `MasksIcon` dans `src/shared/icons.jsx`. **Géométrie fournie par un fichier de design (`design/drama-wine.svg`, la livraison est versionnée, cf. `design/README.md`), à ne pas redessiner** : les 8 tracés et leurs `transform` sont repris tels quels. Deux adaptations assumées, sans toucher aux formes : les remplissages passent en `currentColor` (le vin) et `var(--page-mark-soft)` (les deux aplats d'intérieur) pour que la marque suive le système des sceaux au lieu de figer des hex ; le `viewBox` est recadré au carré (`37.5 36 262 262`) parce que l'encre était décentrée dans la boîte d'origine (259x262 à l'offset 39,36 dans 329x345), ce qui donnait un anneau inégal dans la pastille. Le dessin ne se lit qu'à partir de ~34 px (à 20 px les deux masques se touchent) : il sert en grand dans le hero de l'accueil, sur une pastille agrandie (`.home-brand-mark`, 52 px pour 34 px de dessin), et **en petit sur le lien de retour à l'accueil du bandeau déplié** (`.play-header-home-mark`), où la contrainte qui gagne est l'accord des deux pastilles : le bandeau ne porte que le sceau de la page et celle-ci, et une taille chacune se lisait comme un décalage, donc elle prend exactement la taille du sceau (36 px, 30 px sous 800 px). Ce qu'on lui rend à la place, c'est du remplissage : le dessin y occupe environ trois quarts de la pastille (26 px dans 36, et 22 px dans 30) là où un sceau de page en occupe un peu plus de la moitié. Sous le plancher de lisibilité, donc, mais le mot « PrettyDrama » est écrit juste à côté. Les bandeaux portent par ailleurs le sceau de leur page. La marque n'étant pas une page, `.page-home` prend le bordeaux `#8b2635` de la marque (et non plus l'or) : logo et mot « PrettyDrama » s'accordent. Favicon d'`index.html` et de `respo.html` : même géométrie et même recadrage, **en bordeaux sur tuile crème `#f5eeda`, comme la pastille du hero** (les deux aplats d'intérieur en crème aussi, exactement le rapport de `MasksIcon` sur `--page-mark-soft`), comme les six icônes du site, cf. la ligne suivante. Corollaire mobile : la règle qui rapetisse le sceau à 30/17 px sous 800 px (fin de `theme.css`) est visée sur `.page-header .page-mark` et `.play-header .page-mark`, **jamais sur `.page-mark` tout court**, sinon la marque tombe à 17 px, taille à laquelle les deux visages se touchent. Ça tenait avant par accident (même spécificité que `.home-brand-mark`, qui ne gagnait que parce que `main.jsx` importe `theme.css` avant `home.css`) |
| Icônes | `src/shared/icons.jsx`, toutes en SVG dimensionné sur la font-size (1em) et en `currentColor` : **aucun emoji dans l'UI**, ni de contrôle ni de ponctuation. Sur mobile, `▶ ⏸ ⚠️ 🤖` rendent en couleur pleine, hors palette, avec une hauteur qui change d'une plateforme à l'autre. Deux familles : aplats pour les contrôles (lecture, micro), trait de 2 px pour tout le reste. Exceptions assumées : quelques caractères restent des caractères, parce qu'ils sont monochromes (aucune variante emoji, donc aucun rendu en couleur pleine) et qu'ils suivent la fonte du texte : `✓ ✕ ↓` des étiquettes et boutons, le chevron `▼` du repli de `PlayHeader`, la poignée de glisser `⠿` de `LineRow`, et le `?` de la pastille « type inconnu » du journal. Autre exception, celle-là graphique : `SparkleIcon` (« voix de synthèse ») est en aplat alors que sa famille est au trait, parce que son étiquette est en 11.5 px et qu'un trait s'y referme (un robot au trait n'y était plus qu'une tache). Troisième exception, la seule à sortir complètement du système : **`FlagIcon`**, les deux drapeaux des deux endroits où l'on choisit une langue (`LocaleSwitch` au pied des accueils pour la langue du SITE, la section « Structure » du rail pour celle de la PIÈCE). C'est la seule image du dépôt qui ne soit **ni en `currentColor` ni dimensionnée sur la font-size** : un drapeau a ses couleurs propres et une taille fixe (24x16 dans `.flag-icon`, `theme.css`, une seule règle pour les deux consommateurs, comme les cases à cocher et `.confirm-quote`). Elle est en revanche **dessinée et jamais l'emoji drapeau**, et c'est la règle générale qui l'exige plutôt qu'une entorse de plus : 🇫🇷 est une paire d'indicatifs régionaux, dont Windows ne rend aucun et affiche les deux lettres « FR » à la place, ce qui remettrait en silence une paire de sigles sur la moitié des postes de la troupe. Les deux tiennent une boîte 3:2 commune, l'Union Jack (2:1 dans la réalité) étant étiré à la verticale plutôt que redessiné : deux vignettes de largeurs différentes côte à côte se lisent comme un défaut d'alignement. Le filet de `.flag-icon` est un `box-shadow` extérieur et pas une `border` : sans lui la bande blanche du tricolore et le fond blanc de l'Union Jack se fondent dans le papier crème, et en `border-box` une bordure rognerait un pixel de dessin sur chaque bord. `WarnIcon` porte lui-même sa classe d'alignement `.warn-icon` (`theme.css`) : il ne sert qu'en tête de phrase, dans les cinq avertissements du site |
| Icônes d'onglet et d'écran d'accueil | favicon SVG en `data:` URI dans chaque `.html`, plus un PNG par page dans `public/`. **Chaque icône est la pastille de sceau de sa page** : tuile en `--page-mark-soft` (crème, vert pâle, lilas), glyphe en `--page-mark` (le dessin de la page, aux mêmes tracés que son `PageMark`), donc exactement ce que `.page-mark` peint à l'écran, hex recopiés puisqu'un `<link>` ne lit pas une variable CSS. C'était l'inverse (tuile pleine, glyphe blanc) : ces icônes **sortent du site**, ce sont elles que WhatsApp et iOS montrent en vignette quand on partage le lien de la troupe, et le négatif d'un sceau ne se reconnaissait pas comme le sceau. Rien n'est perdu en petit : à 32 px les deux sens sont nets, à 16 px les deux sont molles de la même façon (les masques se touchent sous ~34 px, cf. la ligne « Logo »), donc le choix se joue en grand, là où vit la vignette. `theme-color`, lui, garde le `--page-mark` plein : il colore la barre du navigateur, pas une icône, et la couleur de page y reste la couleur de page : Safari ne lit ni les favicons SVG ni les `data:` URI, et iOS exige un PNG pour « Ajouter à l'écran d'accueil ». `apple-touch-icon.png` sert les deux accueils (les masques), les cinq autres pages ont le leur. **Dérivés des mêmes tracés que les favicons, pas redessinés** : on reprend le SVG du `<link rel="icon">`, on lui pose `width`/`height` à 180, on retire le `rx='6'` (iOS applique son propre masque arrondi) puis on rastérise et on aplatit en RGB sans alpha (iOS remplit le transparent en noir). Toucher un favicon oblige donc à régénérer son PNG. Les `href` s'écrivent absolus (`/apple-touch-icon.png`) : Vite les rend relatifs au build, `public/` étant recopié tel quel dans `dist/` |
| Cartes de dialogue (look commun Répétition/Enregistrement) | `.dialogue-card` dans `src/shared/theme.css`, qui porte aussi la palette rose/doré « mes répliques » (`.dialogue-card.mine`) et la bordure `.active` communes. Les pages posent `.mine` à côté de leur classe sémantique (`.muted` côté répétition, `.own` côté enregistrement) et ne gardent que leurs vrais écarts (`.hide-text`, `.fresh`, `.recording`…) ; côté enregistrement l'état des prises est porté par l'étiquette `.rec-status` orange/verte (palette en variables `--rec-*` locales à `recorder.css`). **Ces `--rec-*` foncés ne doivent PAS dériver de `--warn` / `--ok`**, qui portent pourtant la même sémantique « à faire / fait » sur l'Avancement : c'est tentant, ça a été proposé en revue, et c'est une régression mesurée. Ces étiquettes sont du texte sur la carte rose « mes répliques » (`--accent-soft` `#f6e8ea`), pas sur le blanc ni sur le crème où les tokens du thème sont taillés, et sur ce fond `--ok` tombe à 4,31:1 (échec AA) quand `--rec-fresh` tient 4,87:1. C'est la sémantique qui est partagée, pas la valeur : le fond n'est pas le même. La mesure est dans le commentaire de `recorder.css`, pour que la question ne se rouvre pas |
| Bandeau de pièce + barre de contrôle basse (communs Répétition/Enregistrement) | composants `src/shared/PlayHeader.jsx` (titre de la pièce, repliable, marque en pied) et `src/shared/ProgressBar.jsx` (slider indexé) ; CSS dans `theme.css` (`.play-header*`, `.controls`, `.ctrl-btn`, y compris `.ctrl-btn.my-jump` pour les sauts « ma réplique », communs aux deux pages…). Pas de tiret cadratin dans les headers. `PlayHeader` sert aussi à l'Édition, à l'Avancement et à la Répartition : les cinq pages qui connaissent la pièce affichent son titre en haut, au même endroit et dans la même serif. **Où vivent la marque et le lien vers l'accueil** : plus dans la rangée du haut. Celle-ci ne dit plus QUE le titre de la pièce (avec le sceau, qui dit la page) : le bouton de repli l'avale en entier, et le mot « PrettyDrama » est descendu en pied du bandeau, où il rejoint le lien. Il y prend exactement l'aspect qu'il avait en haut (`.play-header-home-word` : `--header-serif` 700, 20 px, `--header-accent`, `text-decoration: none` sur le lien pour qu'il ne ressemble jamais à un hyperlien souligné), il change juste de place. Motif : la rangée du haut est la seule ligne toujours visible, et sur mobile la marque y mangeait la largeur du titre de la pièce, qui se coupait en « … » ; le nom du site, lui, se lit très bien une ligne plus bas, là où il est aussi la destination du lien. Ce lien est donc le logo aux deux masques sur sa pastille **plus le mot à sa droite** (`src/shared/HomeLink.jsx`, un seul composant pour les deux bandeaux ; `.play-header-home`, `homeHref` donc `respo.html` depuis l'Édition et l'Avancement), **en pied du bandeau déplié, centré**. Toujours pas de « Retour à l'accueil » écrit (le logo et le nom du site disent la destination) : c'est l'`aria-label`/`title` « Accueil PrettyDrama » qui porte le verbe, et il contient bien le texte visible ; le sceau reste décoratif. Deux filets courts qui s'effacent vers l'extérieur l'encadrent (`.play-header-foot`) : seule et centrée, la pastille avait l'air tombée là ; encadrée, elle ferme le bandeau comme un cul-de-lampe ferme une page (dégradés et courts plutôt qu'une règle pleine d'un bord à l'autre, qui aurait pesé sous des réglages déjà chargés). Son survol reprend le **geste** de toutes les surfaces cliquables du site (cartes de l'accueil, bouton de dépôt) sans le copier à la lettre : il n'en garde que le pas d'un pixel vers le haut, et ce qui remplace son filet et son ombre est **le crème du sceau de la marque qui s'étend et englobe le bloc entier** (logo + mot, c'est un seul bouton) : le sable de la pastille déborde d'elle et enveloppe le nom du site, qui ne font plus qu'un objet posé sur un fond chaud. Il vit dans un `::before` : dégradé radial `closest-side` du crème vers le transparent, qui atteint la transparence AVANT le bord de son propre calque, lui-même plus grand que l'ovale (`inset: -14px -30px`), donc la nappe finit de s'éteindre dehors et plus rien ne se termine sur une ligne (les arrêts intermédiaires, quatre paliers, servent la même cause : deux arrêts seuls laissent une frange là où la pente est raide). Plein crème au centre, là où le vin devait rester sous les 20 % : le sable est assez proche du blanc du bandeau pour le supporter, et c'est la largeur et la pente douce, pas la pâleur, qui l'empêchent de se montrer. Un peu plus lent que le pas de l'ovale pour que le fond monte au lieu de s'allumer d'un coup. La couleur vient de `--page-mark-soft` via la classe **`page-home` posée sur le lien lui-même** (`PlayHeader.jsx`) : ni hex recopié dans `theme.css`, ni token de plus, et sur les pages du responsable la nappe reste crème au lieu de prendre le vert ou le violet du bandeau. `z-index: -1` la passe sous le logo et le mot sans la passer sous le bandeau (c'est le `.play-header` collant qui crée le contexte d'empilement, l'ovale n'en crée pas), `pointer-events: none` garde la cible du clic sur l'ovale seul, et le `border-radius: 999px` de l'ovale ne peint donc plus rien : il ne reste que pour la forme de la bague de focus. Trois essais gardés en commentaire dans `theme.css` parce qu'ils sont tentants : **le filet** à la couleur de marque des cartes (là-bas il existe déjà au repos, c'est la bordure de la carte que le survol teinte ; ici il apparaissait de nulle part et cernait les masques comme une cible, en plus de n'allumer que le rond et de laisser le mot éteint à côté), **le fond teinté + `box-shadow` flous** (un fond s'arrête net au bord de sa boîte, une ombre portée redessine la silhouette : dans les deux cas l'arête revenait), et **la même nappe en vin** (une lueur rouge autour du logo, une alerte plutôt qu'un accueil : ce qui doit s'étendre est le fond du sceau, pas son encre). Le `border: 1px solid transparent` qui le portait au repos est donc retiré aussi : sans lui la pastille fait ses 36 px pleins, exactement comme le sceau de la page (en `border-box`, le filet transparent en mangeait un). Le voile est un `color-mix` sur `--header-accent` et pas `--accent-soft` (couleur figée, qui ne suivrait pas la teinte de la marque) ni `--accent` (re-skinné en violet par l'éditeur, alors que le bandeau partagé doit rendre pareil sur les cinq pages) ; `--shadow-hover` et pas `--shadow` pour cette dernière raison, l'éditeur met le second à `none`. Deux pistes écartées : inverser la pastille (fond vin, dessin sable) noie les masques, qui peignent leurs deux visages en `--page-mark-soft`, et il ne reste qu'une tache pâle ; l'agrandir la redessine à une taille qu'elle ne supporte pas. Bague de focus de la même géométrie que `--focus-ring` mais bâtie sur `--header-accent`, la bague partagée dérivant de `--accent`, que l'éditeur re-skinne. Sur mobile le pouce vise le haut de la barre pour la replier et tombait sur la marque : le geste le plus courant de la page renvoyait au menu principal, en perdant le personnage choisi et, sur l'Enregistrement, les prises non exportées (le `LeaveGuard` prévenait, mais l'accident restait quotidien). Maintenant tout ce que le doigt atteint dans la rangée replie, et sortir de la page demande de déplier d'abord. En pied et pas en tête du bandeau : une sortie de page ne se met pas au-dessus des réglages de la page. Le sceau, lui, reste hors du bouton : son `role="img"`/`aria-label` brouillerait le nom accessible du bouton, et c'est la seule zone inerte de la rangée (elle l'a toujours été). **Corollaire : le bandeau se replie sur les cinq pages, y compris l'Avancement**, qui n'a pourtant aucun réglage ; une page qui ne se replierait pas serait la seule à garder son bandeau ouvert sous le pouce. Son bandeau à elle ne contient que sa doc (cf. la ligne « Phrases de doc » : à quoi sert la page, jamais comment lire la grille, ça c'est le rôle de `.dash-legend` sous le tableau) et le logo de retour : déplier pour ne trouver qu'un logo faisait vide. D'où aussi l'infobulle unique du bouton de repli, « Déplier ou replier le bandeau » et non « les réglages » : une formule par contenu se désaccorderait au premier ajout. Seul `PageHeader` (titre = libellé de page) reste pour les écrans qui n'ont pas encore le manifest, via `PageState` : même géométrie, et il porte le MÊME `HomeLink` en pied, sans quoi la marque sauterait du haut vers le bas du bandeau à l'arrivée du manifest |
| Réglages de la Répétition (les 4 cases) | `.checks-row` (`src/rehearsal/rehearsal.css`), rangée `flex-wrap` à côté du select de personnage : « Couper ma voix » (mes répliques ne sont pas jouées, je les dis), « Cacher mon texte » (les miennes sont floutées), « Bip avant ma réplique », « Démarrer une réplique avant la mienne » (les flèches « ma réplique » s'arrêtent sur la réplique qui me lance, pas sur la mienne). Les libellés disent ce que la case fait : « Muet », « Bip » et « Avant » ne se comprenaient qu'une fois essayés. **La quatrième ne se raccourcit pas en « Démarrer avant ma réplique »** : « avant » y désignerait le rang de la réplique où l'on démarre, alors qu'il désigne un instant dans le libellé du bip juste au-dessus ; deux sens du même mot dans deux cases voisines, dont l'une est celle qui décale les sauts, on la relit deux fois. La longueur est le prix assumé, et ces quatre libellés sont ce qui remplace la phrase de doc du bandeau, que la Répétition n'a plus. **Le repli reste en `flex-wrap`, jamais un nombre de colonnes fixé** (la grille 2x2 a été essayée et retirée) : une largeur de colonne imposée aux quatre les aligne sur le plus long, donc sur téléphone les quatre cases passent à deux lignes chacune alors que les deux courtes tiennent côte à côte. **Sous 800 px, c'est la hauteur des étiquettes (32 px) qui espace les lignes, et le `row-gap` est à zéro** : les deux s'additionnaient, et la zone tactile de 40 px du premier essai laissait 19 px de vide entre deux lignes, plus que les 18 px qui séparent deux cases côte à côte, exactement au moment où la fenêtre devient trop étroite pour tenir les quatre sur une ligne. Un seul chiffre à régler, donc, et la recommandation des 44 px se rattrape en largeur (l'étiquette entière est cliquable). Le `margin-block: -5px` du même bloc va avec : cette hauteur de cible ne doit espacer que les cases entre elles, alors qu'au bord du bloc elle s'ajoutait au `row-gap` de `.character-row` et éloignait les cases du select « Qui jouez-vous ? » (et du pied du bandeau) au passage sous 800 px. La boîte extérieure épouse donc le texte, l'intérieur reste haut. L'infobulle de chaque case reste, elle dit le pourquoi ; le libellé dit le quoi. Le style des cases elles-mêmes (l'accent de la page plutôt que le bleu du système) a quitté ce fichier pour `theme.css`, en sélecteur d'élément : la Recherche de l'Édition en a deux à son tour, et les deux pages les veulent identiques (précédent nommé, `.confirm-quote`). Ce qui reste ici est la zone de clic de 32 px sous 800 px, justifiée par le pouce, que l'Édition n'a pas. La RANGÉE, elle, est restée locale aussi : la Répartition a un temps eu deux cases sous ses selects, elle n'en a plus (sa portée tient dans ses deux selects et son échelle est un curseur, cf. « Page Répartition »), donc cette classe n'a toujours qu'un consommateur |
| Phrases de doc (cartes d'accueil + bandeaux) | **Deux éléments par mode, pas plus, et un seul endroit pour le premier.** (1) `PAGES[page].desc` (`src/shared/pages.js`) : la phrase compacte, servie à la fois par la carte de l'accueil et par le bandeau, que **`PlayHeader` rend lui-même** (`.header-hint`) sur les cinq pages ; une carte qui promet une chose et un bandeau qui en dit une autre décrivaient deux pages différentes, et la même phrase recopiée dans deux fichiers finit par dériver. (2) le `hint` que chaque page passe à `PlayHeader` (même `.header-hint`, une seule classe : les deux paragraphes ont le même style, c'est leur place qui les distingue) : les précisions qui n'auraient aucun sens sur une carte, quand on choisit encore où aller. Les pages n'écrivent donc plus de `.header-hint` elles-mêmes. **Les deux paragraphes encadrent les réglages** : la phrase compacte ouvre le bandeau déplié, juste sous le titre de la pièce et avant le choix d'acte/scène (à quoi sert la page se lit avant qu'on y touche, et c'est le même endroit sur les cinq pages ; sur l'Édition, l'Avancement et la Répartition, dont le bandeau n'a aucun réglage, elle ouvre donc une zone dépliée qui ne contient plus qu'elle, le `hint` et le retour à l'accueil), le `hint` ferme le bandeau sous les réglages (on le relit une fois, il ne doit pas éloigner les réglages du titre). Forme commune aux deux, à tenir : verbe d'action à l'impératif en tête, une dizaine de mots, deux phrases au maximum pour un `hint`, pas de question posée au lecteur (« Qui a enregistré quoi ? » a sauté), les deux-points seulement pour énumérer. **Quand un `hint` porte deux phrases, elles suivent l'ordre du travail** et pas l'ordre d'importance : ce qui sert pendant la saisie d'abord, ce qui sert une fois qu'on a fini ensuite. C'est ce qui fait de l'Édition le seul `hint` du site à ne pas ouvrir sur un impératif (« Dans une réplique, Entrée crée la suivante » précède « Une fois vos modifications terminées, téléchargez le script ») : annoncer la sortie de la page avant même d'y entrer coupait en deux le parcours du fichier, et l'impératif n'est pas perdu, il arrive une phrase plus tard. Les deux phrases y sont séparées par un `<br />`, et surtout pas par un troisième paragraphe (le bandeau n'en porte que deux) : la ligne casse là où le travail change de nature, pendant la saisie puis une fois qu'elle est finie, au lieu de casser à la largeur de la fenêtre. **Le `hint` est facultatif, et seules deux pages en ont** : l'Enregistrement (où appuyer pour enregistrer, quand télécharger) et l'Édition (la touche Entrée, puis où va le script téléchargé). La Répétition n'en a pas, ses quatre cases à cocher disent elles-mêmes ce qu'elles font (cf. « Réglages de la Répétition ») ; l'Avancement non plus, la carte de dépôt est juste sous le bandeau et se lit seule. Une phrase qui redit ce que l'écran montre ne fait que repousser les réglages. Corollaires : le libellé exact des boutons ne se recopie plus dans la phrase (« par le même bouton que les voix des acteurs » plutôt que le nom du bouton en gras plus `script.json` en `code`, d'où la disparition de `.editor-header-hint code`, et celle d'un `.header-hint-more` que rien ne stylait), et la troupe dit « le responsable » et jamais « votre responsable » (possessif de hiérarchie). L'éditeur est la seule page dont la doc porte un lien vers une autre page, et ce lien **n'est pas un mot souligné** : c'est le sceau de la page visée plus son nom (`.hint-page-link` / `.hint-page-mark` dans `editor.css`, classe `page-dashboard` posée sur le lien pour ses couleurs), même forme que le retour à l'accueil en pied de bandeau, et le site nomme ses pages par leur pastille partout ailleurs. Un hyperlien classique y était la seule chose soulignée du bandeau. **Ctrl+F n'est écrit nulle part dans le bandeau** : le raccourci vit dans l'infobulle de l'icône loupe du rail, comme Ctrl+Z et Ctrl+Y vivent dans celles de leurs boutons. Entrée et Maj + Entrée ne sont dans le `hint` que parce qu'un textarea n'a pas d'infobulle pour les porter ; un bouton, si. Et `PAGES.editor.desc` ne bouge pas pour autant : il énumère ce que la page CONTIENT, pas ses outils (ni le téléchargement ni la pile d'annulation n'y figurent), et il est partagé mot pour mot avec la carte de l'accueil, où « cherchez et remplacez » promettrait un outil à qui choisit encore une page. La pastille est la plus petite du site (19 px dans du 13.5 px) et se préfixe `.play-header` pour échapper à la règle mobile qui rapetisse les sceaux du bandeau, sans quoi elle GRANDIRAIT à 30 px sous 800 px. Deux réglages d'alignement mesurés au pixel, à ne pas « simplifier » : le lien est `inline` et pas `inline-flex`, pour que le mot s'asseye sur la ligne de base du paragraphe comme n'importe quel autre mot (une boîte flex n'expose pas la ligne de base de son texte, il fallait la recentrer à la main et elle retombait un pixel trop bas) ; et le sceau se cale sur la bande des capitales, pas sur la hauteur d'x, via `vertical-align: middle` PLUS `margin-bottom: 3px` (`middle` centre la boîte de marge, donc 3 px en bas remontent la pastille de 1,5 px). Une longueur en `vertical-align` aurait été plus directe mais elle part de la ligne de base du sceau, et un `inline-flex` dont l'unique enfant est un SVG n'en a pas de fiable : Chrome la synthétise, la spec parle du bord bas de la marge, soit 4 px d'écart possible d'un navigateur à l'autre. Le remplissage vertical du lien (3 px) ne sert qu'à ce que la nappe de survol couvre le sceau, qui dépasse de la boîte de contenu du texte ; le remplissage droit sert la nappe et pas l'espacement, d'où la marge négative qui l'annule (sans elle le mot suivant se lisait à deux espaces du lien) |
| Repli animé du bandeau | les réglages restent **montés** (sinon rien à animer) dans un conteneur grille dont l'unique ligne passe de la hauteur du contenu à zéro : c'est interpolable, contrairement à `height: auto`. Deux pièges déjà payés, à ne pas réintroduire : la piste s'écrit `minmax(0, 1fr)` / `minmax(0, 0fr)` et **jamais** `1fr` / `0fr` (le minimum d'une piste `fr` vaut `auto`, donc la contribution min-content du contenu : la piste « à zéro » restait haute du padding bas des réglages, soit une ligne vide sous le titre), et l'enfant `.play-header-settings-inner` garde `min-height: 0` (sans quoi le minimum automatique d'un élément de grille le plafonnerait à sa hauteur de contenu, et rien ne se replierait). `visibility: hidden` (propriété discrète, donc appliquée en fin de fermeture et au début de l'ouverture) sort les selects du parcours clavier sans les démonter. Le bouton de repli prend `align-self: stretch` : sans lui il ne mesurait que les 28 px de son titre dans une rangée haute de 36 px, et `align-items: center` laissait quatre pixels morts de part et d'autre, en haut de la barre, exactement là où le pouce vise pour replier (rien ne bouge à l'écran, le fond est transparent et le contenu reste centré). Le repli n'enlève QUE les réglages : la rangée du haut ne bouge pas d'un pixel (ni padding, ni taille du sceau, ni corps du titre), et le titre reste donc centré dans une barre au padding symétrique. Le rognage est posé sur la grille et **pas** sur son enfant : en `border-box`, un enfant de hauteur 0 garde ses 14 px de padding bas, qui débordaient sous le bandeau fermé et l'épaississaient. Il est actif **en permanence**, et ça n'a pas toujours été le cas : une classe `animating`, posée par le composant et retirée par un minuteur de 340 ms, le levait dès que le bandeau était ouvert et immobile, pour une seule et unique raison, le popover de couleur d'une puce de personnage, qui devait pouvoir dépasser du bandeau de l'éditeur. Les puces vivent maintenant dans le rail de l'Édition et leur palette se pose dans son flux (cf. « Panneau latéral de l'Édition »), donc plus aucune des cinq pages n'a de contenu qui dépasse de ses réglages, et cinq pages n'ont plus à faire tourner un minuteur à chaque repli pour personne. La doctrine qui allait avec n'a plus d'objet mais reste notée dans `theme.css`, au cas où un futur contenu débordant ferait revenir la question : c'était un minuteur et pas un `transitionend`, parce qu'à mouvement réduit l'événement ne vient jamais et le bandeau resterait rogné. Vérifié après coup : le remplissage de `.play-header-settings-inner` laisse 14 px sous le dernier contrôle de chacune des cinq pages, donc aucune bague de focus n'est rognée. Durées et `--ease-header` dans `theme.css`, neutralisées par le bloc `prefers-reduced-motion` en fin de fichier |
| Page Enregistrement | structurée comme la Répétition : sélecteurs acte/scène/personnage, navigation contrainte à SES répliques, micro central (SVG) dans la barre basse, bouton Télécharger à droite. Les prises survivent au changement de personnage (ZIP multi-voix : `voix-<noms>.zip`, `voices-<noms>.zip` en anglais, cf. `recorder.zipName` ; le type du dépôt venant de la seule extension, ce nom n'est pas un contrat). Tant qu'aucun personnage n'est choisi : `IntroCard` à la place de la liste de répliques (« Qui jouez-vous ? » + mode d'emploi + un bouton par personnage avec son reste à enregistrer), barre basse masquée et `hint` du bandeau à `null` (la phrase compacte, elle, reste : c'est le `desc` partagé avec la carte d'accueil), select du bandeau en accent (`.character-select.unset`) |
| Statut des répliques (Enregistrement) | 3 états par réplique, étiquette au coin de la carte + légende en tête de liste : `todo` « À enregistrer » (point ambre), `fresh` « À télécharger » (prise de la séance, halo vert + lecteur vert vif ; reste fresh MÊME après téléchargement du ZIP : « Déjà enregistrée » n'est vrai qu'une fois le ZIP intégré par le respo et le site republié), `done` « Déjà enregistrée » (clip publié à jour uniquement, lecteur vert grisé). Le téléchargement ne touche pas aux statuts, il ne pilote que la note « pas sauvegardé »/« téléchargé » ; les flèches de la barre basse parcourent TOUTES mes répliques et portent donc le design des sauts « ma réplique » de la Répétition (`.ctrl-btn.my-jump`, icônes `SkipPrev`/`SkipNext`) ; l'avertissement « pas sauvegardé » vit dans le bandeau du HAUT (jamais dans la barre basse) ; lecteur intégré à la carte (`TakePlayer` : play rond, durée avec workaround Infinity des blobs MediaRecorder, onde décorative déterministe) |
| Jeter une prise (Enregistrement) | bouton corbeille en bout de rangée du lecteur (`.player-delete`, `TrashIcon`), au trait rouge du thème sur une pastille `--accent-soft`, plein au survol : il côtoie le bouton d'écoute (vert, en aplat), où un dessin gris se perdait alors que c'est le seul geste irréversible de la page, **seulement sur une prise de la séance** (`onDelete` n'est passé que si `takes[line.id]` existe) : un clip déjà publié se réécoute mais ne se supprime pas depuis le navigateur, il vit dans le dépôt (le supprimer demanderait un canal de dépôt « retirer ce clip », que rien ne porte aujourd'hui). Désactivé pendant un enregistrement, comme les autres commandes. `deleteTake` révoque l'URL du blob, retire la clé, et **repose `downloaded` à faux** exactement comme refaire une prise : le ZIP déjà téléchargé contient encore la prise jetée, donc il est à refaire. La réplique reprend l'état qu'elle avait avant, `done` ou `todo`, sans code dédié (`lineState` ne fait que retomber sur `line.status`) |
| Couleurs des personnages | **`src/shared/characterColors.js`** (partagé, testé), et plus l'éditeur : deux pages les lisent depuis que la Répartition en colore ses camemberts et son bloc. Couleur **stockée** sur le personnage (`{id, name, color: "#1f77b4"}` dans script.json), et non plus une teinte : la palette est **Tableau 10** (`tab10` de matplotlib, `schemeCategory10` de D3), qu'aucune teinte ne peut indexer (`#ff7f0e` et `#8c564b` sont deux registres du même angle, `#7f7f7f` n'en a pas). Elle vient de la visualisation d'origine de la troupe, qui employait `sns.color_palette("bright", 10)` : les six variantes de seaborn sont le MÊME jeu de dix emplacements dans le même ordre (bleu, orange, vert, rouge, violet, brun, rose, gris, olive, cyan) à la saturation et à la clarté près, et on garde le registre canonique parce qu'il sépare mieux ses propres couleurs que `bright` (ΔE minimal 27.7 contre 24.8) sans descendre son jaune à 1.48:1 sur le crème. Les dix suivantes (slots 11 à 20) sont les dix teintes CLAIRES de `tab20`, dont les entrées paires sont `tab10` à l'identique : une troupe jusqu'à dix personnages ne voit donc que le registre canonique, au-delà l'ensemble faiblit (ΔE 16.6), au-delà de vingt il boucle. **La palette précédente était `oklch(0.58 0.14 H)`, à clarté fixe** : ses douze teintes ne différaient que par la teinte, soit 6 points de L\* et un ΔE minimal de 15.9, et sur un bloc d'un carré par mot deux voisines se confondaient. C'est la clarté qui distingue, pas la teinte. **Aplats pour les surfaces, encre pour le texte** : `characterInk` plafonne la clarté (`oklch(from … min(l, 0.5) c h)`) et met les vingt au-dessus de 5:1 sur les deux fonds du site. Un `color-mix(… 60%, #000)` a été essayé et écarté : il multiplie la clarté ET la chroma, donc il éteint la couleur en même temps qu'il la fonce (le bleu tombait à une chroma de 0.074 et le nom du personnage se lisait comme du noir dans l'éditeur), alors que le plafond garde la chroma d'origine et donne un MEILLEUR contraste. Auto = 1re libre ; modifiable via une palette de vingt pastilles en **deux rangées de dix** (`.swatch-popover`, la rangée foncée puis la claire, soit la structure de `tab20`), qui se pose **dans le flux** du panneau du rail, sous le personnage, et pousse les suivants (elle pendait sous sa puce du temps du bandeau : un panneau qui défile rognerait une boîte accrochée) ; les pastilles prennent la taille de leur case et ne l'imposent pas, la largeur du rail étant réglable de 200 à 560 px. Couleur absente, étrangère ou dupliquée réparée au chargement, et **le comblement n'a qu'une implémentation** : `build_manifest.py` recopie la couleur sans jamais la réparer (il ne valide que la forme `#rrggbb`, un contrat tenu par `test_contracts.py`), et le front comble avec `assignColors`, donc l'Édition et la Répartition montrent les mêmes couleurs même sur un `script.json` qui n'en porte aucune, ce qui est le cas du fichier publié |
| Design « Rail » de l'éditeur | tokens re-skinnés dans `src/editor/editor.css` (`:root` local à la page) : même fond crème `--paper` que les autres pages, filets/neutres réchauffés assortis, accent `#7a5cc0`, fonts IBM Plex Sans/Mono + Spectral (chargées par `editor.html`) ; les autres pages gardent le thème chaud partagé. Le re-skin ne fuit jamais dans le bandeau partagé : PageHeader/PlayHeader consomment les tokens réservés `--header-accent`/`--header-serif` (theme.css), qu'aucune page ne redéfinit (Cormorant 700 chargée aussi par `editor.html`). Les répliques de la scène vivent dans UN panneau blanc (`.line-list` : card blanche arrondie sur le crème, lignes fines à filet 1px dedans) pour le même contraste blanc-sur-crème que les cartes des autres modes. La direction s'appelait « Rail » depuis le début et la page n'en avait pas ; elle en a un (cf. « Panneau latéral de l'Édition »), et `--ed-sidebar` est devenu le fond de la barre latérale que son nom annonçait, au lieu d'être celui des puces de personnage dans un bandeau blanc |
| Édition réservée à l'ordinateur | `src/editor/useTouchPointer.js` : sur un pointeur tactile (`(pointer: coarse)`), l'éditeur ne se rend pas du tout, `App` renvoie un `PageState` d'une seule phrase (« Pour des raisons de praticité, le mode Édition n'est disponible que depuis un ordinateur. »), dans la carte `.page-notice` partagée par tous les états pleine page, sans la classe `.load-error` que le script illisible ajoute par-dessus pour élargir la carte et aligner à gauche ses paragraphes de marche à suivre. L'éditeur y redonne une ombre (`--ed-panel-shadow`), puisque son `:root` local met `--shadow` à `none` : cette carte est le contenu de l'écran, comme son panneau de répliques. Rien d'autre dans le message : ni la liste des gestes qui manquent (glisser une réplique, saisir un long texte), ni les liens vers les trois pages qui marchent au doigt, essayés puis retirés (le retour à l'accueil du bandeau suffit à sortir). C'est la SEULE page ainsi murée, parce que c'est la seule qui demande de glisser des répliques et de saisir de longs textes. **Le critère est le pointeur, pas la largeur** : un téléphone en paysage fait 844 px (il passerait un seuil de largeur) et reste inutilisable, alors qu'une fenêtre d'ordinateur rétrécie garde souris et clavier. Corollaire : ça ne se teste pas en réduisant une fenêtre, il faut l'émulation d'appareil des outils de développement. La requête est **écoutée** (`change`), pas seulement lue au montage, sinon un hybride qui rattache son clavier resterait bloqué jusqu'au rechargement ; sans `matchMedia` on ne bloque pas. Le `fetch` du script a lieu **même quand la page est murée**, et son seul emploi est le bandeau : cet écran nomme la pièce comme les quatre autres bandeaux du site (`title={script.title || "Pièce sans titre"}`), il ne dit pas « Édition » en toutes lettres. Sauter la requête était une économie de rien (un JSON sur une page qui n'affiche qu'une phrase) payée par la seule rangée du haut du site à porter un libellé de page. D'où l'ordre des sorties de `App` : le chargement d'abord (le titre n'arrive qu'avec le script, et cet écran d'attente ne nomme donc rien : `PageState` est appelé sans `title`, et `PageHeader` ne pose pas le `<span>`), le mur ensuite, l'erreur de lecture après lui (un script illisible n'apprend rien à qui ne peut pas éditer). La carte Édition de l'accueil respo n'est **pas** masquée sur mobile : une carte qui disparaît se lit comme une page perdue, un écran qui s'explique ne se lit qu'une fois. Conséquence dans `editor.css` : ses règles tactiles ont été retirées (micro-boutons portés à 40 px, zone de clic de la pastille de couleur étendue par un `::after`), plus aucun doigt ne les atteignait et elles épaississaient les rangées là où la place manque ; sa section « Mobile » est devenue « Fenêtre étroite », et il n'y reste qu'un repli, celui des `.line-row` à 620 px, qui ne se justifie plus que par la largeur de saisie, jamais par le doigt. La règle du popover de couleur (barre basse sous 800 px) a disparu avec son motif, le débordement, la palette étant maintenant dans le flux du rail. Et ce seuil de 620 px n'est plus une requête de **fenêtre** mais de **conteneur** (`container: ed-column`) : depuis que le rail prend sa largeur sur la colonne, la fenêtre ne dit plus rien de la place qui reste pour taper (rail ouvert, une fenêtre de 900 px ne laisse que 580 px de colonne et l'ancienne règle ne se déclenchait pas ; rail replié, le seuil arrive vers 668 px de fenêtre, soit les 48 px de la bande d'icônes). Contrepartie à connaître : `container-type` implique le confinement de mise en page, donc la colonne devient le bloc conteneur des descendants en `position: fixed` ; ne rien y poser de `fixed` (même piège que le transform dnd-kit, qui a valu son portail à `ConfirmModal`) |
| Navigation d'édition | une scène à la fois, désignée dans la section « Structure » du rail (`StructurePanel.jsx`) et **plus dans le bandeau partagé**, qui n'a donc plus de réglages du tout, comme celui de l'Avancement : les deux selects acte/scène, les boutons « + Scène »/« + Acte » et le champ de titre de la pièce sont partis là-bas, avec ce qui façonne la structure (ajouter, supprimer, réordonner). Même motif que le déménagement des puces de personnage, et il se rejoue au niveau du dessus : sur la Répétition et l'Enregistrement on CHOISIT une scène dans un contenu figé, dans l'Édition on la FAÇONNE, et deux listes déroulantes n'affichent qu'une ligne à la fois là où le plan doit montrer la forme de la pièce. La structure était de surcroît éparpillée sur trois endroits (les selects et les deux boutons dans le bandeau, la suppression de l'acte dans la colonne, celle de la scène dans `SceneEditor`) ; `EditableTitle.jsx` a disparu avec ce déménagement, le titre de la pièce étant maintenant un champ en clair et en permanence dans le plan (les actes et les scènes, eux, ne se renomment plus du tout, cf. la ligne « Plan de la pièce »). La colonne de texte ne fait plus que les AFFICHER, en tête de la scène qu'on écrit. À la place du select personnage des autres pages, la **gestion** des personnages, qui ne vit plus dans le bandeau non plus mais dans la section « Personnages » du rail (`CharacterPanel` dans `src/editor/CharacterPanel.jsx` : une puce par personnage, pastille de couleur, renommage en place, compte de répliques, suppression, plus le formulaire d'ajout). Motif : le bandeau est **partagé par cinq pages** et n'a de place que pour ce que les cinq ont en commun, le titre de la pièce et le choix de la scène ; les puces y étaient le seul bloc propre à une page de cette taille, elles poussaient les deux paragraphes de doc et le retour à l'accueil vers le bas, et elles grandissaient avec la distribution, donc la barre d'une pièce à quinze personnages n'avait plus de rapport avec celle d'une pièce à trois. Dans une colonne, la même liste tient à la verticale sans faire bouger le haut de la page, les comptes de répliques s'alignent enfin (une distribution qui se lit sans compter) et elle reste sous les yeux pendant qu'on attribue les répliques, ce que le bandeau n'offrait qu'au prix de son repli. Le compte de répliques d'un personnage, d'un acte ou d'une scène passe par un seul composant, `src/editor/CountBadge.jsx` (le chiffre SEUL à l'écran pour que la colonne s'aligne, la phrase dans l'`aria-label`, `role="img"` pour le rendre valable sur un `<span>`). Le chiffre y passe par `fmt.number`, le formateur des nombres écrits hors de toute phrase : l'`aria-label` est une phrase, donc le moteur y groupe déjà le nombre, et sans ça le même compte se lisait « 1144 » et s'annonçait « 1 144 répliques » : les deux panneaux du rail en avaient chacun leur copie, alors que leur CSS était déjà commun. C'est une vraie `<ul>` (un lecteur d'écran annonce « liste de 7 éléments »), et trois choses tombent avec ce déménagement : la palette de couleurs passe dans le flux du panneau, la machinerie `animating` du bandeau partagé n'a plus d'objet, et `--ed-sidebar` devient enfin le fond de la barre latérale que son nom annonce depuis le début, les puces passant au blanc comme tout ce qui porte du contenu sur ce site. Ne reste donc dans le bandeau que ce que les cinq pages ont en commun (titre de la pièce en serif, doc, retour à l'accueil), plus le téléchargement dans ses `actions`, **en icône seule** (`.btn.script-download-btn`, nom qualifié : l'Enregistrement a son propre bouton de téléchargement, `.zip-download-btn`, aux règles contraires), comme la paire annuler/rétablir qu'il côtoie : son libellé écrit était le seul texte de la rangée toujours visible à concurrencer le titre de la pièce sur une fenêtre étroite. Le verbe vit dans l'`aria-label`/`title` « Télécharger le script », et la phrase de doc du bandeau dit quoi faire du fichier. Il reste l'action principale, donc son dessin est plus grand (19 px contre 17), mais sa HAUTEUR est calée sur celle des boutons d'historique (31 px de part et d'autre) : trois boutons voisins dont un dépasse de deux pixels se lisent comme un défaut d'alignement, pas comme une hiérarchie ; c'est la largeur et l'accent qui la disent. Il est **désactivé quand `dirty` est faux**, comme les deux boutons d'historique le sont quand la pile est vide : redéposer le script tel qu'il est déjà publié n'apprend rien à l'Action, et `dirty` étant un comparatif d'états et pas un drapeau, annuler jusqu'au dernier téléchargement l'éteint aussi. Seule l'infobulle change (« Aucune modification à télécharger pour l'instant »), jamais l'`aria-label` : le nom d'un bouton ne dépend pas de son état. **Cette infobulle est portée par une enveloppe `.btn-tip` et jamais par le bouton** : un contrôle `disabled` ne reçoit aucun événement souris, donc son propre `title` ne s'affiche pas (Chrome, Safari) et l'explication n'arrivait jamais au moment où elle sert ; les trois boutons de la rangée suivent la même règle. Corollaire connu : un `script.json` réparé au chargement par `sanitizeScript` n'est pas téléchargeable tant qu'on n'y touche pas (la pièce affichée diffère alors du fichier du dépôt) ; la moindre édition rouvre la porte |
| Plan de la pièce (section « Structure » du rail) | `src/editor/StructurePanel.jsx` : le titre de la pièce, sa langue, ses actes, ses scènes ; à la fois la NAVIGATION de l'éditeur et le seul endroit où la structure se façonne (ajouter, supprimer, réordonner). **Seule la PIÈCE porte un nom.** Un acte et une scène ne se renomment pas du tout : leur libellé est DÉRIVÉ de leur rang (« Acte I », « Scène 3 », `src/shared/structureLabels.js`), donc ce panneau n'a plus qu'un champ de texte, le titre de la pièce, plus le choix de sa langue. Le renommage a existé ici, en champs en clair et en permanence (`NameField`, disparu), et l'abandonner a DISSOUT un problème au lieu de le résoudre : un titre stocké est une donnée dans une langue, et il voyageait, vers `manifest.json`, vers le PDF, vers les colonnes de l'Avancement, vers la portée de la Répartition, sans que rien puisse le traduire ni ait à le faire, la vraie pièce n'ayant jamais usé de cette liberté (dix scènes, toutes nommées « Scène N »). Un libellé dérivé redevient du texte d'interface ordinaire, dans la langue du lecteur. Ce que ça coûte, et c'est tout : un acte ne peut plus s'appeler « Prologue » ; si ça revient un jour, c'est un champ FACULTATIF à rajouter, pas une refonte. Corollaire de tuyauterie : c'est le PYTHON qui compose les mêmes libellés pour le papier (`STRUCTURE` et `roman_numeral` dans `build_script_pdf.py`), depuis la langue de la PIÈCE et non celle du lecteur (à l'écran un libellé d'acte est de la navigation, sur le papier c'est le document ; le chiffre étant le même des deux côtés, personne ne perd sa place), et `TestStructureLabels` (`test_contracts.py`) tient les deux implémentations en accord, chiffres romains compris. **Une scène reste la seule chose du plan où l'on NAVIGUE, donc c'est un bouton** : c'était un champ, et le chemin clavier vers l'ouverture venait gratuitement avec (la tabulation atteignait le champ, et le focaliser ouvrait la scène) ; sans plus rien à taper, un libellé statique aurait supprimé ce chemin, d'où un vrai `<button>`, atteint par tabulation et activé par Entrée ou Espace. Le nom d'un ACTE, lui, ne mène nulle part (ses scènes sont listées juste dessous, et c'est là qu'on choisit), donc il reste du texte nu. La rangée entière est cliquable par commodité souris ; le ✕ y fait `stopPropagation`, supprimer une scène n'étant pas une façon d'y aller. Quatre choses à ne pas défaire. (1) **Une scène ne change jamais d'acte** (`MOVE_SCENE` est borné à son acte) : c'est ce qui garde l'action à un seul indice de conteneur, un glissement d'un acte à l'autre demandant de choisir en plus où elle atterrit. (2) Le filtrage des cibles de dépôt est fait **à la détection de collision** (`collision`, qui ne garde que les conteneurs du bon préfixe) et pas seulement en garde à l'arrivée : les deux `SortableContext` imbriqués vivent dans un seul `DndContext`, donc sans lui `closestCenter` désignait la scène d'un autre acte et le glissement se figeait sans rien expliquer. (3) Les actes et les scènes n'ayant pas d'id, leur identité dnd-kit et leurs clés React sont des **rangs** préfixés par le type (`act:2`, `scene:2:0`) ; c'est aussi ce qu'attendent `MOVE_ACT`/`MOVE_SCENE`. (4) Le regard suit l'objet déplacé, via `indexAfterMove` et `indexAfterRemoval` (`reducer.js`, testés à côté de `MOVE_*`) : ces deux fonctions décrivent exactement la permutation que les actions appliquent, les faire diverger est le bug qu'elles évitent (glisser l'acte III au-dessus de l'acte I pendant qu'on y travaille faisait sauter la colonne sur un autre acte). Corollaire dans `history.js` : **toute clé de fusion identifie son objet par une valeur STABLE**, un id de réplique ou rien du tout, et plus jamais par un rang. Les renommages d'acte et de scène étaient les seuls à se cléer sur un rang (`RENAME_ACT:0`), ce qui ne tenait que parce que tout ce qui déplace un rang a une clé nulle et fermait donc la rafale au passage ; ils n'existent plus, donc cette précaution n'a plus d'objet et il ne faut pas la faire revenir sans elle. Deux objets différents ne peuvent pas se retrouver dans la même étape d'annulation, et c'est testé (`_coalesceKeyForTests`) |
| Annuler / rétablir (éditeur) | `src/editor/history.js` : pile d'états qui **enveloppe** `scriptReducer` (qui reste pur et inchangé), `{present, past, future, lastKey, saved}`, `past` plafonné à 100 entrées (les états sont structurellement partagés, une entrée coûte quelques objets). Toute nouvelle édition vide `future` (la timeline fourche). Les frappes clavier (`EDIT_TEXT` d'une même réplique, `SET_TITLE`) sont fusionnées en une seule étape, close par toute autre action, par un undo/redo ou par `HISTORY_BREAK` (dispatché au blur du textarea et du titre) ; `LOAD_SCRIPT` réinitialise la pile ; une action refusée par le reducer n'empile rien. Le remplacement de la recherche (`SET_LINE_TEXTS`, cf. « Recherche et remplacement ») ne fusionne avec rien : c'est **une** étape, qu'il touche une réplique ou cent, et c'est pour ça qu'il n'emprunte pas `EDIT_TEXT`, dont la clé de fusion laisserait la rafale de frappes ouverte sur la réplique réécrite (un seul Ctrl+Z aurait alors annulé la frappe suivante avec le remplacement). Paire de boutons icône `.history-group` dans les `actions` du bandeau + raccourcis Ctrl+Z / Ctrl+Y (et Cmd+Maj+Z) globaux, interceptés même dans un textarea (l'undo natif du navigateur désynchroniserait la pile) sauf quand il n'y a rien à annuler/rétablir |
| Confirmations de suppression (éditeur) | jamais de `window.confirm` (dialogue natif hors thème) : `src/shared/ConfirmModal.jsx` (partagé depuis que l'enregistrement s'en sert aussi, via `LeaveGuard`), même habillage `.modal-backdrop`/`.modal` que le modal de suppression de personnage, rendu en **portail** dans `document.body` (une `.line-row` peut porter un transform dnd-kit, qui deviendrait le bloc conteneur du backdrop `fixed`). Escape annule, le bouton destructif prend le focus (sauf quand un `primaryLabel`/`onPrimary` optionnel offre une sortie sûre : c'est elle qui le prend). Utilisé pour réplique (`LineRow`), scène (`SceneEditor`), acte et sortie de page (`App`), le remplacement global (`SearchPanel`), et hors éditeur pour la prise d'enregistrement (`TakePlayer`) ; supprimer un élément VIDE ne demande rien. Le remplacement global est le seul de la liste qui ne détruise rien d'irréversible (il s'annule en une étape) et il se confirme quand même, pour la raison qui fait confirmer une suppression d'acte : **ce qu'il touche n'est pas à l'écran**, et c'est le nombre qui surprend. La citation de la réplique visée (`.confirm-quote`, coupée par `excerpt` de `data.js`) est partagée par les deux pages qui citent : elle vit dans `theme.css`, plus dans `editor.css`, et son filet suit `--border` (donc la teinte de la page) |
| « Modifications non téléchargées » (éditeur) | dérivé, jamais un drapeau : `dirty = present !== saved` où `saved` est l'état de la pile d'annulation correspondant au dernier `script.json` téléchargé (le script chargé au départ). Annuler jusqu'à cet état fait donc disparaître l'étiquette, y compris quand la pile n'est pas vide (édition, téléchargement, édition, annulation). Le téléchargement dispatche `MARK_SAVED` ; l'identité des objets suffit, la pile restitue les états qu'elle a stockés |
| Sortie de page avec du travail non téléchargé | **un seul composant pour les deux pages concernées** : `src/shared/LeaveGuard.jsx` (`active`, `title`, `saveLabel`, `onSave`, message en children), monté par l'éditeur (`active={dirty}`, `onSave={download}`) et par l'enregistrement (`active={hasUnexported}`, `onSave={downloadZip}`, awaité car async). Il porte les deux couches : (1) clics interceptés en phase capture sur `document`, tout `a[href]` qui sort de la page ouvre un `ConfirmModal` du thème (« Télécharger … puis quitter » / « Quitter quand même » / Annuler) ; écoute globale plutôt que lien par lien, donc les futurs liens sont couverts d'office ; clic modifié, `target="_blank"`, `download` et ancres internes passent ; (2) `setBeforeUnloadGuard` (seul appelant restant) comme filet pour le rechargement, la barre d'adresse et la fermeture d'onglet : là le navigateur n'autorise QUE son dialogue (message et style imposés depuis Chrome 51 / Firefox 44), rien à habiller, ne pas réessayer. Détails à ne pas défaire : `setBeforeUnloadGuard(false)` à la main avant de naviguer (sinon le dialogue natif se superpose au modal), 200 ms de délai après le téléchargement (décharger la page dans la même tâche l'annule), et le modal reste affiché quand `active` retombe. **Aucune persistance locale du travail** (localStorage) : décision produit, un brouillon oublié dans un navigateur redeviendrait une source de vérité périmée face au dépôt |
| Enregistrement micro | `src/recorder/useRecorder.js` (MediaRecorder, stream réutilisé, `release()` en fin de session) |
| Construction du ZIP | `downloadZip` dans `src/recorder/App.jsx` |
| Page Répartition | `src/stats/` : `stats.js` (**pur et testé**, tout le calcul), `App.jsx` (le dessin seul), `stats.css`. Portage de la visualisation que la troupe produisait en Python (dépôt `theatre_transport_de_femme`, `viz/generate_viz.py` et `viz/main.tex`) : deux camemberts (mots, répliques) et un bloc où **chaque carré est un mot**, replié rangée par rangée, coloré par le personnage qui le prononce. **« Carré » et jamais « pixel »**, alors que c'est bien un pixel par mot que la référence Python dessinait : la page est ouverte à toute la troupe, et un pixel n'est un mot connu que de qui sait déjà qu'un pixel *conceptuel* est rendu par plusieurs pixels d'écran (huit de côté au réglage par défaut, quatre au bout de la course) ; « carré » se voit à l'écran et ne demande rien à personne. Le mot « pixel » ne reste donc que là où il désigne un vrai pixel d'écran (l'arrondi de la largeur du bloc). Les trois libellés de panneau sont ceux du PDF, mot pour mot : c'est le même document, servi à l'écran et tenu à jour. **Ouverte à toute la troupe** (elle est dans `ACTOR_CARDS`, pas dans `RESPO_ONLY`), donc elle porte le sceau commun de la troupe et c'est son icône, le camembert, qui la dit. **Portée** : les deux selects habituels du bandeau, et **les trois niveaux tiennent dedans** : chacun porte en premier choix le niveau au-dessus de lui, « Toute la pièce » dans celui d'acte comme « Tout l'acte » dans celui de scène, donc deux contrôles pour trois niveaux et le même geste écrit deux fois de la même façon. Sur « Toute la pièce », le select de scène est **désactivé, grisé et vide** : rien à lire dedans, et c'est le but. « Tout l'acte » n'y serait plus vrai, et un libellé de repli (« Toutes les scènes » a été essayé, puis retiré) ne fait que redire ce que le champ d'à côté vient d'annoncer, en donnant à un champ éteint l'air de porter une valeur ; un champ vide et gris se lit d'un coup d'œil comme « pas de choix ici », et la portée ne s'écrit qu'à l'endroit qui la décide. La vieille objection à ce choix (un contrôle grisé que rien n'expliquait) tombe par la lecture de la rangée : le champ voisin dit « Toute la pièce » et reste actif, donc rien n'est bloqué et la cause est à côté de l'effet. Désactivé et non retiré, pour que la rangée ne change pas de forme sous le curseur ; l'option vide existe pour que le select reste CONTRÔLÉ sur la même valeur `ALL` ; et pas d'infobulle dessus, un contrôle `disabled` ne recevant aucun événement souris (même leçon que les boutons du bandeau de l'Édition et leur enveloppe `.btn-tip`). **Le grisé demande une règle**, `select:disabled` dans `theme.css` : le `background: #fff` du select défait celui du navigateur, donc sans elle un champ éteint se lisait exactement comme un champ actif. Ce n'est pas l'`opacity: 0.45` de `.btn:disabled`, et l'écart est voulu : un bouton éteint est un cul-de-sac qu'on n'a pas à lire, un champ éteint montre encore une valeur, et c'est elle qui explique. Elle vit dans le thème et pas dans `stats.css`, en sélecteur d'élément comme les cases à cocher : l'aspect d'un champ partagé par cinq bandeaux ne s'écrit pas dans la page qui est seule à s'en servir aujourd'hui. **Deux essais écartés**, dans cet ordre. Une case à cocher « Toute la pièce » d'abord : sur ce site une case est un réglage d'AFFICHAGE (les quatre de la Répétition, les deux de la Recherche), or ce contrôle change ce que la page MONTRE. Puis un bouton bascule `.scope-all` dans la rangée des selects (`.btn` aux métriques d'un select, `aria-pressed`, `.btn.primary` une fois enfoncé), qui désactivait les deux : enfoncé, son accent plein à côté de deux champs éteints ne se lisait plus comme la commande qu'il était mais comme l'ÉTAT qu'il venait de produire, une étiquette allumée au-dessus de deux champs morts, et rien ne disait qu'on pouvait le relâcher. Corollaire : `actIndex` vaut `ALL` sur la pièce entière, c'est l'état d'ouverture de la page (on lit d'abord la pièce en entier, on descend ensuite dans un acte puis une scène), et les deux rangs vont au calcul tels quels, sans traduction. **Choisir un acte pose sa PREMIÈRE scène** et pas « Tout l'acte » : on descend d'un cran à chaque geste (la pièce, un acte, une scène), et s'arrêter sur l'acte entier demandait de rechoisir dans le second select ce qu'on venait de choisir dans le premier ; « Tout l'acte » reste en tête de sa liste, à un clic de là. C'est aussi la valeur de départ de `sceneIndex`, pour n'avoir qu'un défaut à connaître (elle ne se voit pas au chargement, la pièce entière ignorant le rang de scène). Le repli sur `ALL` quand l'acte n'a aucune scène n'est pas de la prudence : un select contrôlé sur une valeur dont aucune option ne répond perd sa valeur. **Le seul réglage d'affichage de la page est au-dessous**, le curseur « Mots par ligne » (cf. plus bas), et il ne ressemble donc pas aux selects : ni cadre ni fond, étiquette et nombre à l'encre douce. Cinq choses à ne pas défaire. (1) **La somme des largeurs des rectangles vaut le nombre de mots**, c'est LE contrat du bloc (`blockRects`, testé) : une réplique à cheval sur trois rangées donne trois rectangles, et un mot occupe exactement un carré, une fois. (2) Un `<rect>` par **tronçon** et pas par mot, les répliques consécutives d'un même personnage étant fusionnées d'abord (elles seraient adjacentes et de la même couleur) : 1166 rectangles pour les 10 307 mots de la pièce. En SVG et pas en `<canvas>` (le site n'en a qu'un, l'oscilloscope du micro) et en `crispEdges`, sans quoi un liseré translucide entre deux tronçons voilait la grille entière. **Sa largeur rendue est arrondie à un multiple entier du nombre de colonnes** (`@supports (width: round(...))` dans `stats.css`, le nombre descendant du JSX en `--stats-columns`), et c'est ce qui rend les mots tous de la même taille : `crispEdges` cale chaque bord sur le pixel écran, or 120 colonnes dans les 820 px de la carte font 6,833 px par mot, donc les cellules sortaient à 7, 7, 7, 6, 7… (mesuré), une colonne étroite tous les six mots et une rangée basse toutes les six rangées, ce qui se lit exactement comme le blocage d'un JPEG. À 720 px (6 px pile) tout est régulier, en densité simple comme double. Le prix est la largeur, jusqu'à une cellule de moins que la carte, d'où le centrage ; les deux autres pistes sont documentées et écartées dans `stats.css` (retirer `crispEdges` ramène le liseré, le corriger demande de faire déborder chaque rectangle sur son voisin et de peindre à l'envers). (3) **Chaque camembert se trie sur SA grandeur** : trier les deux sur les mots laissait la colonne du camembert des répliques dans le désordre (162, 200, 192…), et une colonne de nombres qui ne descend pas se relit chiffre par chiffre. C'est un ordre d'AFFICHAGE, donc il vit dans le composant et pas dans `speechStats`. (4) **Les pourcentages ne sont pas posés sur les parts** (contrairement à l'`autopct` de matplotlib) : dès qu'une part est petite son étiquette chevauche sa voisine, et sur une scène à huit personnages il y en a toujours une. Ils vivent dans la légende, alignés en `tabular-nums` comme les nombres de l'Avancement, et le total occupe le centre de l'anneau, **à une taille qui rétrécit quand le texte ne tient plus** (`centerFontSize`, testé) : à taille figée, le total d'une pièce à six chiffres de mots sortait sous l'anneau et l'unité la plus longue (« répliques ») touchait déjà les bords. Le calcul est une largeur promise (54 unités du viewBox : le trou en fait 63 de diamètre, mais un texte posé au-dessus du centre n'a que la corde, 55 là où passe le haut des chiffres) divisée par la chasse des chiffres `tabular-nums` ; il ne change rien aux nombres d'aujourd'hui, et c'est ce que le test vérifie d'abord. Les deux tailles nominales vivent dans `stats.js` et pas dans `stats.css`, puisque c'est le module qui décide de les réduire. (5) **Le personnage mis en évidence appartient à la PAGE, pas à un panneau** : les trois dessins répondent au même choix, donc les autres personnages passent en opacité réduite dans les deux anneaux ET dans le bloc, ce que la référence produisait en un fichier `all_image_<nom>.svg` par personnage. Il se désigne d'une part de camembert ou d'une ligne de l'une des trois légendes ; le bloc était seul à savoir isoler, alors que les camemberts avaient exactement la même chose à montrer, et une page qui n'offre à cliquer qu'une liste en bas se lit comme inerte. Les **trois légendes sont un seul composant** (`CharacterLegend`), donc le même bouton et le même intitulé qu'elles portent des nombres ou non, et c'est la SEULE surface accessible au clavier : les parts et les blocs vivent dans des SVG en `role="img"`, dont les descendants ne sont pas exposés (une trentaine d'arrêts de tabulation qui ne diraient rien de plus). Deux états et pas un : `selected` est le choix arrêté, `hovered` le préfigure et gagne quand il existe, donc survoler un autre nom montre l'autre sans perdre le choix. **Le survol ne vaut qu'à la souris** (`hoverProps` filtre sur `pointerType`) : au doigt le navigateur émet un survol émulé après l'appui, qui restait accroché et empêchait le second appui d'éteindre. `aria-pressed` et l'`aria-label` du bloc suivent le choix ARRÊTÉ et jamais le survol (un bouton n'est pas enfoncé parce que la souris passe dessus, une description ne se réécrit pas sous le curseur). **La mosaïque, elle, ne se désigne pas du tout au pointeur** : elle suit le choix mais ne le prend pas, ni au clic ni au survol. Un mot fait 8 px au réglage par défaut et 4 px au bout de la course, donc viser le bon personnage y est un coup de dés et un tronçon d'un seul mot est intouchable (essayé, retiré) ; au survol elle changerait de personnage tous les deux pixels et rallumerait les trois dessins en continu. Sa légende est en haut de la page, large et nommée, et c'est aussi ce qui garde une seule façon de désigner quelqu'un au clavier comme à la souris. **Cette légende vit en barre, sous le bandeau et au-dessus du contenu** (`.stats-legend-bar`), et plus sous la mosaïque : sur la pièce entière le bloc fait plusieurs écrans, donc en pied de carte elle n'était visible qu'en bout de course, et pendant tout le défilement on lisait des couleurs sans pouvoir les nommer ni en isoler une, alors qu'elle est la seule surface qui désigne quelqu'un au clavier. Elle est horizontale et **passe à la ligne**, jamais un défilement en travers, qui cacherait les derniers noms (à dix personnages la rangée dépasse les 900 px du contenu) ; le prix est sa hauteur, bornée à 30 dvh, au-delà de quoi elle défile (précédent du journal de l'Avancement), et ses boutons étant focalisables cette zone n'a pas besoin du `tabIndex` que la grille de l'Avancement se donne. Elle garde ses cibles de 40 px au pouce : c'est la seule commande du contenu, et la rétrécir pour gagner de la hauteur la rendrait moins sûre à viser au moment même où elle est toujours sous le doigt. Ce qui la porte est la **coquille partagée à la hauteur de la fenêtre** (`.page-shell` / `.page-scroll`, `theme.css`), où seul le contenu défile : c'est la même que celle de l'Édition, remontée dans le thème le jour où les deux pages l'ont écrite chacune de son côté, et pour la même raison qu'elle, coller une barre SOUS le bandeau demanderait sinon de connaître la hauteur de celui-ci, qui est un inconnu **animé** (titre sur deux lignes, deux paragraphes de doc, repli sur 0,26 s), donc de la mesurer en JS, de la remesurer à chaque repli, et la barre traînerait un quart de seconde derrière l'animation. Ici rien n'est mesuré et `PlayHeader` n'est pas touché : son `position: sticky` dans un ancêtre qui ne défile pas tient le haut parce qu'il **est** en haut. Trois écarts avec l'Édition : la hauteur reste le `100dvh` par défaut du thème là où l'Édition pose `--shell-height: 100vh` (cette page-ci s'ouvre au doigt, donc une barre d'adresse qui se rétracte y change vraiment la hauteur utile) ; la coquille est posée sur un élément de la page et jamais sur `body`, pour que les deux écrans pleine page de `PageState` gardent le défilement normal ; et la barre consomme `--header-shadow`, token réservé au bandeau partagé, ce que la CI interdit de REDÉFINIR mais pas de lire, parce que deux bandes empilées doivent porter la même ombre. Corollaire : **la phrase qui dit comment lire la mosaïque est passée sous le titre du panneau**, avant le dessin, alors qu'elle en fermait la carte : sous un bloc de plusieurs écrans elle n'arrivait qu'à qui avait défilé jusqu'au bout, c'est-à-dire à qui n'en avait plus besoin. Elle nomme maintenant « la barre du haut » là où elle disait « un nom », la légende n'étant plus juste sous le dessin qu'elle explique. Un personnage muet dans la portée choisie ne s'isole pas (les dessins deviendraient vides sans qu'on comprenne pourquoi) mais le choix n'est pas effacé pour autant, revenir à une portée où il parle le remontre. Deux écarts assumés avec la référence : elle fusionnait les répliques consécutives AVANT de compter, donc son camembert comptait des prises de parole, alors qu'ici il compte les répliques du `script.json`, l'unité de tout le site ; **la largeur du bloc est FIXE comme chez elle** (50 colonnes par scène, 100 pour la pièce), à ceci près qu'il n'y a plus qu'un seul chiffre, le même pour toutes les portées, et qu'il se règle : le curseur « Mots par ligne » du bandeau, de `MIN_COLUMNS` (50) à `MAX_COLUMNS` (200) par pas de 5, `DEFAULT_COLUMNS` (100) au départ. Elle a un temps été DÉRIVÉE du nombre de mots de la portée, ce qui donnait à chaque scène une forme flatteuse (deux fois plus large que haute) mais **au prix d'un carré qui changeait de taille d'une portée à l'autre**, donc de deux blocs qui ne se comparaient pas : une scène de 300 mots et la pièce entière remplissaient la même carte, et rien ne disait laquelle était longue. À nombre de mots par ligne constant, un carré fait la même taille partout et la HAUTEUR dit la longueur. Le réglage existe parce qu'aucune constante ne va d'une pièce de 500 mots à une de 30 000 ; **le défaut et le plancher sont les deux valeurs de la référence**, `w=100` pour la pièce entière et `w=50` par scène (`generate_viz.py`) : la page sert le même document que le PDF de la troupe, donc son bloc se lit à la largeur où elle l'a toujours lu, et la course ne fait que prolonger ces deux chiffres. Ça tombe juste par ailleurs, 100 colonnes dans une carte de 820 px donnant 800 px de bloc et un carré de 8 px pile (103 rangées pour la pièce). 120 a été le défaut le temps d'un essai, mais ce n'était que le plafond de l'ancienne formule dérivée, un chiffre sans origine. Les bornes tiennent pour le reste à une seule arithmétique, le côté d'un carré = largeur utile de la carte / nombre de colonnes (la carte fait la fenêtre moins 82 px : container 40, filet 2, carte 40) : sur la mise en page la plus large, 820 px, 50 colonnes donnent 16 px de côté et 200 en donnent 4. En dessous de 50 le bloc cesse d'être une mosaïque pour devenir une pile de barres (34 px à 24 colonnes) et une réplique moyenne occupe plusieurs rangées, donc on ne lit plus l'alternance des voix ; au-delà de 200 les tronçons courts commencent à disparaître (c'est déjà 52 rangées pour la pièce entière), et sur un téléphone (238 px de carte) le carré y descend à 1 px, donc le haut de la course est un réglage d'ordinateur. Un plafond de 250 a été essayé et retiré, la course cessant de servir bien avant. Les seuils eux-mêmes sont un jugement vérifié aux deux bouts à l'écran, pas une mesure. Le pas de 5 tombe juste sur les trois chiffres (c'est testé), et ce n'est pas cosmétique : une valeur hors de la grille du pas ferait sauter le premier appui sur une flèche du clavier. **Un curseur et pas une liste de valeurs** : le bloc se recompose pendant qu'on le tire, et c'est en le voyant bouger qu'on trouve la largeur qui va à sa pièce ; la valeur est écrite à côté, un curseur sans nombre ne se règle pas deux fois pareil. `accent-color` plutôt qu'une piste reconstruite, comme les cases à cocher et pour la même raison. Il ne vit qu'en mémoire, comme la largeur du rail de l'Édition (le projet n'a aucune persistance locale). Avec le curseur vient **un plancher d'un pixel par carré** dans `stats.css` (`min-width` sur `.stats-block`), inatteignable avec les bornes d'aujourd'hui et gardé quand même, parce qu'il est ce qui rend le plafond relevable sans redécouvrir le piège : `round(down, A, B)` vaut ZÉRO dès que A < B, donc dès que la carte est plus étroite que le réglage, et le bloc disparaît alors en silence, légendes restant en place. À 200 mots par ligne il faudrait une fenêtre de 282 px, sous tous les téléphones réels ; à 250, le temps de l'essai, un écran de 320 px suffisait. Le prix est un bloc qui peut dépasser sa carte d'au plus une largeur de carré moins un pixel, largement dans ses 20 px de remplissage ; rendre la main au `width: 100%` à la place ferait peindre par `crispEdges` certains carrés à 1 px et d'autres à 0, c'est-à-dire des mots qui s'effacent. Un **plafond de 12 px par carré** a par ailleurs existé ici du temps où le curseur descendait à 24 : c'est le plancher de 50 qui porte maintenant cette garantie, et le plafond faisait flotter un bloc étroit au milieu de sa carte. `countWords` reprend le découpage du `re.findall(r'\w+')` de la référence, apostrophes comprises (« l'crâne » compte deux mots) : ça gonfle les totaux dans l'absolu, mais la page n'affiche que des proportions et les chiffres concordent avec le PDF. Les répliques sans personnage valide sont comptées **à part** et signalées, jamais fondues dans le total (précédent de la grille de l'Avancement) |
| Grille d'avancement (Avancement) | `ProgressTable` dans `src/dashboard/App.jsx`, CSS `.dash-table*` dans `dashboard.css` : personnages × scènes, « enregistrées / total » par case, la même échelle ambre/vert portée par les cases, les noms, les numéros de scène et les actes (`statusClass`), donc une rangée, une colonne ou un acte entièrement vert se lit sans compter. Deux choses à ne pas défaire. (1) **La colonne des noms est figée** (`position: sticky; left: 0` sur `.dash-name` et sur les deux `.dash-corner`, qui sont de la même colonne) et c'est le conteneur qui défile, jamais la page : une pièce à dix scènes ne tient sur aucun téléphone, et une case « 2/5 » sans son nom en face ne dit rien. Le coin prend le fond de la PAGE (`--paper`) et non plus `background: none`, sinon les numéros de scène défilent visiblement par-dessous, et c'est LUI qui porte le filet gauche des deux rangées d'en-tête (`border-right`) : sur la première cellule d'acte et de scène, ce filet partait au défilement et le haut du tableau se retrouvait ouvert à gauche. Le conteneur est une région nommée et `tabIndex={0}`, sans quoi une zone qui défile et ne contient rien de focalisable serait hors d'atteinte au clavier. **Aucune ombre au bord de la colonne figée** : essayée pour dire qu'une case passe dessous, elle salissait chaque nom d'un dégradé gris ; ce qui sépare est le filet continu du haut du tableau à son pied. (2) **Le tableau est en `border-collapse: separate`** (`border-spacing: 0`), et ce n'est pas un détail de style : sous `collapse` les filets sont peints par le tableau et pas par les cellules, donc ils ne suivent pas une cellule `sticky` et la colonne des noms perdait toutes ses bordures dès le premier pixel de défilement. Chaque cellule porte donc son filet droit et bas, et deux règles rendent les bords qui n'ont aucun voisin pour les porter (filet gauche des noms, filet haut des actes et du premier nom) ; le filet haut ne se pose surtout pas sur tous les noms, il s'ajouterait au filet bas du précédent et donnerait une ligne de 2 px entre personnages. Le nom est enveloppé dans un `.dash-name-text` qui se coupe en « … » (`title` sur la cellule pour le rendre en entier) : la colonne figée prend sur la largeur des scènes, un nom à rallonge ne doit pas laisser un filet de grille pour tout le reste. Responsive : c'est cette colonne qu'on resserre d'abord sous 560 px (nom plus court, moins de gouttière), les cases ensuite, et le pas des scènes descend à 44 px, de quoi tenir « 12/12 » |
| Seul retour du respo (journal des dépôts) | `scripts/update_history.py` → `data/history.json` (`{runs: [{at, files}]}`, la plus récente d'abord, plafonné à 30), recopié dans `manifest.json` par `build_manifest` ; affiché par `Journal` dans `src/dashboard/App.jsx`. **C'est le canal d'erreur du projet** : il n'y a plus ni issue GitHub ni statut README, donc un fichier refusé ne se dit QUE là. **Une ligne par FICHIER et pas par dépôt** (chaque fichier a son propre sort : un ZIP abîmé au milieu de trois bons n'empêche pas les autres), dans un `<table>` dont le conteneur défile (`max-height` + `overflow: auto`, en-tête `sticky` : une trentaine de dépôts ne doit pas allonger la page sans fin). Quatre colonnes : date (année comprise, un journal se relit des mois plus tard), statut, type, détail. **Les deux colonnes d'icônes empruntent la pastille des sceaux** (`page-mark` + `.dash-journal-mark`, centrées) : le type porte le sceau de la page productrice (micro de l'Enregistrement, plume de l'Édition, les deux mêmes que le bouton de dépôt juste au-dessus, donc aucune légende à donner ; pastille neutre avec un `?` pour un fichier qu'aucune page ne revendique), et le statut prend les mêmes vert et ambre que la grille au-dessus avec `CheckIcon`/`CrossIcon`. Aucun mot dans ces deux colonnes, donc chaque pastille porte un `aria-label` et un `title`. Détail optionnel (`detailOf`) : rien pour un script réussi, nom du ZIP et compte de répliques pour des voix, nom et motif pour un échec. Le tableau est plafonné à `JOURNAL_ROWS` lignes, et **le dit** (`.dash-journal-more`, « N dépôts plus anciens non affichés ») : dans le seul canal de retour du projet, un tableau qui s'arrête sans un mot se lit comme « il n'y a rien de plus ». **Aucun rappel en haut de page** : la ligne du journal suffit, un bandeau d'alerte a été essayé puis retiré. La date de la première ligne fait aussi office de détecteur de panne (un run en échec ne commite rien, donc elle arrête d'avancer) ; **pas d'horodatage du run dans le manifest**, un champ réécrit à chaque exécution ferait différer `manifest.json` à tous les pushes, donc un commit robot chaque fois |
| Bouton de dépôt (Avancement) | `githubUploadUrl()` (`src/shared/data.js`) → `https://github.com/<owner>/<repo>/upload/master/uploads`, reconstruit depuis l'URL Pages ; `null` hors github.io (dev, domaine perso), où la carte se masque plutôt que de forger un 404. Deux formes d'URL Pages à couvrir : site de projet (`owner.github.io/<repo>/…`, le dépôt est le premier segment) et **site racine** (`owner.github.io/…`, où le premier segment est un nom de fichier et le dépôt porte le nom du domaine) ; sans ce second cas le bouton pointait vers `github.com/<owner>/dashboard.html`. **Une seule carte** (`UploadLinks`) : il n'y a qu'un dossier de dépôt, et deux cartes vers la même URL se liraient comme deux destinations. Les deux sceaux (micro, plume) l'encadrent et disent les deux sortes de fichiers, `.dash-upload-word` colorant chaque mot avec le `--page-mark` de sa page |
| Filtre audio ffmpeg | `audio_filter(peak)` dans `scripts/process_uploads.py`, construit par prise à partir de son pic (mesuré par `measure_peak_dbfs`, une passe `volumedetect` avant la conversion). Deux choses à ne pas défaire : le seuil de silence est **relatif au pic** de la prise, jamais une valeur absolue (les niveaux varient énormément selon la distance au micro et l'AGC du navigateur : un seuil fixe laissait une seconde de vide sur une prise forte et mangeait le premier mot d'une prise faible), et `start_duration` est **obligatoire** (les navigateurs posent un clic de quelques dizaines de ms sur les tout premiers échantillons ; sans durée d'attaque minimale, `silenceremove` considère que le son a commencé à l'échantillon 0 et ne coupe rien du tout). Sous `SILENT_PEAK_DBFS` la prise est convertie sans trim : la rogner entièrement écrirait un mp3 vide et illisible |
| Contrats inter-fichiers tenus par la CI | `scripts/tests/test_contracts.py`. Il vérifie ce qui ne vivait que dans un commentaire « keep in sync », en **lisant les sources** plutôt qu'en recopiant les valeurs attendues (recopier ne ferait que déplacer le problème) : `SAFE_ID` (JS) identique à `LINE_ID_PATTERN` (Python) ; aucun CSS de page ne redéfinit un token `--header-*` (la liste est lue dans `theme.css`, donc un nouveau token réservé est couvert d'office) ; aucune règle de bandeau ne consomme `--accent`, `--font-serif` ou `--shadow`, tous re-skinnés par l'éditeur ; chaque clé de `PAGES` a ses deux variables de sceau dans `theme.css` et pas l'inverse ; les entrées de `vite.config.js` et les `.html` de la racine coïncident exactement ; et les cinq gardes de l'i18n (classe `TestCatalogues`), qui sont ce qui remplace le mieux les tests de composant que ce projet n'a pas : toute clé passée à `t()` ou `<T>` existe dans les DEUX catalogues (une clé mal tapée s'affiche en clair à l'écran), aucune clé déclarée n'est inutilisée (une clé orpheline signale une chaîne qu'on a cru traduire et qui est restée en dur, ce qui est arrivé à `common.loadingScript`), et **aucun littéral visible ne survit dans `src/`**, vu sous trois angles complémentaires parce qu'aucun ne suffit (un littéral accentué, un attribut ou une prop qui porte du texte, un nœud de texte JSX). Ces trois-là surveillent TOUT `src/`, sans liste de fichiers à tenir, et c'est une leçon payée : ils ont commencé en liste de fichiers « déjà traduits », qui grandissait phase par phase pour ne pas garder une CI rouge tout un chantier, et cette liste est exactement ce qui a laissé passer cinq pages entières. Le relevé des clés balaie l'appel `t(…)` ENTIER, parenthèses équilibrées, sinon il ne voyait pas `t(canUndo ? "editor.undo.tip" : "editor.undo.none")` ; une clé COMPOSÉE à l'exécution (`page.${page}.label`, `rail.${key}.tip`) est couverte par motif, et une table de clés se NOMME `*_KEY`/`*_KEYS` pour être relevée. La parité des deux catalogues, elle, est vérifiée côté JS (`locales/parity.test.js`), qui demande `Intl.PluralRules`. Un test qui ne peut pas échouer ne sert à rien : chacun de ces gardes a été vérifié par mutation |
| Script de la pièce en PDF | `scripts/build_script_pdf.py` : `script.json` → `.tex` → `data/script.pdf`. Moteur : **`pdflatex`, paquets TeX de la distribution** (`texlive-latex-recommended`, `texlive-lang-french`, `texlive-fonts-recommended`, `lmodern`), c'est aussi celui du script d'origine de la troupe ; `tectonic` reste accepté en second dans `ENGINES` pour qui l'a déjà, le rendu est équivalent à l'espace avant les deux-points près (en XeTeX il suit la règle française de plus près). **tectonic a été essayé en CI puis écarté, ne pas y revenir sans nouvel élément** : il est plus rapide en régime établi (binaire statique de 26 Mo, ~0,5 s de compilation contre 0,25 s), mais il imposait une URL de release figée, une empreinte sha256 à faire suivre à chaque version et un `actions/cache` pour ne pas repayer 40 s de téléchargement de fichiers TeX (cache que GitHub évince au bout de 7 jours sans usage, donc souvent froid pour une troupe hors saison). Trois choses qui pourrissent dans un dépôt fait pour être forké puis laissé tranquille des années, pour gagner moins d'une minute sur un job hebdomadaire ; les paquets apt sont signés par la distribution et n'ajoutent aucune relation de confiance. La compilation elle-même n'a jamais été le coût : 0,25 s pour les 26 pages de la pièce, tout le reste est l'installation. La mise en page reprend celle du script LaTeX que la troupe utilisait déjà (`article` 10pt deux colonnes, babel français, `\speak` = nom en capitales grasses puis deux-points, filet entre scènes) : le PDF doit ressembler au script que les acteurs ont eu en main. **Une pièce en un seul acte n'imprime pas son titre d'acte** (`show_acts`) : un intertitre qui ne distingue rien de rien ouvrait la première page au-dessus de la première scène. L'acte reste dans `script.json`, l'éditeur travaillant toujours par acte ; c'est la mise en page qui le tait. Trois choses à ne pas défaire. (1) **Ce script ne peut pas faire échouer le déploiement** : il rend 0 même quand LaTeX plante, et ses deux étapes de `build.yml` sont en `continue-on-error`. C'est structurel, pas de la prudence : `build.yml` n'écrit ni issue ni journal, donc un run rouge ne se raconte nulle part et le respo ne verrait que le site cesser de se mettre à jour. (2) **Le texte est échappé** (`latex_escape`) avant d'entrer dans le `.tex` : il vient d'un champ de saisie, et `% & $ # _ { } ~ ^ \` sont du code pour LaTeX. Une seule passe de `re.sub`, jamais un `str.replace` par caractère, qui ré-échapperait les antislashs qu'il vient d'écrire. La même fonction **aplatit les blancs**, et ce n'est pas de la cosmétique : une ligne vide est un `\par` pour TeX, or `\lhead` (titre courant) et `\MakeUppercase` (nom du personnage) s'arrêtent net sur une fin de paragraphe dans leur argument, donc un titre ou un nom hand-édité sur deux paragraphes faisait disparaître le PDF de TOUTE la pièce, sans un mot au respo. LaTeX ramenant de toute façon toute suite de blancs à une espace, il n'y a rien à perdre ; une réplique y gagne même de rester un seul paragraphe, donc de garder son nom de personnage en tête. (3) `lmodern` n'est pas cosmétique : sans lui les fontes T1 n'existent qu'en tailles discrètes et le titre demandé à 52 pt sortait à 35,83 pt avec un simple avertissement dans le journal (c'est ce qui arrivait au script d'origine, d'où les 36 pt d'aujourd'hui, taille à laquelle le titre tient sur une ligne). **La page de titre demande trois précautions cumulées**, chacune mesurée sur le PDF rendu et aucune suffisante seule : `\newgeometry{margin=...,nohead,nofoot}` (la géométrie du corps réserve 1,5 pouce en haut pour l'en-tête contre 0,75 en bas, et sans `nohead,nofoot` geometry garde encore la hauteur d'en-tête), `\topskip` à zéro (TeX cale la première boîte d'une page à au moins cette distance du haut sans rien rendre au ressort du dessous), et un `\vbox to \textheight` avec un ressort de chaque côté plutôt que deux `\vspace*{\fill}`, qui laissaient une douzaine de pixels d'écart constants. Ces deux ressorts sont dans un rapport **3:5 et non 1:1** : le titre se pose au centre OPTIQUE, aux deux cinquièmes de la hauteur, parce qu'un titre mathématiquement centré sur une page par ailleurs vide se lit comme tombé trop bas. Les rapports voisins ont été comparés sur le rendu (1:1 tombe, 1:2 remonte trop et le vide du dessous déséquilibre la page). Le `\hsize\textwidth` de ce vbox n'est pas décoratif : en document deux colonnes sa largeur vaudrait une demi-page et le titre se couperait. Vient ensuite un **verso blanc** (`\thispagestyle{empty}\null`), pour qu'en recto-verso le texte commence sur une belle page ; `\restoregeometry` se place **après** lui et jamais avant, sinon son saut de page expédie une page vide de plus, celle-là avec le titre courant en haut. La découpe est **pure d'un côté, binaire de l'autre** : `render_tex` rend une chaîne, donc toute la mise en page se teste en lisant du texte (`test_build_script_pdf.py`), et seul le test de bout en bout compile vraiment, en se sautant là où aucun moteur n'est installé. Aucune dépendance Python : le module écrit du texte et lance un binaire. Le bouton de téléchargement vit sur l'Avancement (`ScriptPdfLink`, `.dash-script-btn`), pas sur un accueil : c'est au respo de décider quelle version fait foi. Le fichier reste servi à une URL publique, c'est la discrétion de `respo.html`, pas un verrou. C'est **`.btn.primary`, le bouton de téléchargement du site**, le même que le ZIP des prises (Enregistrement) et que le script (Édition) : un téléchargement se présente pareil partout. Les deux gestes de la page **ne se ressemblent donc pas au repos**, et c'est le but : le dépôt est une carte blanche (une destination, on part sur GitHub), le téléchargement un aplat d'accent. Deux essais écartés avant : un lien sobre en gris (trop effacé), puis une deuxième carte identique à celle du dépôt (les deux se confondaient). Ils vivent dans `.dash-actions`, colonne centrée en `align-items: center` et **pas** `stretch`, qui étirerait le bouton à la largeur de la carte, ce qu'aucun autre bouton du site ne fait. Hors github.io la carte de dépôt disparaît et le bouton reste seul. **Le bouton se masque lui aussi quand le PDF n'est pas là**, et pas seulement en dev : le fichier est gitignoré et produit par deux étapes `continue-on-error`, donc il manque dès qu'une install apt ou une compilation échoue, et sur une pièce jamais saisie. `ScriptPdfLink` le sonde en `HEAD` au montage et ne rend rien tant qu'il n'a pas répondu (`ready` à `null`, donc le bouton apparaît après la page mais ne disparaît jamais sous la souris) : dans le seul canal de retour du respo, un téléchargement qui rend une page d'erreur renommée « transport-de-femmes.pdf » est pire que pas de bouton, parce qu'il se croit réussi. Même raison qui fait `githubUploadUrl()` masquer la carte plutôt que forger un 404 ; le middleware de dev de `vite.config.js` répond donc au `HEAD` (sans corps) et connaît le type MIME `.pdf`. **Le survol, lui, est commun** : c'est la classe partagée `.lift-hover` (`theme.css`), ombre haute plus pas d'un pixel vers le haut, le geste de toutes les surfaces cliquables du site (les six cartes des deux accueils la portent aussi, avec `--lift: -3px`, seul écart légitime : une carte haute se lève de plus). Le bouton n'en reprend pas le filet teinté (sa bordure est déjà en accent, il n'y aurait rien à voir) et garde à la place l'assombrissement de `.btn.primary:hover`. La `transition` reste chez chaque surface et n'est **pas** remontée dans `.lift-hover` : `transition` est une seule propriété, donc une déclaration locale l'écraserait au lieu de s'y ajouter ; toute surface qui prend la classe doit lister `transform` et `box-shadow` dans SA transition (ici il fallait de toute façon les ajouter, `.btn` ne faisant glisser que `background` et `border-color`). Le retour à l'accueil du bandeau ne prend pas la classe : il garde le pas d'un pixel mais remplace l'ombre par la nappe crème du sceau, c'est un autre geste. **Pas de sceau sur le bouton** : sur la carte de dépôt la plume désigne le `script.json` qu'on DÉPOSE, la reprendre pour un PDF qu'on TÉLÉCHARGE ferait lire deux gestes opposés sous la même pastille. Libellé « Télécharger la pièce à imprimer (PDF) » et **surtout pas « le script de la pièce »** : la carte de dépôt dit déjà « script de la pièce (JSON) », et deux libellés partageant leur groupe de mots ne se distinguaient plus que par l'acronyme, alors que PDF est le seul des trois (ZIP, JSON, PDF) qu'un respo lit sans y penser. « à imprimer » dit l'usage ; le mot « script » reste au dépôt, où il désigne le fichier de travail. Le `download` renomme le fichier avec le slug de la pièce, « script.pdf » ne disant rien dans un dossier de téléchargements. Les didascalies n'y sont pas encore : elles n'existent pas dans `script.json`, et le script LaTeX d'origine en distinguait trois sortes (`\stage`, `\stageinline`, `\stagemain`), donc le futur champ ne sera pas un booléen |
| Panneau latéral de l'Édition (le rail) | `src/editor/EditorRail.jsx` : une bande de trois icônes toujours visible (48 px, un plan, un personnage et une loupe) et **une section ouverte à la fois** (272 px) à gauche de la colonne de texte, « Structure » (`StructurePanel.jsx`), « Personnages » (`CharacterPanel.jsx`) et « Recherche » (`SearchPanel.jsx`). L'ordre des icônes est l'ordre du parcours au clavier depuis le bandeau, et Structure vient d'abord parce qu'elle porte la NAVIGATION de la page (elle a remplacé les selects d'acte et de scène du bandeau) : c'est aussi la section ouverte à l'arrivée, la refermer d'office cacherait le choix de la scène que les deux selects affichaient sans qu'on demande. **Pas un `role="tablist"`** : un tablist promet un onglet sélectionné en permanence, alors que le rail a un état « rien d'ouvert » ; ce sont trois boutons de dévoilement à `aria-expanded`, comme le bouton de repli du bandeau. **Les icônes de la bande sont le seul interrupteur du panneau**, comme la barre d'activité d'un éditeur de code : cliquer une icône ouvre sa section, recliquer celle de la section ouverte replie le rail, et Escape le replie aussi (écouté sur le panneau, cf. la fin de cette ligne). Elles disent donc quelle section ET si elle est ouverte (`aria-expanded`, lu par le CSS), et elles sont le seul mobilier de l'état replié, où le rail fait exactement la largeur de la bande. **Deux languettes de repli ont été essayées puis retirées**, et la seconde a coûté un bug qu'il ne faut pas refabriquer : posée au milieu du bord droit, elle partageait son `pointerdown` avec la poignée de largeur, qui appelle `setPointerCapture` (sans quoi le glissement lâche le curseur) ; or un pointeur capturé retarge le `click` sur l'élément capturant, donc le clic arrivait au bord et pas au bouton et la languette ne repliait qu'une fois sur deux (celles où le double-clic « largeur par défaut » sortait avant la capture). La contourner demandait de replier depuis le `pointerdown` de la poignée, c'est-à-dire de deviner dans un geste de glissement s'il visait un bouton ; séparer les deux objets remettait deux colonnes de mobilier sur un seul bord. La première languette vivait en tête du panneau, à côté du titre : un bouton de plus dans une rangée de titre, et rien n'y disait de quel côté le panneau allait se ranger. Ce qui est perdu et assumé : rien ne DESSINE plus le repli (une icône allumée annonce « ouvert », pas « cliquez pour fermer »), c'est la convention de la barre d'activité qui le porte, et le geste se joue là où on choisit la section plutôt qu'au bord opposé du panneau.
**Le bord droit ne fait donc plus qu'une chose, régler la largeur** (on tire n'importe où dessus), et c'est son FILET qui dit ce geste : il passe de 1 px à 2 px et prend l'accent au survol. Rien d'autre ne change de couleur, et surtout pas le bord sur toute sa largeur, comme au premier essai : ça posait un pavé violet en travers de la page pour annoncer un geste d'un pixel. Il mesure 10 px (`--ed-rail-edge`) : une poignée de 6 px se rate à la souris, et les 14 px du temps de la languette laissaient une gouttière morte entre le texte du panneau et la colonne de la pièce. Le panneau n'est monté que quand une section est ouverte : rien à sortir du parcours clavier, contrairement au bandeau, qui doit garder ses réglages montés pour animer une hauteur inconnue. **Le panneau lui-même ne défile pas** : sa tête (le titre de la section) est fixe, et chaque section accorde le défilement à ce qui doit défiler et à lui seul, la liste des résultats pour la Recherche, la liste des personnages pour l'autre. Motif : les réglages de la recherche partaient vers le haut dès qu'on parcourait les correspondances, et les faire redescendre chercher pour relire la requête, recocher « Mot entier » ou relancer un remplacement était le geste le plus fréquent de l'écran ; côté personnages, le formulaire d'ajout reste sous les yeux d'une distribution à vingt noms, et **juste sous la liste** : `.character-list` s'écrit `flex: 0 1 auto` et surtout pas `flex: 1`, qui lui faisait prendre toute la colonne même à trois personnages et poussait le formulaire au bas du panneau, à un écran de la liste qu'il complète. Elle fait donc la hauteur de son contenu et ne se laisse comprimer (donc défiler) que quand la place manque. Corollaire : pas de filet entre les deux, contrairement à la Recherche, où la liste des résultats défile toujours et où la frontière est réelle ; ici un trait en travers du panneau sous trois personnages ne séparerait rien, et quand la liste défile pour de bon c'est son ascenseur qui le dit. Chaque frontière porte un filet qui **file jusqu'aux bords du panneau** (marges négatives du remplissage) : une fois qu'une partie est fixe et l'autre défilante, rien ne disait plus où passait la limite, et un résultat coupé en haut de la liste se lisait comme un défaut d'affichage ; une ligne qui s'arrêterait à douze pixels des bords se lirait, elle, comme un soulignement de bloc et pas comme une division de panneau. La tuyauterie est le `min-height: 0` de `.editor-rail-body`, sans lequel le minimum automatique d'un élément flex vaut sa hauteur de contenu et rien ne défile (même piège que `.editor-layout` et `.play-header-settings-inner`). **La page ne défile plus en entier** : la coquille partagée `.page-shell` (`theme.css`, la Répartition l'emploie aussi) fait la hauteur de la fenêtre, `.editor-shell` ne posant plus que `--shell-height: 100vh` ; le bandeau reste dans le flux en haut, et la rangée du dessous (`.editor-layout`, grille `auto minmax(0, 1fr)`) donne au rail sa largeur et à la colonne le reste ; le rail et `<main class="editor-column">` défilent chacun pour son compte. Motif : le rail (donc la liste des correspondances) doit rester sous les yeux pendant qu'on parcourt les répliques, et la seule autre façon de l'obtenir, un rail collant sous le bandeau, demande de connaître la hauteur du bandeau, qui est un inconnu **animé** (titre sur deux lignes, deux paragraphes de doc, repli sur 0,26 s) : il faudrait la mesurer en JS, la remesurer à chaque repli, et le `top` du rail traînerait un quart de seconde derrière l'animation à chaque geste. Ici rien n'est mesuré, et `PlayHeader` n'est pas touché : son `position: sticky` dans un ancêtre qui ne défile pas se comporte comme `relative`, il tient le haut parce qu'il **est** en haut. `vh` et pas `dvh` : une barre d'adresse qui se rétracte est un problème de téléphone, et cette page ne s'ouvre pas au doigt. La coquille est posée sur un élément de l'éditeur et jamais sur `body` : les deux écrans pleine page (script illisible, ouverture au doigt) gardent le défilement normal, leur carte pouvant dépasser d'une fenêtre basse. **La colonne de 860 px reste centrée sur ce qui reste**, donc ouvrir le rail décale le texte de 136 px : c'est pour ça que le rail s'anime (0,26 s et `--ease-header`, la courbe du repli du bandeau, emprunté et jamais redéfini ; la page a deux surfaces qui se replient, elles doivent se replier du même geste), et c'est aussi pourquoi la piste de la compensation a été écartée, elle mange la gouttière gauche jusqu'à ce que le texte touche le rail. **L'animation est une largeur et pas la ruse `minmax(0, 0fr)` du bandeau** : là-bas la hauteur du contenu est inconnue et ne s'interpole pas, ici la largeur ouverte est un nombre qu'on choisit ; une piste `fr` ferait en plus dépendre la largeur du rail d'un nom de personnage à rallonge. On garde en revanche la leçon : c'est le rail qui rogne (`overflow: clip`, cf. le piège de géométrie plus bas) et le panneau qui garde sa largeur fixe, un panneau élastique reflowant son texte à chaque image du glissement. D'où le **`flex: none` sur `.editor-rail-panel`**, qui n'est pas décoratif : par défaut un élément flex a `flex-shrink: 1`, donc pendant le repli le panneau se laissait écraser au lieu d'être rogné, et son texte se recomposait à chaque image, exactement ce que cette géométrie cherche à éviter (ça se voyait aussi au repos, le panneau mesurait un pixel de moins que demandé). **La largeur du panneau se règle en tirant son bord droit** (`.editor-rail-edge`, `role="separator"` focalisable, donc réglable aussi aux flèches, avec Début/Fin pour les bornes et un second appui rapproché pour revenir au défaut) ; bornes 200 px (en dessous, une puce de personnage complète ne tient plus sur une ligne) et 560 px (au-delà, le panneau prend plus de place que la colonne de texte sur une fenêtre courante). Trois détails à ne pas défaire : la transition de largeur est coupée pendant le glissement (classe `resizing`), sinon le panneau poursuit le curseur avec un quart de seconde de retard, ce qui se lit comme une latence ; `preventDefault` sur `pointerdown` empêche la sélection de texte de démarrer sous le curseur, MAIS il supprime du même coup les événements souris de compatibilité, donc le retour au défaut est un double-clic reconnu à la main sur les horodatages et pas un `onDoubleClick` ; et la largeur ne vit qu'en mémoire, le temps de l'onglet, parce que le projet n'a aucune persistance locale et qu'en ouvrir une pour une préférence d'affichage serait la première. **Deux pièges de géométrie déjà payés, à ne pas défaire.** (1) Le rail rogne en `overflow: clip` et **jamais** `hidden` : `hidden` en fait un conteneur de défilement (invisible mais défilable par programme), donc au montage du panneau, quand le champ de recherche prenait le focus, le navigateur faisait défiler le rail horizontalement pour l'amener à l'écran ; la bande d'icônes sortait par la gauche (mesuré : `left = -104`) puis revenait à mesure que la largeur arrivait, ce qui se lisait comme un bégaiement de la barre permanente. Les pistes écartées supposaient toutes de retarder le focus derrière un minuteur, c'est-à-dire de faire revenir la machinerie que le repli du bandeau vient de perdre. (2) La rangée de `.editor-layout` s'écrit **`grid-template-rows: minmax(0, 1fr)`** et jamais implicitement : le minimum d'une piste `auto` (comme d'une piste `fr`) vaut la contribution min-content de son contenu, donc la rangée grandissait à la hauteur DÉPLIÉE de la liste des résultats (mesuré : 8643 px pour 144 correspondances). Le rail dépassait la fenêtre de plusieurs milliers de pixels, sa poignée de largeur avec, donc la seule chose qu'on attrape pour resserrer le panneau descendait hors de l'écran (la languette de repli qui vivait alors sur ce bord y descendait avec elle : on ne pouvait plus refermer le panneau à la souris). Ça tenait par accident du temps où le rail était en `overflow: hidden`, un conteneur de défilement contribuant zéro ; le passage à `clip` a fait tomber l'accident, et c'est le même piège que le `minmax(0, 0fr)` du repli du bandeau, à un axe près. **Le rail ne prend en revanche ni `position` ni `z-index`** : il créerait un contexte d'empilement et enfermerait le `z-index: 20` de la palette de couleurs, qui doit passer au-dessus du fond plein écran à 19 (le BORD ne prend plus de `position` non plus, la languette qu'il portait ayant disparu ; un `relative` sans `z-index` y était sans danger, mais il ne sert plus à rien) ; et les 8 px de remplissage de la bande sont là pour que son `overflow: hidden` ne rogne pas la bague de focus des icônes. Mouvement réduit : `editor.css` a son propre bloc en fin de fichier, pendant de celui de `theme.css` (chaque fichier neutralise SES animations, et faire connaître des classes de l'éditeur au thème partagé casserait le sens des deux). **Rien ne replie le rail à la place de l'utilisateur** sur une fenêtre étroite : une règle CSS qui ramènerait un rail ouvert à la largeur de sa bande ferait mentir son `aria-expanded`, un `matchMedia` combattrait celui qui vient de l'ouvrir, et le projet a déjà tranché ce genre d'arbitrage (« le critère est le pointeur, pas la largeur ») ; contrairement au mur tactile la page reste utilisable, la colonne rétrécit et les rangées de réplique se replient (cf. « Édition réservée à l'ordinateur »). Repère `complementary` étiqueté « Structure, personnages et recherche » (et pas « panneau latéral » : l'étiquette nomme ce qu'il y a dedans), **avant** `<main>` dans le DOM comme à l'écran, ce qui redonne au plan et aux personnages la place qu'ils avaient dans la tabulation, du temps où les selects d'acte et de scène ouvraient les réglages du bandeau. Escape est écouté sur le panneau et non dans les sections : le rail est le seul à savoir à quelle icône rendre le focus, et sans ça une fermeture au clavier le laisse sur le `body` |
| Recherche et remplacement (Édition) | `src/editor/search.js` (pur, testé), `useSearch.js` (l'état), `SearchPanel.jsx` (l'affichage). Porte sur **le texte des répliques uniquement**, dans toute la pièce : ni titres d'acte ou de scène, ni noms de personnages (le renommage d'un personnage a déjà son geste dédié, et un remplacement global ne doit pas pouvoir toucher « Scène 1 »). Casse et accents ignorés par défaut, plus deux cases côte à côte, « Respecter la casse » et « Mot entier », en `flex-wrap` comme les quatre réglages de la Répétition et jamais en colonnes fixées (elles tiennent dans les 248 px du panneau par défaut et repassent l'une sous l'autre d'elles-mêmes quand on le resserre vers ses 200 px). **L'ordre des blocs**, du haut du panneau vers la liste : les deux cases, le champ de requête, le champ de remplacement, le compte et ses flèches. Ce sont les deux CHAMPS qui doivent se toucher, comme dans un éditeur de code : on tape l'un, on tape l'autre, on relit les deux avant de remplacer, et le chevron qui ouvre le second est ainsi à côté de ce qu'il fait apparaître. Les cases, qui règlent la recherche, se lisent très bien avant elle ; entre les deux champs (leur place d'origine) elles les séparaient de trois blocs, et le remplacement ne se lisait plus comme la suite de la requête. Le champ de remplacement est **décalé de la largeur du chevron plus sa gouttière** (26 + 4 px), donc aligné exactement sur le champ de requête : le bloc entier se lit comme le dépliage du bouton qui est à sa gauche. **Le remplacement est REPLIÉ par défaut**, comme dans un éditeur de code, derrière un chevron posé à gauche du champ de requête : la plupart du temps on cherche une réplique pour aller la retoucher à la main, et un champ « Remplacer par » toujours ouvert propose une réécriture en masse à qui voulait seulement retrouver un passage. Ce chevron est **encadré au repos** (filet, fond de carte) et **haut comme le champ** (`align-self: stretch`, donc aucune hauteur à tenir synchrone avec le remplissage du champ), et il passe à l'accent plein une fois ouvert : c'est le seul chemin vers le remplacement, or le premier essai n'était qu'un glyphe gris sans cadre au bord d'un champ de saisie, où il se lisait comme une décoration du champ et pas comme un bouton. Il est **démonté** et pas seulement masqué (rien à animer ici, donc rien ne doit rester dans le parcours au clavier), mais le texte déjà tapé survit, il vit dans `useSearch` comme la requête. Corollaire : **Ctrl+H** ouvre la recherche avec le remplacement déjà déplié, sans quoi un remplacement replié par défaut se paierait d'un clic de plus à chaque fois. Le drapeau d'ouverture vit dans `useSearch` et pas dans le panneau, comme la requête : changer de section du rail démonte le panneau. **Pas de regex**, et ce n'est pas un manque : le matching est un `indexOf`, donc rien à échapper, aucun état « expression invalide » à afficher et aucun retour sur trace catastrophique ; « Mot entier » est un test de frontière (`\p{L}`/`\p{N}`, ni `_` ni `'` ni `-` : « vous » est un mot entier dans « mettez-vous ») et pas un `\b`. **Le contrat central est un repliement à LONGUEUR CONSERVÉE** (`foldText`) : on replie point de code par point de code et on refuse tout candidat qui ne fait pas la même longueur que sa source, donc `foldText(raw).length === raw.length` et un indice dans le replié est un indice dans le texte BRUT, celui du textarea. C'est ce qui permet de passer les offsets directement à `setSelectionRange` et au découpage du remplacement, sans carte d'index nulle part. La table `UNIFY` (apostrophes et tirets typographiques ramenés au clavier) n'est pas un raffinement : la pièce publiée porte l'apostrophe courbe sur 253 répliques et la droite sur 324, donc sans elle « l'amour » n'en trouve que la moitié. Limites assumées et documentées en tête de module : `œ`, `æ`, `ﬁ`, `İ`, et un texte déjà stocké en NFD (`cutsGrapheme` refuse alors l'occurrence plutôt que d'en rendre une moitié, qui laisserait un accent orphelin après remplacement). **Ce n'est pas la normalisation du projet** : `scripts/normalize.py` reste la seule implémentation de celle-ci, elle compare une réplique au texte d'un enregistrement et voyage jusqu'au manifest, alors que ce repliement ne sert qu'à trouver une occurrence à l'écran et vit le temps d'un rendu. **Un seul itérateur d'occurrences** (`eachMatch`) sert la recherche, le remplacement d'une occurrence et le remplacement global : sinon « Tout remplacer » pourrait réécrire ce que la liste n'a jamais montré. Deux règles de pas à ne pas confondre : après une occurrence ACCEPTÉE on repart après elle (pas de recouvrement), après une occurrence REFUSÉE on repart d'UN cran, sinon une occurrence qui la chevauche disparaît. Les occurrences sont **toujours fraîches** (`useMemo` sur le script), jamais un instantané, qui ne serait pas seulement périmé mais faux, ses offsets pointant dans un texte disparu ; le repliement est mémorisé par OBJET réplique dans une `WeakMap`, ce qui n'est correct que parce que le reducer est immuable, et fait qu'une frappe ne replie qu'une réplique. **La correspondance courante est une ancre de position** (`{lineId, lineOrdinal, start}`) et pas un rang : les rangs glissent à chaque frappe. Quand aucune occurrence n'est exactement là (après un remplacement, après une frappe, après un changement de requête, après un Ctrl+Z sur un « Tout remplacer »), `currentIndex` vaut -1, le compte s'affiche, rien n'est marqué courant et le « suivant » reprend là où on en était : tout cela tombe du calcul dérivé, sans une ligne de code par cas. Résultats **groupés par acte/scène**, correspondance surlignée dans un extrait CENTRÉ sur elle (`matchExcerpt` et pas `excerpt` de `data.js`, qui tronque depuis le début et cacherait une occurrence au six-centième caractère d'une tirade), blancs aplatis à l'affichage seulement. **Aucun plafond d'affichage, et une liste FENÊTRÉE** (`ResultList` dans `SearchPanel.jsx`) : toutes les occurrences existent, y compris les 6216 d'une requête d'un seul caractère sur la vraie pièce, mais seule la tranche visible est rendue (une douzaine de rangées, une vingtaine en défilant). Le chemin pour y arriver s'est fait en trois temps, et les trois comptent. (1) Tout rendre coûtait à React **329 ms de tâche bloquante** pour cette requête, 88 ms dès 750 occurrences : la frappe bégayait. `content-visibility: auto` n'y faisait rien, il épargne la mise en page et la peinture, pas le travail de React. (2) `useDeferredValue` (`useSearch.js`) a réglé le RENDU, qui devient interruptible et abandonne la passe déjà périmée quand la frappe suivante arrive : on ne paie plus les états intermédiaires (« v », « vo », « vou » en tapant « vous »), et le pire cas tombe à 76 ms. Ça reste, et c'est la phase de COMMIT, qui ne s'interrompt pas : créer puis détruire des dizaines de milliers de nœuds se paie comptant. (3) D'où le fenêtrage, qui ramène toutes les tâches longues à **zéro**, mesuré sur chaque frappe. Les trois mécanismes restent en place, le deuxième servant encore à ne pas rendre les états intermédiaires.
Ce qui rend le fenêtrage possible est la **hauteur fixe** des rangées, et elle a d'abord été payée pour une autre raison : avec des rangées de hauteurs variables (44, 52 ou 62 px selon la longueur de l'extrait) et une hauteur seulement estimée, `scrollHeight` changeait à mesure que le défilement atteignait de nouvelles rangées (mesuré : 8278 px de dérive sur la course complète), donc le curseur du défileur se recalait sous la souris, restait au-dessus d'elle et descendait moins vite. Le contrat est maintenant écrit **des deux côtés** : 66 px par rangée (62 de contenu plus 4 de gouttière, en remplissage bas et pas en `gap`, qui ne compterait pas dans la hauteur de l'élément) et 30 px par en-tête de scène, dans `editor.css` comme dans `ResultList` ; les faire diverger décale la liste sous l'ascenseur. Le 62 est lui-même une addition (2 x 1 px de filet + 2 x 5 px de remplissage + 14 px de nom de personnage + 2 lignes de texte à 18 px), l'extrait est coupé net à deux lignes et l'en-tête à une seule. **La carte remplit sa rangée** (`.search-hit` en `height: 100%`) et **rien ne s'insère entre les deux**, sans quoi les cartes n'ont plus toutes la même hauteur : un `<li>` de trop entre la rangée et le bouton (élément de liste dans un élément de liste, donc du HTML invalide au passage) a suffi à faire retomber le pourcentage sur un parent à hauteur automatique, donc sur le contenu ; un extrait qui tient sur une ligne, ou une réplique dont le personnage a disparu, laissait alors 14 à 18 px de crème sous sa carte et l'interligne de la liste avait l'air de changer d'une rangée à l'autre. Corollaire dans le module pur : l'extrait est **dissymétrique** (`EXCERPT_BEFORE` 34, `EXCERPT_AFTER` 64), parce que l'occurrence doit être garantie visible dans ces deux lignes, donc ce qui la précède est bridé à peu près à une ligne, et parce qu'on lit vers l'avant. Deux conséquences de tuyauterie : la liste est **plate** (les en-têtes de scène y sont des éléments comme les autres, ce qui rend l'arithmétique des positions triviale là où un fenêtrage par groupe demanderait de découper chaque groupe), et **la correspondance courante est amenée à l'écran** quand elle change, sans quoi « suivant » marquerait une rangée qui n'est même pas rendue. Prix assumé, celui de toute liste fenêtrée : un lecteur d'écran n'annonce que les rangées rendues, pas les 6216 ; le compte, lui, est dit en clair juste au-dessus et reste le seul `aria-live` du panneau. Un plafond de 60 lignes a existé, avec sa ligne « N résultats de plus non affichés » sur le motif de `.dash-journal-more` ; il ne protégeait rien qu'on ait mesuré, et une liste tronquée dans le seul outil qui sert à faire le tour d'une pièce demandait de deviner ce qu'on ne voyait pas. Compte en « N correspondances dans M scènes », dont **la forme de phrase ne change jamais** selon les nombres (« dans 1 scène » compris) : il se réécrit à chaque frappe, et une phrase qui change de forme en tapant se lit comme un clignotement ; il est le seul `aria-live` du panneau, la liste bavarderait. Pas de « 3 sur 12 » à côté des flèches, la liste étant toujours affichée et marquant la ligne courante (`aria-current`). **Deux modes de navigation, et le second n'est pas un détail** : un clic sur un résultat focalise la réplique et y sélectionne le texte trouvé (on va éditer là), alors qu'Entrée et F3 sélectionnent SANS prendre le clavier, sinon le curseur partirait dans un textarea où Entrée crée déjà la réplique suivante et la touche ne se répéterait plus. Contrepartie connue : Chrome ne peint pas la sélection d'un textarea non focalisé, donc dans ce mode le repère visible est la ligne courante du panneau plus le défilement. La navigation passe par `focusRequest` (`App.jsx`), généralisation du `focusLineId` d'origine (`{lineId, selection, focus}`, effacé dès qu'il est honoré) : les quatre changements d'état vivent dans le même gestionnaire, donc React les regroupe en UN rendu où la scène cible est déjà désignée, la rangée se monte avec sa demande dans le même commit et l'effet tourne après, sur un textarea posé. Ni `setTimeout` ni `requestAnimationFrame`, et `React.memo` n'y fait rien : les autres rangées reçoivent `null`, superficiellement égal à leur rendu précédent. Raccourcis dans un effet **séparé** de celui d'annuler/rétablir, qui se réabonne à chaque édition : Ctrl+F (pris au navigateur exprès, sa recherche ne lit pas les textarea et une seule scène est montée ; il reprend la sélection de la réplique courante comme graine), Ctrl+H (idem, plus le remplacement déplié), F3 / Maj+F3, Escape. Le garde `if (e.defaultPrevented) return;` est obligatoire et pas défensif : `ConfirmModal` écoute Escape en phase de CAPTURE et appelle `preventDefault` sans `stopPropagation`, donc sans lui un Escape destiné à la modale fermerait aussi le panneau derrière. Entrée appartient au CHAMP et jamais à `window`. Focaliser le champ demande un COMPTEUR (`focusSeq`) là où la demande de focus d'une réplique n'en a pas besoin (elle s'auto-efface) : Ctrl+F sur un panneau déjà ouvert doit re-focaliser alors qu'aucun état ne change. Côté reducer, une seule action, `SET_LINE_TEXTS` (`[{lineId, text}]`, les ids de réplique étant uniques dans toute la pièce), appliquée par `applyTextEdits`, qui rend l'état PRÉCIS reçu quand rien ne change et garde l'identité des actes, scènes et répliques intacts (`mapAllLines` ne convient pas, il reconstruit tout) |
| Classes et helpers partagés à ne pas recopier | Dans `theme.css`, posées en JSX : **`.truncate`** (le triplet overflow/ellipsis/nowrap d'un nom qui ne doit pas pousser ses voisins ; quatre porteurs, la largeur maximale et le `flex` restent locaux, et l'appelant double toujours d'un `title`), **`.btn-tip`** (l'enveloppe qui porte l'infobulle d'un bouton qui s'éteint : un contrôle `disabled` ne reçoit aucun événement souris, donc son propre `title` ne s'affiche pas et l'explication n'arrive jamais au moment où elle sert ; l'`aria-label` reste sur le bouton, il ne dépend pas de son état. L'Édition l'emploie pour annuler/rétablir/télécharger, l'Enregistrement pour les quatre boutons de sa barre basse), **`.page-shell` / `.page-scroll`** (coquille à la hauteur de la fenêtre dont seul le contenu défile, réglée par `--shell-height` : l'Édition le pose à `100vh`, la Répartition garde le `100dvh` par défaut parce qu'elle s'ouvre au doigt), **`--shadow-float`** (l'ombre d'un calque qui FLOTTE et n'est pas posé sur la page : le modal et la pop-up « À vous… »), et la règle **`.checks-row label, .search-options label`** (la géométrie d'une étiquette de case à cocher, qui avait déjà divergé d'un pixel de gouttière entre les deux pages). Côté JS : **`src/shared/mountPage.jsx`** (le montage d'une page, identique pour les sept documents ; l'import de `theme.css` vit dedans et son ORDRE compte, d'où l'import de ce module AVANT `App.jsx` dans chaque point d'entrée) et **`myLineNumber`** dans `data.js`, à côté de `myLineNumbers` (le libellé « (3/12) » de mes cartes, `t` reçu en argument puisque ce module est couvert par `node --test`) |
| Chemins repo côté Python | `scripts/common.py` (`REPO_ROOT`, `write_json`) |
| Middleware dev data/clips | `serveRepoData` dans `vite.config.js` |

Pour tester les pages sans build : éditer `data/manifest.json` à la main puis
`npm run dev`.

`data/history.json` se **livre vide** (`{"runs": []}`) : le journal d'un dépôt est
l'histoire d'une troupe, pas une donnée d'exemple. Il ne l'est pas dans ce dépôt-ci
pendant le prototypage (de vrais dépôts y ont tourné, comme `data/script.json` et
`clips/` portent la vraie pièce) : c'est à vider avec eux avant publication, et pas
à la main d'ici là. `Journal` reste affiché
même vide (tableau et colonnes en place, une ligne d'explication à la place des
dépôts) : c'est le seul canal de retour du projet, donc il doit se faire
connaître avant le premier dépôt, pas apparaître le jour où il a une mauvaise
nouvelle à donner. Pour voir la page peuplée, copier le jeu d'exemple partagé,
qui couvre exprès les quatre cas d'affichage :

```bash
cp scripts/tests/history-example.json data/history.json && python3 scripts/build_manifest.py
```

C'est le **même fichier que les tests** (`test_history.py` vérifie sa forme et
`test_build_manifest.py` sa recopie dans le manifest) : un exemple qui dérive du
contrat casse la CI au lieu de mentir tranquillement à l'écran.
